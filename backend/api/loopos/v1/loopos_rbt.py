# yapf: disable
# isort: skip_file
# ruff: noqa
# mypy: disable-error-code="func-returns-value"



# To not generate code where imported names might get shadowed when a user
# specifies some name in their proto file to be the same as one of our imported
# names, (for example: a request field named `uuid`) we bind all imports to
# names that are forbidden in 'proto' and therefore can never collide.

# Standard imports.
from __future__ import annotations as IMPORT_future_annotations

# The following MUST appear before the rest of the imports, since those imports
# may be invalid (broken) if the generated code is mismatched with the installed
# libraries.
import reboot.versioning as IMPORT_reboot_versioning
IMPORT_reboot_versioning.check_generated_code_compatible("1.0.4")

# ATTENTION: no types in this file should be imported with their unqualified
#            name (e.g. `from typing import Any`). That would cause clashes
#            with user-defined methods that have the same name. Use
#            fully-qualified names (e.g. `IMPORT_typing.Any`) instead.
import asyncio as IMPORT_asyncio
import builtins as IMPORT_builtins
import contextvars as IMPORT_contextvars
import dataclasses as IMPORT_dataclasses
import google.protobuf.descriptor as IMPORT_google_protobuf_descriptor
import google.protobuf.json_format as IMPORT_google_protobuf_json_format
import google.protobuf.message as IMPORT_google_protobuf_message
import grpc as IMPORT_grpc
import grpc_status._async as IMPORT_rpc_status_async
from grpc_status import rpc_status as IMPORT_rpc_status_sync
import json as IMPORT_json
import os as IMPORT_os
import traceback as IMPORT_traceback
import uuid as IMPORT_uuid
import pickle as IMPORT_pickle
import reboot as IMPORT_reboot
import log.log as IMPORT_log_log   # type: ignore[import]
import typing as IMPORT_typing
import reboot.aio.backoff as IMPORT_reboot_aio_backoff
import functools as IMPORT_functools
from abc import abstractmethod as IMPORT_abc_abstractmethod
from datetime import datetime as IMPORT_datetime_datetime
from datetime import timedelta as IMPORT_datetime_timedelta
from datetime import timezone as IMPORT_datetime_timezone
from google.protobuf import timestamp_pb2 as IMPORT_google_protobuf_timestamp_pb2
from google.protobuf import wrappers_pb2 as IMPORT_google_protobuf_wrappers_pb2
from google.protobuf.empty_pb2 import Empty as IMPORT_google_protobuf_empty_pb2_Empty
import reboot.aio.tracing as IMPORT_reboot_aio_tracing
from google.rpc import status_pb2 as IMPORT_google_rpc_status_pb2
from tzlocal import get_localzone as IMPORT_tzlocal_get_localzone
import reboot.aio.call as IMPORT_reboot_aio_call
import reboot.aio.caller_id as IMPORT_reboot_aio_caller_id
import reboot.aio.contexts as IMPORT_reboot_aio_contexts
import reboot.aio.headers as IMPORT_reboot_aio_headers
import reboot.aio.idempotency as IMPORT_reboot_aio_idempotency
import reboot.aio.internals.channel_manager as IMPORT_reboot_aio_internals_channel_manager
import reboot.aio.internals.middleware as IMPORT_reboot_aio_internals_middleware
import reboot.aio.internals.tasks_cache as IMPORT_reboot_aio_internals_tasks_cache
import reboot.aio.internals.tasks_dispatcher as IMPORT_reboot_aio_internals_tasks_dispatcher
import reboot.aio.placement as IMPORT_reboot_aio_placement
import reboot.aio.servicers as IMPORT_reboot_aio_servicers
import reboot.aio.state_managers as IMPORT_reboot_aio_state_managers
import reboot.aio.stubs as IMPORT_reboot_aio_stubs
import reboot.aio.tasks as IMPORT_reboot_aio_tasks
import reboot.aio.types as IMPORT_reboot_aio_types
import reboot.aio.external as IMPORT_reboot_aio_external
import reboot.aio.workflows as IMPORT_reboot_aio_workflows
import reboot.aio.auth as IMPORT_reboot_aio_auth
import reboot.aio.auth.authorizers as IMPORT_reboot_aio_auth_authorizers
import reboot.aio.auth.token_verifiers as IMPORT_reboot_aio_auth_token_verifiers
import reboot.aio.aborted as IMPORT_reboot_aio_aborted
import reboot.settings as IMPORT_reboot_settings
import reboot.nodejs.python as IMPORT_reboot_nodejs_python
from reboot.time import DateTimeWithTimeZone as IMPORT_reboot_time_DateTimeWithTimeZone
import rbt.v1alpha1 as IMPORT_rbt_v1alpha1
import rbt.v1alpha1.nodejs_pb2 as IMPORT_rbt_v1alpha1_nodejs_pb2
import google.protobuf.any_pb2 as IMPORT_google_protobuf_any_pb2
import sys as IMPORT_sys
import mcp.server.fastmcp as IMPORT_mcp_server_fastmcp
import reboot.mcp.context as IMPORT_reboot_mcp_context
import reboot.mcp.ui as IMPORT_reboot_mcp_ui


# User defined or referenced imports.
import google.protobuf.any_pb2
import google.protobuf.descriptor_pb2
import google.protobuf.empty_pb2
import google.protobuf.struct_pb2
import google.protobuf.timestamp_pb2
import loopos.v1.loopos_pb2
import loopos.v1.loopos_pb2_grpc
import rbt.v1alpha1.options_pb2
import rbt.v1alpha1.tasks_pb2
import rbt.v1alpha1.tasks_pb2_grpc

logger = IMPORT_log_log.get_logger(__name__)

# We won't validate Pydantic state models while they are under construction.
states_being_constructed: set[str] = set()
# To support writers seeing partial updates of transactions,
# and transactions seeing updates from writers, we need to store
# a reference to the latest state in an ongoing transaction.
#
# Moreover, we need to update that _reference_ after each writer
# executes within a transaction.
ongoing_transaction_states: dict[str, IMPORT_reboot_api.Model] = {}


# Helper to get the `ongoing_transaction_states` dictionary key.
# The key should contain both the state ID and the state type name
# to avoid conflicts when multiple states share the same ID.
def ongoing_transaction_state_key(
    context: IMPORT_reboot_aio_contexts.ReaderContext
    | IMPORT_reboot_aio_contexts.WriterContext
    | IMPORT_reboot_aio_contexts.TransactionContext,
) -> str:
    return f"{context.state_type_name}/{context.state_id}"

class Unset:
    pass

UNSET = Unset()


import pydantic as IMPORT_pydantic
from loopos.v1.loopos import api as IMPORT_api
import reboot.api as IMPORT_reboot_api


def UserToProto(state: User.State, protobuf_state: loopos.v1.loopos_pb2.User):
    assert isinstance(state, IMPORT_reboot_api.Model)
    proto = IMPORT_reboot_api.pydantic_to_proto(
        state,
        User.State,
        loopos.v1.loopos_pb2.User,
    )
    protobuf_state.CopyFrom(proto)

def UserFromProto(
    state: loopos.v1.loopos_pb2.User,
    is_initial_state: bool = False,
) -> User.State:
    assert isinstance(state, IMPORT_google_protobuf_message.Message)
    if is_initial_state:
        # Currently we have a special case when we are creating a state, so
        # 'is_initial_state=True' is passed. In that case we skip any validation
        # and create the Pydantic model using 'model_construct', which will
        # apply all 'default' and 'default_factory' values defined in the
        # Pydantic model, since we know the Protobuf 'state' will be always
        # "empty" and later when the constructor is done, we will do
        # the proper validation and convert that Pydantic model back to
        # Protobuf.
        return User.State.model_construct()

    return IMPORT_reboot_api.proto_to_pydantic(
        state,
        User.State,
    )
def UserIngestTextMessageResponseToProto(
    response: User.IngestTextMessageResponse
) -> loopos.v1.loopos_pb2.UserIngestTextMessageResponse:
    assert isinstance(response, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        response,
        User.IngestTextMessageResponse,
        loopos.v1.loopos_pb2.UserIngestTextMessageResponse
    )

def UserIngestTextMessageResponseFromProto(
    response: loopos.v1.loopos_pb2.UserIngestTextMessageResponse
) -> User.IngestTextMessageResponse:
    assert isinstance(response, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        response,
        User.IngestTextMessageResponse
    )

def UserIngestTextMessageRequestToProto(
    request: User.IngestTextMessageRequest
) -> loopos.v1.loopos_pb2.UserIngestTextMessageRequest:
    assert isinstance(request, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        request,
        User.IngestTextMessageRequest,
        loopos.v1.loopos_pb2.UserIngestTextMessageRequest,
    )

def UserIngestTextMessageRequestFromProto(
    request: loopos.v1.loopos_pb2.UserIngestTextMessageRequest
) -> User.IngestTextMessageRequest:
    assert isinstance(request, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        request,
        User.IngestTextMessageRequest
    )

def UserIngestTextMessageRequestFromInputFields(
    property_id: str | Unset,
    text: str | Unset,
    sender_user_id: str | Unset,
):
    assert User.IngestTextMessageRequest is not None


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(property_id, Unset):
        __args__['property_id'] = property_id
    if not isinstance(text, Unset):
        __args__['text'] = text
    if not isinstance(sender_user_id, Unset):
        __args__['sender_user_id'] = sender_user_id

    return User.IngestTextMessageRequest(
        **__args__,
    )

def UserIngestVoiceNoteResponseToProto(
    response: User.IngestVoiceNoteResponse
) -> loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse:
    assert isinstance(response, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        response,
        User.IngestVoiceNoteResponse,
        loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse
    )

def UserIngestVoiceNoteResponseFromProto(
    response: loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse
) -> User.IngestVoiceNoteResponse:
    assert isinstance(response, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        response,
        User.IngestVoiceNoteResponse
    )

def UserIngestVoiceNoteRequestToProto(
    request: User.IngestVoiceNoteRequest
) -> loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest:
    assert isinstance(request, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        request,
        User.IngestVoiceNoteRequest,
        loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest,
    )

def UserIngestVoiceNoteRequestFromProto(
    request: loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest
) -> User.IngestVoiceNoteRequest:
    assert isinstance(request, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        request,
        User.IngestVoiceNoteRequest
    )

def UserIngestVoiceNoteRequestFromInputFields(
    property_id: str | Unset,
    audio_url: str | Unset,
    sender_user_id: str | Unset,
):
    assert User.IngestVoiceNoteRequest is not None


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(property_id, Unset):
        __args__['property_id'] = property_id
    if not isinstance(audio_url, Unset):
        __args__['audio_url'] = audio_url
    if not isinstance(sender_user_id, Unset):
        __args__['sender_user_id'] = sender_user_id

    return User.IngestVoiceNoteRequest(
        **__args__,
    )

def UserListTicketsResponseToProto(
    response: User.ListTicketsResponse
) -> loopos.v1.loopos_pb2.UserListTicketsResponse:
    assert isinstance(response, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        response,
        User.ListTicketsResponse,
        loopos.v1.loopos_pb2.UserListTicketsResponse
    )

def UserListTicketsResponseFromProto(
    response: loopos.v1.loopos_pb2.UserListTicketsResponse
) -> User.ListTicketsResponse:
    assert isinstance(response, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        response,
        User.ListTicketsResponse
    )

def UserListTicketsRequestToProto(
) -> google.protobuf.empty_pb2.Empty:
    assert issubclass(google.protobuf.empty_pb2.Empty, IMPORT_google_protobuf_empty_pb2_Empty)
    return google.protobuf.empty_pb2.Empty()


def UserQueryBrainResponseToProto(
    response: User.QueryBrainResponse
) -> loopos.v1.loopos_pb2.UserQueryBrainResponse:
    assert isinstance(response, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        response,
        User.QueryBrainResponse,
        loopos.v1.loopos_pb2.UserQueryBrainResponse
    )

def UserQueryBrainResponseFromProto(
    response: loopos.v1.loopos_pb2.UserQueryBrainResponse
) -> User.QueryBrainResponse:
    assert isinstance(response, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        response,
        User.QueryBrainResponse
    )

def UserQueryBrainRequestToProto(
    request: User.QueryBrainRequest
) -> loopos.v1.loopos_pb2.UserQueryBrainRequest:
    assert isinstance(request, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        request,
        User.QueryBrainRequest,
        loopos.v1.loopos_pb2.UserQueryBrainRequest,
    )

def UserQueryBrainRequestFromProto(
    request: loopos.v1.loopos_pb2.UserQueryBrainRequest
) -> User.QueryBrainRequest:
    assert isinstance(request, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        request,
        User.QueryBrainRequest
    )

def UserQueryBrainRequestFromInputFields(
    query: str | Unset,
    property_id: str | Unset,
):
    assert User.QueryBrainRequest is not None


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(query, Unset):
        __args__['query'] = query
    if not isinstance(property_id, Unset):
        __args__['property_id'] = property_id

    return User.QueryBrainRequest(
        **__args__,
    )

def UserLiveStateResponseToProto(
    response: User.LiveStateResponse
) -> loopos.v1.loopos_pb2.UserLiveStateResponse:
    assert isinstance(response, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        response,
        User.LiveStateResponse,
        loopos.v1.loopos_pb2.UserLiveStateResponse
    )

def UserLiveStateResponseFromProto(
    response: loopos.v1.loopos_pb2.UserLiveStateResponse
) -> User.LiveStateResponse:
    assert isinstance(response, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        response,
        User.LiveStateResponse
    )

def UserLiveStateRequestToProto(
) -> google.protobuf.empty_pb2.Empty:
    assert issubclass(google.protobuf.empty_pb2.Empty, IMPORT_google_protobuf_empty_pb2_Empty)
    return google.protobuf.empty_pb2.Empty()


def UserCreateResponseToProto(
    response: None
) -> google.protobuf.empty_pb2.Empty:
    assert response is None
    return google.protobuf.empty_pb2.Empty()

def UserCreateResponseFromProto(
    response: google.protobuf.empty_pb2.Empty
) -> None:
    assert isinstance(response, IMPORT_google_protobuf_empty_pb2_Empty)
    return None

def UserCreateRequestToProto(
) -> google.protobuf.empty_pb2.Empty:
    assert issubclass(google.protobuf.empty_pb2.Empty, IMPORT_google_protobuf_empty_pb2_Empty)
    return google.protobuf.empty_pb2.Empty()



def OpsTicketToProto(state: OpsTicket.State, protobuf_state: loopos.v1.loopos_pb2.OpsTicket):
    assert isinstance(state, IMPORT_reboot_api.Model)
    proto = IMPORT_reboot_api.pydantic_to_proto(
        state,
        OpsTicket.State,
        loopos.v1.loopos_pb2.OpsTicket,
    )
    protobuf_state.CopyFrom(proto)

def OpsTicketFromProto(
    state: loopos.v1.loopos_pb2.OpsTicket,
    is_initial_state: bool = False,
) -> OpsTicket.State:
    assert isinstance(state, IMPORT_google_protobuf_message.Message)
    if is_initial_state:
        # Currently we have a special case when we are creating a state, so
        # 'is_initial_state=True' is passed. In that case we skip any validation
        # and create the Pydantic model using 'model_construct', which will
        # apply all 'default' and 'default_factory' values defined in the
        # Pydantic model, since we know the Protobuf 'state' will be always
        # "empty" and later when the constructor is done, we will do
        # the proper validation and convert that Pydantic model back to
        # Protobuf.
        return OpsTicket.State.model_construct()

    return IMPORT_reboot_api.proto_to_pydantic(
        state,
        OpsTicket.State,
    )
def OpsTicketCreateResponseToProto(
    response: None
) -> google.protobuf.empty_pb2.Empty:
    assert response is None
    return google.protobuf.empty_pb2.Empty()

def OpsTicketCreateResponseFromProto(
    response: google.protobuf.empty_pb2.Empty
) -> None:
    assert isinstance(response, IMPORT_google_protobuf_empty_pb2_Empty)
    return None

def OpsTicketCreateRequestToProto(
    request: OpsTicket.CreateRequest
) -> loopos.v1.loopos_pb2.OpsTicketCreateRequest:
    assert isinstance(request, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        request,
        OpsTicket.CreateRequest,
        loopos.v1.loopos_pb2.OpsTicketCreateRequest,
    )

def OpsTicketCreateRequestFromProto(
    request: loopos.v1.loopos_pb2.OpsTicketCreateRequest
) -> OpsTicket.CreateRequest:
    assert isinstance(request, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        request,
        OpsTicket.CreateRequest
    )

def OpsTicketCreateRequestFromInputFields(
    property_id: str | Unset,
    source: str | Unset,
    raw_input: str | Unset,
    transcript_native: str | Unset,
    transcript_en: str | Unset,
    detected_language: str | Unset,
    sender_user_id: str | Unset,
    created_at: str | Unset,
):
    assert OpsTicket.CreateRequest is not None


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(property_id, Unset):
        __args__['property_id'] = property_id
    if not isinstance(source, Unset):
        __args__['source'] = source
    if not isinstance(raw_input, Unset):
        __args__['raw_input'] = raw_input
    if not isinstance(transcript_native, Unset):
        __args__['transcript_native'] = transcript_native
    if not isinstance(transcript_en, Unset):
        __args__['transcript_en'] = transcript_en
    if not isinstance(detected_language, Unset):
        __args__['detected_language'] = detected_language
    if not isinstance(sender_user_id, Unset):
        __args__['sender_user_id'] = sender_user_id
    if not isinstance(created_at, Unset):
        __args__['created_at'] = created_at

    return OpsTicket.CreateRequest(
        **__args__,
    )

def OpsTicketTriageResponseToProto(
    response: None
) -> google.protobuf.empty_pb2.Empty:
    assert response is None
    return google.protobuf.empty_pb2.Empty()

def OpsTicketTriageResponseFromProto(
    response: google.protobuf.empty_pb2.Empty
) -> None:
    assert isinstance(response, IMPORT_google_protobuf_empty_pb2_Empty)
    return None

def OpsTicketTriageRequestToProto(
) -> google.protobuf.empty_pb2.Empty:
    assert issubclass(google.protobuf.empty_pb2.Empty, IMPORT_google_protobuf_empty_pb2_Empty)
    return google.protobuf.empty_pb2.Empty()


def OpsTicketAcknowledgeDispatchResponseToProto(
    response: None
) -> google.protobuf.empty_pb2.Empty:
    assert response is None
    return google.protobuf.empty_pb2.Empty()

def OpsTicketAcknowledgeDispatchResponseFromProto(
    response: google.protobuf.empty_pb2.Empty
) -> None:
    assert isinstance(response, IMPORT_google_protobuf_empty_pb2_Empty)
    return None

def OpsTicketAcknowledgeDispatchRequestToProto(
    request: OpsTicket.AcknowledgeDispatchRequest
) -> loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest:
    assert isinstance(request, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        request,
        OpsTicket.AcknowledgeDispatchRequest,
        loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest,
    )

def OpsTicketAcknowledgeDispatchRequestFromProto(
    request: loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest
) -> OpsTicket.AcknowledgeDispatchRequest:
    assert isinstance(request, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        request,
        OpsTicket.AcknowledgeDispatchRequest
    )

def OpsTicketAcknowledgeDispatchRequestFromInputFields(
    dispatch_id: str | Unset,
):
    assert OpsTicket.AcknowledgeDispatchRequest is not None


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(dispatch_id, Unset):
        __args__['dispatch_id'] = dispatch_id

    return OpsTicket.AcknowledgeDispatchRequest(
        **__args__,
    )

def OpsTicketDispatchWithEscalationResponseToProto(
    response: OpsTicket.DispatchWithEscalationResponse
) -> loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse:
    assert isinstance(response, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        response,
        OpsTicket.DispatchWithEscalationResponse,
        loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse
    )

def OpsTicketDispatchWithEscalationResponseFromProto(
    response: loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse
) -> OpsTicket.DispatchWithEscalationResponse:
    assert isinstance(response, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        response,
        OpsTicket.DispatchWithEscalationResponse
    )

def OpsTicketDispatchWithEscalationRequestToProto(
    request: OpsTicket.DispatchWithEscalationRequest
) -> loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationRequest:
    assert isinstance(request, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        request,
        OpsTicket.DispatchWithEscalationRequest,
        loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationRequest,
    )

def OpsTicketDispatchWithEscalationRequestFromProto(
    request: loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationRequest
) -> OpsTicket.DispatchWithEscalationRequest:
    assert isinstance(request, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        request,
        OpsTicket.DispatchWithEscalationRequest
    )

def OpsTicketDispatchWithEscalationRequestFromInputFields(
    vendor_id: str | Unset,
    cost_estimate_usd: float | Unset,
):
    assert OpsTicket.DispatchWithEscalationRequest is not None


    __args__: dict[str, IMPORT_typing.Any] = {}

    if not isinstance(vendor_id, Unset):
        __args__['vendor_id'] = vendor_id
    if not isinstance(cost_estimate_usd, Unset):
        __args__['cost_estimate_usd'] = cost_estimate_usd

    return OpsTicket.DispatchWithEscalationRequest(
        **__args__,
    )

def OpsTicketProposeNewRuleResponseToProto(
    response: OpsTicket.ProposeNewRuleResponse
) -> loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse:
    assert isinstance(response, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        response,
        OpsTicket.ProposeNewRuleResponse,
        loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse
    )

def OpsTicketProposeNewRuleResponseFromProto(
    response: loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse
) -> OpsTicket.ProposeNewRuleResponse:
    assert isinstance(response, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        response,
        OpsTicket.ProposeNewRuleResponse
    )

def OpsTicketProposeNewRuleRequestToProto(
) -> google.protobuf.empty_pb2.Empty:
    assert issubclass(google.protobuf.empty_pb2.Empty, IMPORT_google_protobuf_empty_pb2_Empty)
    return google.protobuf.empty_pb2.Empty()


def OpsTicketShowBrainSourcesResponseToProto(
    response: OpsTicket.ShowBrainSourcesResponse
) -> loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse:
    assert isinstance(response, IMPORT_reboot_api.Model)
    return IMPORT_reboot_api.pydantic_to_proto(
        response,
        OpsTicket.ShowBrainSourcesResponse,
        loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse
    )

def OpsTicketShowBrainSourcesResponseFromProto(
    response: loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse
) -> OpsTicket.ShowBrainSourcesResponse:
    assert isinstance(response, IMPORT_google_protobuf_message.Message)
    return IMPORT_reboot_api.proto_to_pydantic(
        response,
        OpsTicket.ShowBrainSourcesResponse
    )

def OpsTicketShowBrainSourcesRequestToProto(
) -> google.protobuf.empty_pb2.Empty:
    assert issubclass(google.protobuf.empty_pb2.Empty, IMPORT_google_protobuf_empty_pb2_Empty)
    return google.protobuf.empty_pb2.Empty()



############################ Legacy gRPC Servicers ############################
# This section is relevant (only) for servicers that implement a legacy gRPC
# service in a Reboot context. It is irrelevant to clients.

def MakeLegacyGrpcServiceable(
    # A legacy gRPC servicer type can't be more specific than `type`,
    # because legacy gRPC servicers (as generated by the gRPC `protoc`
    # plugin) do not share any common base class other than `object`.
    servicer_type: type
) -> IMPORT_reboot_aio_servicers.Serviceable:
    raise ValueError(f"Unknown legacy gRPC servicer type '{servicer_type}'")



############################ Reboot Servicer Middlewares ############################
# This section is relevant (only) for servicers implementing a Reboot servicer. It
# is irrelevant to clients, except for the fact that some clients are _also_ such
# servicers.

# For internal calls, we can use a magic token to bypass token verification and
# authorization checks. The token provides no auth information (e.g.,
# `context.auth is None`).
__internal_magic_token__: str = f'internal-{IMPORT_uuid.uuid4()}'

class UserServicerMiddleware(IMPORT_reboot_aio_internals_middleware.Middleware):

    def __init__(
        self,
        *,
        servicer: UserBaseServicer,
        application_id: IMPORT_reboot_aio_types.ApplicationId,
        server_id: IMPORT_reboot_aio_types.ServerId,
        state_manager: IMPORT_reboot_aio_state_managers.StateManager,
        placement_client: IMPORT_reboot_aio_placement.PlacementClient,
        channel_manager: IMPORT_reboot_aio_internals_channel_manager._ChannelManager,
        tasks_cache: IMPORT_reboot_aio_internals_tasks_cache.TasksCache,
        token_verifier: IMPORT_typing.Optional[IMPORT_reboot_aio_auth_token_verifiers.TokenVerifier],
        effect_validation: IMPORT_reboot_aio_contexts.EffectValidation,
        ready: IMPORT_asyncio.Event,
    ):
        super().__init__(
            application_id=application_id,
            server_id=server_id,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            service_names = [
                IMPORT_reboot_aio_types.ServiceName("loopos.v1.UserMethods"),
            ],
            placement_client=placement_client,
            channel_manager=channel_manager,
            effect_validation=effect_validation,
        )

        self._servicer = servicer
        self._state_manager = state_manager
        self.tasks_dispatcher = IMPORT_reboot_aio_internals_tasks_dispatcher.TasksDispatcher(
            application_id=application_id,
            dispatch=self.dispatch,
            tasks_cache=tasks_cache,
            ready=ready,
            complete_task=self._state_manager.complete_task,
        )

        # Store the type of each method's request so that stored requests can be
        # deserialized into the correct type.
        self.request_type_by_method_name: dict[str, type[IMPORT_google_protobuf_message.Message]] = {
            'IngestTextMessage': loopos.v1.loopos_pb2.UserIngestTextMessageRequest,
            'IngestVoiceNote': loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest,
            'ListTickets': google.protobuf.empty_pb2.Empty,
            'QueryBrain': loopos.v1.loopos_pb2.UserQueryBrainRequest,
            'LiveState': google.protobuf.empty_pb2.Empty,
            'Create': google.protobuf.empty_pb2.Empty,
        }

        # Get authorizer, if any, converting from a rule if necessary.
        def convert_authorizer_rule_if_necessary(
            authorizer_or_rule: IMPORT_typing.Optional[
                IMPORT_reboot_aio_auth_authorizers.Authorizer | IMPORT_reboot_aio_auth_authorizers.AuthorizerRule
            ]
        ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer:

            # If no authorizer or rule is provided, return the default
            # authorizer which allows if app internal or allows if in
            # dev mode (and logs some warnings to help the user
            # realize where they are missing authorization).
            if authorizer_or_rule is None:
                return IMPORT_reboot_aio_auth_authorizers.DefaultAuthorizer(
                    'User',
                    is_user_type=True,
                )

            if isinstance(authorizer_or_rule, IMPORT_reboot_aio_auth_authorizers.AuthorizerRule):
                return UserAuthorizer(
                    _default=authorizer_or_rule
                )

            return authorizer_or_rule

        self._authorizer = convert_authorizer_rule_if_necessary(
            servicer.authorizer()
        )

        # Create token verifier.
        self._token_verifier: IMPORT_typing.Optional[IMPORT_reboot_aio_auth_token_verifiers.TokenVerifier] = (
            servicer.token_verifier() or token_verifier
        )

        # Since users specify errors as proto messages they can't raise them
        # directly - to do so they have to use the `Aborted` wrapper, which will
        # hold the original proto message. On errors we'll need to check whether
        # such wrappers hold a proto message for a specified error, so we can
        # avoid retrying tasks that complete with a specified error.
        self._specified_errors_by_service_method_name: dict[str, list[str]] = {
        }


    def add_to_server(self, server: IMPORT_grpc.aio.Server) -> None:
        loopos.v1.loopos_pb2_grpc.add_UserMethodsServicer_to_server(
            self, server
        )

    async def inspect(self, state_ref: IMPORT_reboot_aio_types.StateRef) -> IMPORT_typing.AsyncIterator[IMPORT_google_protobuf_message.Message]:
        """Implementation of `Middleware.inspect()`."""
        context = self.create_context(
            headers=IMPORT_reboot_aio_headers.Headers(
                application_id=self.application_id,
                state_ref=state_ref,
            ),
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            method="inspect",
            context_type=IMPORT_reboot_aio_contexts.ReaderContext,
        )

        async with self._state_manager.streaming_reader_idempotency_key(
            context,
            self._servicer.__state_type__,
            authorize=None,
        ) as states:
            async for (state, idempotency_key) in states:
                yield state

    async def react_query(
        self,
        headers: IMPORT_reboot_aio_headers.Headers,
        method: str,
        request_bytes: bytes,
    ) -> IMPORT_typing.AsyncIterator[tuple[IMPORT_typing.Optional[IMPORT_google_protobuf_message.Message], list[IMPORT_uuid.UUID]]]:
        """Returns the response of calling 'method' given a message
        deserialized from the provided 'request_bytes' for each state
        update that creates a different response.

        # The caller (react.py) should have already ensured that this server
        # is authoritative for this traffic.
        assert self.placement_client.server_for_actor(
            headers.application_id,
            headers.state_ref,
        ) == self._server_id

        NOTE: only unary reader methods are supported."""
        # Need to define these up here since we can only do that once.
        last_response: IMPORT_typing.Optional[IMPORT_google_protobuf_message.Message] = None
        aggregated_idempotency_keys: list[IMPORT_uuid.UUID] = []
        if method == 'IngestTextMessage':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer User."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'IngestVoiceNote':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer User."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'ListTickets':

            context = self.create_context(
                headers=headers,
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                method='ListTickets',
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            )

            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="ListTickets() (reactively)",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                context.auth = await self._maybe_verify_token(
                    headers=headers, method='ListTickets'
                )

                request = google.protobuf.empty_pb2.Empty()
                request.ParseFromString(request_bytes)

                async with self._state_manager.reactively(
                    context,
                    self._servicer.__state_type__,
                    authorize=self._maybe_authorize(
                        method_name='loopos.v1.UserMethods.ListTickets',
                        headers=headers,
                        auth=context.auth,
                        request=request,
                    ),
                ) as states:
                    async for (state, idempotency_keys) in states:

                        aggregated_idempotency_keys.extend(idempotency_keys)

                        # Note: This does not do any defensive copying currently:
                        # see https://github.com/reboot-dev/respect/issues/2636.
                        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
                        async def run__ListTickets(validating_effects: bool) -> IMPORT_google_protobuf_message.Message:
                            return await self.__ListTickets(
                                context,
                                state,
                                request,
                                validating_effects=validating_effects,
                            )

                        response = await run__ListTickets()

                        if last_response != response:
                            yield (response, aggregated_idempotency_keys)
                            last_response = response
                        else:
                            yield (None, aggregated_idempotency_keys)

                        aggregated_idempotency_keys.clear()
        elif method == 'QueryBrain':

            context = self.create_context(
                headers=headers,
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                method='QueryBrain',
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            )

            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="QueryBrain() (reactively)",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                context.auth = await self._maybe_verify_token(
                    headers=headers, method='QueryBrain'
                )

                request = loopos.v1.loopos_pb2.UserQueryBrainRequest()
                request.ParseFromString(request_bytes)

                async with self._state_manager.reactively(
                    context,
                    self._servicer.__state_type__,
                    authorize=self._maybe_authorize(
                        method_name='loopos.v1.UserMethods.QueryBrain',
                        headers=headers,
                        auth=context.auth,
                        request=request,
                    ),
                ) as states:
                    async for (state, idempotency_keys) in states:

                        aggregated_idempotency_keys.extend(idempotency_keys)

                        # Note: This does not do any defensive copying currently:
                        # see https://github.com/reboot-dev/respect/issues/2636.
                        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
                        async def run__QueryBrain(validating_effects: bool) -> IMPORT_google_protobuf_message.Message:
                            return await self.__QueryBrain(
                                context,
                                state,
                                request,
                                validating_effects=validating_effects,
                            )

                        response = await run__QueryBrain()

                        if last_response != response:
                            yield (response, aggregated_idempotency_keys)
                            last_response = response
                        else:
                            yield (None, aggregated_idempotency_keys)

                        aggregated_idempotency_keys.clear()
        elif method == 'LiveState':

            context = self.create_context(
                headers=headers,
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                method='LiveState',
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            )

            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="LiveState() (reactively)",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                context.auth = await self._maybe_verify_token(
                    headers=headers, method='LiveState'
                )

                request = google.protobuf.empty_pb2.Empty()
                request.ParseFromString(request_bytes)

                async with self._state_manager.reactively(
                    context,
                    self._servicer.__state_type__,
                    authorize=self._maybe_authorize(
                        method_name='loopos.v1.UserMethods.LiveState',
                        headers=headers,
                        auth=context.auth,
                        request=request,
                    ),
                ) as states:
                    async for (state, idempotency_keys) in states:

                        aggregated_idempotency_keys.extend(idempotency_keys)

                        # Note: This does not do any defensive copying currently:
                        # see https://github.com/reboot-dev/respect/issues/2636.
                        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
                        async def run__LiveState(validating_effects: bool) -> IMPORT_google_protobuf_message.Message:
                            return await self.__LiveState(
                                context,
                                state,
                                request,
                                validating_effects=validating_effects,
                            )

                        response = await run__LiveState()

                        if last_response != response:
                            yield (response, aggregated_idempotency_keys)
                            last_response = response
                        else:
                            yield (None, aggregated_idempotency_keys)

                        aggregated_idempotency_keys.clear()
        elif method == 'Create':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer User."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        else:
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer User."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' not found"
            )
            yield  # Unreachable but necessary for mypy.

    async def react_mutate(
        self,
        headers: IMPORT_reboot_aio_headers.Headers,
        method: str,
        request_bytes: bytes,
    ) -> IMPORT_google_protobuf_message.Message:
        """Returns the response of calling 'method' given a message
        deserialized from the provided 'request_bytes'."""
        if method == 'IngestTextMessage':
            request = loopos.v1.loopos_pb2.UserIngestTextMessageRequest()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = loopos.v1.loopos_pb2_grpc.UserMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.IngestTextMessage(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise User.IngestTextMessageAborted.from_status(
                            status
                        ) from None
                    raise User.IngestTextMessageAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        elif method == 'IngestVoiceNote':
            request = loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = loopos.v1.loopos_pb2_grpc.UserMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.IngestVoiceNote(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise User.IngestVoiceNoteAborted.from_status(
                            status
                        ) from None
                    raise User.IngestVoiceNoteAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        elif method == 'ListTickets':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                "Method 'ListTickets' is invalid for servicer User."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=f"Method '{method}' is invalid"
            )
        elif method == 'QueryBrain':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                "Method 'QueryBrain' is invalid for servicer User."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=f"Method '{method}' is invalid"
            )
        elif method == 'LiveState':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                "Method 'LiveState' is invalid for servicer User."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=f"Method '{method}' is invalid"
            )
        elif method == 'Create':
            request = google.protobuf.empty_pb2.Empty()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = loopos.v1.loopos_pb2_grpc.UserMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.Create(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise User.CreateAborted.from_status(
                            status
                        ) from None
                    raise User.CreateAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        else:
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                f"Method '{method}' is invalid for servicer User."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' not found"
            )

    async def dispatch(
        self,
        task: IMPORT_reboot_aio_tasks.TaskEffect,
        *,
        only_validate: bool = False,
        on_loop_iteration: IMPORT_reboot_aio_internals_tasks_dispatcher.OnLoopIterationCallable = (lambda iteration, next_iteration_schedule: None),
    ) -> IMPORT_reboot_aio_internals_tasks_dispatcher.TaskResponseOrStatus:
        """Dispatches the tasks to execute unless 'only_validate' is set to
        true, in which case just ensures that the task actually exists.
        Note that this function will be called *by* tasks_dispatcher; it will
        not itself call into tasks_dispatcher."""

        if 'IngestTextMessage' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (loopos.v1.loopos_pb2.UserIngestTextMessageResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_IngestTextMessage(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (UserWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).IngestTextMessage(
                            UserIngestTextMessageRequestFromProto(IMPORT_typing.cast(loopos.v1.loopos_pb2.UserIngestTextMessageRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('loopos.v1.UserMethods.IngestTextMessage', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_IngestTextMessage(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    method='IngestTextMessage',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'IngestVoiceNote' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_IngestVoiceNote(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (UserWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).IngestVoiceNote(
                            UserIngestVoiceNoteRequestFromProto(IMPORT_typing.cast(loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('loopos.v1.UserMethods.IngestVoiceNote', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_IngestVoiceNote(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    method='IngestVoiceNote',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'ListTickets' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (loopos.v1.loopos_pb2.UserListTicketsResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_ListTickets(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (UserWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).ListTickets(
                            bearer_token=__internal_magic_token__,
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('loopos.v1.UserMethods.ListTickets', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_ListTickets(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    method='ListTickets',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'QueryBrain' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (loopos.v1.loopos_pb2.UserQueryBrainResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_QueryBrain(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (UserWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).QueryBrain(
                            UserQueryBrainRequestFromProto(IMPORT_typing.cast(loopos.v1.loopos_pb2.UserQueryBrainRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('loopos.v1.UserMethods.QueryBrain', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_QueryBrain(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    method='QueryBrain',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'LiveState' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (loopos.v1.loopos_pb2.UserLiveStateResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_LiveState(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (UserWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).LiveState(
                            bearer_token=__internal_magic_token__,
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('loopos.v1.UserMethods.LiveState', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_LiveState(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    method='LiveState',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'Create' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (google.protobuf.empty_pb2.Empty(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_Create(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (UserWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).Create(
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('loopos.v1.UserMethods.Create', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_Create(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    method='Create',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )

        # There are no tasks for this service.
        start_or_validate = "start" if not only_validate else "validate"
        raise RuntimeError(
            f"Attempted to {start_or_validate} task '{task.method_name}' "
            f"on 'User' which does not exist"
        )

    # User specific methods:
    async def __IngestTextMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: loopos.v1.loopos_pb2.User,
        request: loopos.v1.loopos_pb2.UserIngestTextMessageRequest,
        *,
        validating_effects: bool,
    ) -> loopos.v1.loopos_pb2.UserIngestTextMessageResponse:
        try:
            typed_state: User.State = UserFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            # TODO: assert that there are no ongoing transactions for this state.
            #
            # The `typed_state` should be already validated above, so we can
            # just store it here.
            ongoing_transaction_states[ongoing_transaction_state_key(context)] = typed_state
            response = (
                await self._servicer._IngestTextMessage(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            UserToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [loopos.v1.loopos_pb2.UserIngestTextMessageResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='User.IngestTextMessage',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = User.IngestTextMessageAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'loopos.v1.User.IngestTextMessage') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'loopos.v1.User.IngestTextMessage') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'loopos.v1.User.IngestTextMessage') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'loopos.v1.User.IngestTextMessage') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'loopos.v1.User.IngestTextMessage') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.User.IngestTextMessage') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'loopos.v1.User.IngestTextMessage') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.User.IngestTextMessage') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'loopos.v1.User.IngestTextMessage') {type(exception).__name__}: {exception}"
            )
        finally:
            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                del ongoing_transaction_states[ongoing_transaction_state_key(context)]

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _IngestTextMessage(
        self,
        request: loopos.v1.loopos_pb2.UserIngestTextMessageRequest,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> loopos.v1.loopos_pb2.UserIngestTextMessageResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='IngestTextMessage'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = loopos.v1.loopos_pb2.UserIngestTextMessageResponse()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=User.IngestTextMessageAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            assert transaction is not None
            async with self._state_manager.transaction(
                context,
                self._servicer.__state_type__,
                transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.UserMethods.IngestTextMessage',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, complete):

                response = await self.__IngestTextMessage(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await complete(
                    IMPORT_reboot_aio_state_managers.Effects(
                        state=state,
                        response=response,
                    )
                )
                return response

    async def _schedule_IngestTextMessage(
        self,
        *,
        request: loopos.v1.loopos_pb2.UserIngestTextMessageRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, loopos.v1.loopos_pb2.UserIngestTextMessageResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            method='IngestTextMessage',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = loopos.v1.loopos_pb2.UserIngestTextMessageResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=User.IngestTextMessageAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='IngestTextMessage'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.UserMethods.IngestTextMessage',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await UserServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).IngestTextMessage(
                    UserIngestTextMessageRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def IngestTextMessage(
        self,
        request: loopos.v1.loopos_pb2.UserIngestTextMessageRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> loopos.v1.loopos_pb2.UserIngestTextMessageResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> loopos.v1.loopos_pb2.UserIngestTextMessageResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_IngestTextMessage(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    method='IngestTextMessage',
                    context_type=IMPORT_reboot_aio_contexts.TransactionContext,
                )
                assert context is not None

                return await self._IngestTextMessage(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __IngestVoiceNote(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: loopos.v1.loopos_pb2.User,
        request: loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest,
        *,
        validating_effects: bool,
    ) -> loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse:
        try:
            typed_state: User.State = UserFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            # TODO: assert that there are no ongoing transactions for this state.
            #
            # The `typed_state` should be already validated above, so we can
            # just store it here.
            ongoing_transaction_states[ongoing_transaction_state_key(context)] = typed_state
            response = (
                await self._servicer._IngestVoiceNote(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            UserToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='User.IngestVoiceNote',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = User.IngestVoiceNoteAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'loopos.v1.User.IngestVoiceNote') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'loopos.v1.User.IngestVoiceNote') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'loopos.v1.User.IngestVoiceNote') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'loopos.v1.User.IngestVoiceNote') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'loopos.v1.User.IngestVoiceNote') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.User.IngestVoiceNote') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'loopos.v1.User.IngestVoiceNote') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.User.IngestVoiceNote') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'loopos.v1.User.IngestVoiceNote') {type(exception).__name__}: {exception}"
            )
        finally:
            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                del ongoing_transaction_states[ongoing_transaction_state_key(context)]

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _IngestVoiceNote(
        self,
        request: loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='IngestVoiceNote'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=User.IngestVoiceNoteAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            assert transaction is not None
            async with self._state_manager.transaction(
                context,
                self._servicer.__state_type__,
                transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.UserMethods.IngestVoiceNote',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, complete):

                response = await self.__IngestVoiceNote(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await complete(
                    IMPORT_reboot_aio_state_managers.Effects(
                        state=state,
                        response=response,
                    )
                )
                return response

    async def _schedule_IngestVoiceNote(
        self,
        *,
        request: loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            method='IngestVoiceNote',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=User.IngestVoiceNoteAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='IngestVoiceNote'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.UserMethods.IngestVoiceNote',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await UserServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).IngestVoiceNote(
                    UserIngestVoiceNoteRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def IngestVoiceNote(
        self,
        request: loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_IngestVoiceNote(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    method='IngestVoiceNote',
                    context_type=IMPORT_reboot_aio_contexts.TransactionContext,
                )
                assert context is not None

                return await self._IngestVoiceNote(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __ListTickets(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: loopos.v1.loopos_pb2.User,
        request: google.protobuf.empty_pb2.Empty,
        *,
        validating_effects: bool,
    ) -> loopos.v1.loopos_pb2.UserListTicketsResponse:
        try:
            typed_state: User.State = UserFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                typed_state = ongoing_transaction_states[ongoing_transaction_state_key(context)].model_copy(deep=True)
            response = (
                await self._servicer._ListTickets(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            UserToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [loopos.v1.loopos_pb2.UserListTicketsResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='User.ListTickets',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = User.ListTicketsAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'loopos.v1.User.ListTickets') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'loopos.v1.User.ListTickets') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'loopos.v1.User.ListTickets') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'loopos.v1.User.ListTickets') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'loopos.v1.User.ListTickets') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.User.ListTickets') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'loopos.v1.User.ListTickets') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.User.ListTickets') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'loopos.v1.User.ListTickets') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _ListTickets(
        self,
        request: google.protobuf.empty_pb2.Empty,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> loopos.v1.loopos_pb2.UserListTicketsResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='ListTickets'
        )


        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=User.ListTicketsAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            authorizer = self._maybe_authorize(
                method_name='loopos.v1.UserMethods.ListTickets',
                headers=context._headers,
                auth=context.auth,
                request=request,
            )
            async with self._state_manager.reader(
                context,
                self._servicer.__state_type__,
                authorize=authorizer,
            ) as state:
                response = await self.__ListTickets(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )
                return response

    async def _schedule_ListTickets(
        self,
        *,
        request: google.protobuf.empty_pb2.Empty,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, loopos.v1.loopos_pb2.UserListTicketsResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            method='ListTickets',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = loopos.v1.loopos_pb2.UserListTicketsResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=User.ListTicketsAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='ListTickets'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.UserMethods.ListTickets',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await UserServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).ListTickets(
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def ListTickets(
        self,
        request: google.protobuf.empty_pb2.Empty,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> loopos.v1.loopos_pb2.UserListTicketsResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> loopos.v1.loopos_pb2.UserListTicketsResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_ListTickets(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    method='ListTickets',
                    context_type=IMPORT_reboot_aio_contexts.ReaderContext,
                )
                assert context is not None

                return await self._ListTickets(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __QueryBrain(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: loopos.v1.loopos_pb2.User,
        request: loopos.v1.loopos_pb2.UserQueryBrainRequest,
        *,
        validating_effects: bool,
    ) -> loopos.v1.loopos_pb2.UserQueryBrainResponse:
        try:
            typed_state: User.State = UserFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                typed_state = ongoing_transaction_states[ongoing_transaction_state_key(context)].model_copy(deep=True)
            response = (
                await self._servicer._QueryBrain(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            UserToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [loopos.v1.loopos_pb2.UserQueryBrainResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='User.QueryBrain',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = User.QueryBrainAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'loopos.v1.User.QueryBrain') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'loopos.v1.User.QueryBrain') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'loopos.v1.User.QueryBrain') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'loopos.v1.User.QueryBrain') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'loopos.v1.User.QueryBrain') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.User.QueryBrain') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'loopos.v1.User.QueryBrain') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.User.QueryBrain') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'loopos.v1.User.QueryBrain') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _QueryBrain(
        self,
        request: loopos.v1.loopos_pb2.UserQueryBrainRequest,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> loopos.v1.loopos_pb2.UserQueryBrainResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='QueryBrain'
        )


        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=User.QueryBrainAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            authorizer = self._maybe_authorize(
                method_name='loopos.v1.UserMethods.QueryBrain',
                headers=context._headers,
                auth=context.auth,
                request=request,
            )
            async with self._state_manager.reader(
                context,
                self._servicer.__state_type__,
                authorize=authorizer,
            ) as state:
                response = await self.__QueryBrain(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )
                return response

    async def _schedule_QueryBrain(
        self,
        *,
        request: loopos.v1.loopos_pb2.UserQueryBrainRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, loopos.v1.loopos_pb2.UserQueryBrainResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            method='QueryBrain',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = loopos.v1.loopos_pb2.UserQueryBrainResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=User.QueryBrainAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='QueryBrain'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.UserMethods.QueryBrain',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await UserServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).QueryBrain(
                    UserQueryBrainRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def QueryBrain(
        self,
        request: loopos.v1.loopos_pb2.UserQueryBrainRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> loopos.v1.loopos_pb2.UserQueryBrainResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> loopos.v1.loopos_pb2.UserQueryBrainResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_QueryBrain(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    method='QueryBrain',
                    context_type=IMPORT_reboot_aio_contexts.ReaderContext,
                )
                assert context is not None

                return await self._QueryBrain(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __LiveState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: loopos.v1.loopos_pb2.User,
        request: google.protobuf.empty_pb2.Empty,
        *,
        validating_effects: bool,
    ) -> loopos.v1.loopos_pb2.UserLiveStateResponse:
        try:
            typed_state: User.State = UserFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                typed_state = ongoing_transaction_states[ongoing_transaction_state_key(context)].model_copy(deep=True)
            response = (
                await self._servicer._LiveState(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            UserToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [loopos.v1.loopos_pb2.UserLiveStateResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='User.LiveState',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = User.LiveStateAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'loopos.v1.User.LiveState') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'loopos.v1.User.LiveState') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'loopos.v1.User.LiveState') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'loopos.v1.User.LiveState') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'loopos.v1.User.LiveState') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.User.LiveState') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'loopos.v1.User.LiveState') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.User.LiveState') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'loopos.v1.User.LiveState') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _LiveState(
        self,
        request: google.protobuf.empty_pb2.Empty,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> loopos.v1.loopos_pb2.UserLiveStateResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='LiveState'
        )


        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=User.LiveStateAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            authorizer = self._maybe_authorize(
                method_name='loopos.v1.UserMethods.LiveState',
                headers=context._headers,
                auth=context.auth,
                request=request,
            )
            async with self._state_manager.reader(
                context,
                self._servicer.__state_type__,
                authorize=authorizer,
            ) as state:
                response = await self.__LiveState(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )
                return response

    async def _schedule_LiveState(
        self,
        *,
        request: google.protobuf.empty_pb2.Empty,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, loopos.v1.loopos_pb2.UserLiveStateResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            method='LiveState',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = loopos.v1.loopos_pb2.UserLiveStateResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=User.LiveStateAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='LiveState'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.UserMethods.LiveState',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await UserServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).LiveState(
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def LiveState(
        self,
        request: google.protobuf.empty_pb2.Empty,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> loopos.v1.loopos_pb2.UserLiveStateResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> loopos.v1.loopos_pb2.UserLiveStateResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_LiveState(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    method='LiveState',
                    context_type=IMPORT_reboot_aio_contexts.ReaderContext,
                )
                assert context is not None

                return await self._LiveState(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: loopos.v1.loopos_pb2.User,
        request: google.protobuf.empty_pb2.Empty,
        *,
        validating_effects: bool,
    ) -> User.CreateEffects:
        try:
            states_being_constructed.add(context.state_id)
            typed_state: User.State = UserFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                typed_state = ongoing_transaction_states[ongoing_transaction_state_key(context)].model_copy(deep=True)
            response = (
                await self._servicer._Create(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )

            # TODO: it's premature to overwrite the state now given that the
            # writer might still "fail" and an error will get propagated back
            # to the ongoing transaction which will still see the effects of
            # this writer. What we should be doing instead is creating a
            # callback API that we invoke only after a writer completes
            # that lets us update the state _reference_ then.
            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                for field, value in typed_state.model_dump().items():
                    setattr(ongoing_transaction_states[ongoing_transaction_state_key(context)], field, value)

            UserToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [google.protobuf.empty_pb2.Empty],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='User.Create',
                validating_effects=validating_effects,
                context=context,
            )
            return User.CreateEffects(
                state=state,
                response=response,
                tasks=context._tasks,
                _colocated_upserts=context._colocated_upserts,
            )
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = User.CreateAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'loopos.v1.User.Create') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'loopos.v1.User.Create') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'loopos.v1.User.Create') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'loopos.v1.User.Create') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'loopos.v1.User.Create') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.User.Create') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'loopos.v1.User.Create') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.User.Create') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'loopos.v1.User.Create') {type(exception).__name__}: {exception}"
            )
        finally:
            states_being_constructed.remove(context.state_id)

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _Create(
        self,
        request: google.protobuf.empty_pb2.Empty,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='Create'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = google.protobuf.empty_pb2.Empty()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=User.CreateAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.UserMethods.Create',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                transaction=transaction,
                from_constructor=True,
                requires_constructor=True,
            ) as (state, writer):

                effects = await self.__Create(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await writer.complete(effects)

                # TODO: We need a single `Effects` superclass for all methods, so we
                # would need to make it "partially" generic (with per-method subclasses
                # filling out the rest of the generic parameters) in order to fix this.
                return effects.response  # type: ignore[return-value]

    async def _schedule_Create(
        self,
        *,
        request: google.protobuf.empty_pb2.Empty,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, google.protobuf.empty_pb2.Empty]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            method='Create',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = google.protobuf.empty_pb2.Empty()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=User.CreateAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='Create'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.UserMethods.Create',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=True,
                requires_constructor=True
            ) as (state, writer):

                task = await UserServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).Create(
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def Create(
        self,
        request: google.protobuf.empty_pb2.Empty,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> google.protobuf.empty_pb2.Empty:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> google.protobuf.empty_pb2.Empty:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_Create(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    method='Create',
                    context_type=IMPORT_reboot_aio_contexts.WriterContext,
                )
                assert context is not None

                return await self._Create(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    def _maybe_authorize(
        self,
        *,
        method_name: str,
        headers: IMPORT_reboot_aio_headers.Headers,
        auth: IMPORT_typing.Optional[IMPORT_reboot_aio_auth.Auth],
        request: IMPORT_typing.Optional[UserRequestTypes] = None,
    ) -> IMPORT_typing.Optional[IMPORT_typing.Callable[[IMPORT_typing.Optional[UserStateType]], IMPORT_typing.Awaitable[None]]]:
        """Returns a function to check authorization for the given method.

        Raises `PermissionDenied` in case Authorizer is present but the request
        is not authorized.
        """
        # To authorize internal calls, we use an internal magic token.
        if headers.bearer_token == __internal_magic_token__:
            return None

        assert self._authorizer is not None

        async def authorize(state: IMPORT_typing.Optional[UserStateType]) -> None:
            # Create context for the authorizer. This is a `ReaderContext`
            # independently of the calling context.
            with self.use_context(
                headers=(
                    # Get headers suitable for doing authorization.
                    headers.copy_for_token_verification_and_authorization()
                ),
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                method=method_name,
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            ) as context:
                context.auth = auth

                # Get the authorizer decision.
                authorization_decision = await self._authorizer.authorize(
                    method_name=method_name,
                    context=context,
                    state=state,
                    request=request,
                )

            # Enforce correct authorizer decision type.
            try:
                IMPORT_reboot_aio_types.assert_type(
                    authorization_decision,
                    [
                        IMPORT_rbt_v1alpha1.errors_pb2.Ok,
                        IMPORT_rbt_v1alpha1.errors_pb2.Unauthenticated,
                        IMPORT_rbt_v1alpha1.errors_pb2.PermissionDenied,
                    ]
                )
            except TypeError as e:
                # Retyping.cast the exception to provide more context.
                authorizer_type = f"{type(self._authorizer).__module__}.{type(self._authorizer).__name__}"
                raise TypeError(
                    f"Authorizer '{authorizer_type}' "
                    f"returned unexpected type '{type(authorization_decision).__name__}' "
                    f"for method '{method_name}' on "
                    f"`loopos.v1.User('{headers.state_ref.id}')`"
                ) from e

            # If the decision is not `True`, raise a `SystemAborted` with either a
            # `PermissionDenied` error (in case of `False`) or an `Unauthenticated`
            # error.
            if not isinstance(authorization_decision, IMPORT_rbt_v1alpha1.errors_pb2.Ok):
                if isinstance(authorization_decision, IMPORT_rbt_v1alpha1.errors_pb2.Unauthenticated):
                    logger.warning(
                        f"Unauthenticated call to '{method_name}' on "
                        f"`loopos.v1.User('{headers.state_ref.id}')`"
                    )

                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    authorization_decision,
                    message=
                    f"You are not authorized to call '{method_name}' on "
                    f"`loopos.v1.User('{headers.state_ref.id}')`"
                )

        return authorize

    async def _maybe_verify_token(
        self,
        *,
        headers: IMPORT_reboot_aio_headers.Headers,
        method: str,
    ) -> IMPORT_typing.Optional[IMPORT_reboot_aio_auth.Auth]:
        """Verify the bearer token and if a token verifier is present.

        Returns the (optional) `reboot.aio.auth.Auth` object
        produced by the token verifier if the token can be verified.
        """
        if self._token_verifier is not None:
            if headers.bearer_token == __internal_magic_token__:
                return None

            with self.use_context(
                headers=(
                    # Get headers suitable for doing token verification.
                    headers.copy_for_token_verification_and_authorization()
                ),
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                method=method,
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            ) as context:
                result = await self._token_verifier.verify_token(
                    context=context,
                    token=headers.bearer_token,
                )
                if isinstance(result, IMPORT_rbt_v1alpha1.errors_pb2.Unauthenticated):
                    raise IMPORT_reboot_aio_aborted.SystemAborted(
                        result,
                        message=result.message or None,
                    )
                return result

        return None

class OpsTicketServicerMiddleware(IMPORT_reboot_aio_internals_middleware.Middleware):

    def __init__(
        self,
        *,
        servicer: OpsTicketBaseServicer,
        application_id: IMPORT_reboot_aio_types.ApplicationId,
        server_id: IMPORT_reboot_aio_types.ServerId,
        state_manager: IMPORT_reboot_aio_state_managers.StateManager,
        placement_client: IMPORT_reboot_aio_placement.PlacementClient,
        channel_manager: IMPORT_reboot_aio_internals_channel_manager._ChannelManager,
        tasks_cache: IMPORT_reboot_aio_internals_tasks_cache.TasksCache,
        token_verifier: IMPORT_typing.Optional[IMPORT_reboot_aio_auth_token_verifiers.TokenVerifier],
        effect_validation: IMPORT_reboot_aio_contexts.EffectValidation,
        ready: IMPORT_asyncio.Event,
    ):
        super().__init__(
            application_id=application_id,
            server_id=server_id,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            service_names = [
                IMPORT_reboot_aio_types.ServiceName("loopos.v1.OpsTicketMethods"),
            ],
            placement_client=placement_client,
            channel_manager=channel_manager,
            effect_validation=effect_validation,
        )

        self._servicer = servicer
        self._state_manager = state_manager
        self.tasks_dispatcher = IMPORT_reboot_aio_internals_tasks_dispatcher.TasksDispatcher(
            application_id=application_id,
            dispatch=self.dispatch,
            tasks_cache=tasks_cache,
            ready=ready,
            complete_task=self._state_manager.complete_task,
        )

        # Store the type of each method's request so that stored requests can be
        # deserialized into the correct type.
        self.request_type_by_method_name: dict[str, type[IMPORT_google_protobuf_message.Message]] = {
            'Create': loopos.v1.loopos_pb2.OpsTicketCreateRequest,
            'Triage': google.protobuf.empty_pb2.Empty,
            'AcknowledgeDispatch': loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest,
            'DispatchWithEscalation': loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationRequest,
            'ProposeNewRule': google.protobuf.empty_pb2.Empty,
            'ShowBrainSources': google.protobuf.empty_pb2.Empty,
        }

        # Get authorizer, if any, converting from a rule if necessary.
        def convert_authorizer_rule_if_necessary(
            authorizer_or_rule: IMPORT_typing.Optional[
                IMPORT_reboot_aio_auth_authorizers.Authorizer | IMPORT_reboot_aio_auth_authorizers.AuthorizerRule
            ]
        ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer:

            # If no authorizer or rule is provided, return the default
            # authorizer which allows if app internal or allows if in
            # dev mode (and logs some warnings to help the user
            # realize where they are missing authorization).
            if authorizer_or_rule is None:
                return IMPORT_reboot_aio_auth_authorizers.DefaultAuthorizer(
                    'OpsTicket',
                    is_user_type=False,
                )

            if isinstance(authorizer_or_rule, IMPORT_reboot_aio_auth_authorizers.AuthorizerRule):
                return OpsTicketAuthorizer(
                    _default=authorizer_or_rule
                )

            return authorizer_or_rule

        self._authorizer = convert_authorizer_rule_if_necessary(
            servicer.authorizer()
        )

        # Create token verifier.
        self._token_verifier: IMPORT_typing.Optional[IMPORT_reboot_aio_auth_token_verifiers.TokenVerifier] = (
            servicer.token_verifier() or token_verifier
        )

        # Since users specify errors as proto messages they can't raise them
        # directly - to do so they have to use the `Aborted` wrapper, which will
        # hold the original proto message. On errors we'll need to check whether
        # such wrappers hold a proto message for a specified error, so we can
        # avoid retrying tasks that complete with a specified error.
        self._specified_errors_by_service_method_name: dict[str, list[str]] = {
        }


    def add_to_server(self, server: IMPORT_grpc.aio.Server) -> None:
        loopos.v1.loopos_pb2_grpc.add_OpsTicketMethodsServicer_to_server(
            self, server
        )

    async def inspect(self, state_ref: IMPORT_reboot_aio_types.StateRef) -> IMPORT_typing.AsyncIterator[IMPORT_google_protobuf_message.Message]:
        """Implementation of `Middleware.inspect()`."""
        context = self.create_context(
            headers=IMPORT_reboot_aio_headers.Headers(
                application_id=self.application_id,
                state_ref=state_ref,
            ),
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            method="inspect",
            context_type=IMPORT_reboot_aio_contexts.ReaderContext,
        )

        async with self._state_manager.streaming_reader_idempotency_key(
            context,
            self._servicer.__state_type__,
            authorize=None,
        ) as states:
            async for (state, idempotency_key) in states:
                yield state

    async def react_query(
        self,
        headers: IMPORT_reboot_aio_headers.Headers,
        method: str,
        request_bytes: bytes,
    ) -> IMPORT_typing.AsyncIterator[tuple[IMPORT_typing.Optional[IMPORT_google_protobuf_message.Message], list[IMPORT_uuid.UUID]]]:
        """Returns the response of calling 'method' given a message
        deserialized from the provided 'request_bytes' for each state
        update that creates a different response.

        # The caller (react.py) should have already ensured that this server
        # is authoritative for this traffic.
        assert self.placement_client.server_for_actor(
            headers.application_id,
            headers.state_ref,
        ) == self._server_id

        NOTE: only unary reader methods are supported."""
        # Need to define these up here since we can only do that once.
        last_response: IMPORT_typing.Optional[IMPORT_google_protobuf_message.Message] = None
        aggregated_idempotency_keys: list[IMPORT_uuid.UUID] = []
        if method == 'Create':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer OpsTicket."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'Triage':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer OpsTicket."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'AcknowledgeDispatch':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer OpsTicket."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'DispatchWithEscalation':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer OpsTicket."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'ProposeNewRule':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer OpsTicket."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' is invalid"
            )
            yield  # Necessary for type checking.
        elif method == 'ShowBrainSources':

            context = self.create_context(
                headers=headers,
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                method='ShowBrainSources',
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            )

            with IMPORT_reboot_aio_tracing.span(
                state_name=f"{context.state_type_name}('{context.state_id}')",
                span_name="ShowBrainSources() (reactively)",
                # The naming above matches Python, but not TypeScript.
                python_specific=True,
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
            ):
                context.auth = await self._maybe_verify_token(
                    headers=headers, method='ShowBrainSources'
                )

                request = google.protobuf.empty_pb2.Empty()
                request.ParseFromString(request_bytes)

                async with self._state_manager.reactively(
                    context,
                    self._servicer.__state_type__,
                    authorize=self._maybe_authorize(
                        method_name='loopos.v1.OpsTicketMethods.ShowBrainSources',
                        headers=headers,
                        auth=context.auth,
                        request=request,
                    ),
                ) as states:
                    async for (state, idempotency_keys) in states:

                        aggregated_idempotency_keys.extend(idempotency_keys)

                        # Note: This does not do any defensive copying currently:
                        # see https://github.com/reboot-dev/respect/issues/2636.
                        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
                        async def run__ShowBrainSources(validating_effects: bool) -> IMPORT_google_protobuf_message.Message:
                            return await self.__ShowBrainSources(
                                context,
                                state,
                                request,
                                validating_effects=validating_effects,
                            )

                        response = await run__ShowBrainSources()

                        if last_response != response:
                            yield (response, aggregated_idempotency_keys)
                            last_response = response
                        else:
                            yield (None, aggregated_idempotency_keys)

                        aggregated_idempotency_keys.clear()
        else:
            logger.warning(
                "Got a React query request with an invalid method name: "
                f"Method '{method}' is invalid for servicer OpsTicket."
                "\n"
                "Do you have a browser tab open for an older version "
                "of this application, or for a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' not found"
            )
            yield  # Unreachable but necessary for mypy.

    async def react_mutate(
        self,
        headers: IMPORT_reboot_aio_headers.Headers,
        method: str,
        request_bytes: bytes,
    ) -> IMPORT_google_protobuf_message.Message:
        """Returns the response of calling 'method' given a message
        deserialized from the provided 'request_bytes'."""
        if method == 'Create':
            request = loopos.v1.loopos_pb2.OpsTicketCreateRequest()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = loopos.v1.loopos_pb2_grpc.OpsTicketMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.Create(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise OpsTicket.CreateAborted.from_status(
                            status
                        ) from None
                    raise OpsTicket.CreateAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        elif method == 'Triage':
            request = google.protobuf.empty_pb2.Empty()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = loopos.v1.loopos_pb2_grpc.OpsTicketMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.Triage(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise OpsTicket.TriageAborted.from_status(
                            status
                        ) from None
                    raise OpsTicket.TriageAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        elif method == 'AcknowledgeDispatch':
            request = loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = loopos.v1.loopos_pb2_grpc.OpsTicketMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.AcknowledgeDispatch(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise OpsTicket.AcknowledgeDispatchAborted.from_status(
                            status
                        ) from None
                    raise OpsTicket.AcknowledgeDispatchAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        elif method == 'DispatchWithEscalation':
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                f"Method 'DispatchWithEscalation' can not be called via React (for now)"
            )
        elif method == 'ProposeNewRule':
            request = google.protobuf.empty_pb2.Empty()
            request.ParseFromString(request_bytes)

            # NOTE: we automatically retry mutations that come through
            # React when we get a `IMPORT_grpc.StatusCode.UNAVAILABLE` to
            # match the retry logic we do in the React code generated
            # to handle lack/loss of connectivity.
            #
            # TODO(benh): revisit this decision if we ever see reason
            # to call `react_mutate()` from any place other than where
            # we're executing React (e.g., browser, next.js server
            # component, etc).
            call_backoff = IMPORT_reboot_aio_backoff.Backoff()
            while True:
                # We make a full-fledged gRPC call, so that if this traffic
                # was misrouted (i.e. this server is not authoritative
                # for the state), it will now go to the right place. The
                # receiving middleware will handle things like effect
                # validation and so forth.
                assert headers.application_id is not None  # Guaranteed by `Headers`.
                stub = loopos.v1.loopos_pb2_grpc.OpsTicketMethodsStub(
                    self.channel_manager.get_channel_to(
                        self.placement_client.address_for_actor(
                            headers.application_id,
                            headers.state_ref,
                        )
                    )
                )
                call = stub.ProposeNewRule(
                    request=request,
                    metadata=headers.to_grpc_metadata(),
                )
                try:
                    return await call
                except IMPORT_grpc.aio.AioRpcError as error:
                    if error.code() == IMPORT_grpc.StatusCode.UNAVAILABLE:
                        await call_backoff()
                        continue

                    # Reconstitute the error that the server threw, if it was a declared error.
                    status = await IMPORT_rpc_status_async.from_call(call)
                    if status is not None:
                        raise OpsTicket.ProposeNewRuleAborted.from_status(
                            status
                        ) from None
                    raise OpsTicket.ProposeNewRuleAborted.from_grpc_aio_rpc_error(
                        error
                     ) from None

        elif method == 'ShowBrainSources':
            # Invariant here is that users should not have called this
            # directly but only through code generated React
            # components which should not have been generated except
            # for valid method candidates.
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                "Method 'ShowBrainSources' is invalid for servicer OpsTicket."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=f"Method '{method}' is invalid"
            )
        else:
            logger.warning(
                "Got a react mutate request with an invalid method name: "
                f"Method '{method}' is invalid for servicer OpsTicket."
                "\n"
                "Do you have an old browser tab still open for an older version "
                "of this application, or a different application all together?"
            )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.InvalidMethod(),
                message=
                    f"Method '{method}' not found"
            )

    async def dispatch(
        self,
        task: IMPORT_reboot_aio_tasks.TaskEffect,
        *,
        only_validate: bool = False,
        on_loop_iteration: IMPORT_reboot_aio_internals_tasks_dispatcher.OnLoopIterationCallable = (lambda iteration, next_iteration_schedule: None),
    ) -> IMPORT_reboot_aio_internals_tasks_dispatcher.TaskResponseOrStatus:
        """Dispatches the tasks to execute unless 'only_validate' is set to
        true, in which case just ensures that the task actually exists.
        Note that this function will be called *by* tasks_dispatcher; it will
        not itself call into tasks_dispatcher."""

        if 'Create' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (google.protobuf.empty_pb2.Empty(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_Create(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (OpsTicketWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).Create(
                            OpsTicketCreateRequestFromProto(IMPORT_typing.cast(loopos.v1.loopos_pb2.OpsTicketCreateRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('loopos.v1.OpsTicketMethods.Create', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_Create(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                    method='Create',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'Triage' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (google.protobuf.empty_pb2.Empty(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_Triage(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (OpsTicketWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).Triage(
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('loopos.v1.OpsTicketMethods.Triage', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_Triage(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                    method='Triage',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'AcknowledgeDispatch' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (google.protobuf.empty_pb2.Empty(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_AcknowledgeDispatch(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (OpsTicketWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).AcknowledgeDispatch(
                            OpsTicketAcknowledgeDispatchRequestFromProto(IMPORT_typing.cast(loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest, task.request)),
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('loopos.v1.OpsTicketMethods.AcknowledgeDispatch', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_AcknowledgeDispatch(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                    method='AcknowledgeDispatch',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'DispatchWithEscalation' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_DispatchWithEscalation(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await self.__DispatchWithEscalation(
                            context,
                            IMPORT_typing.cast(loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationRequest, task.request),
                            validating_effects=validating_effects,
                        )
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('loopos.v1.OpsTicketMethods.DispatchWithEscalation', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise

            @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
            async def run_DispatchWithEscalation_workflow(
                validating_effects: bool,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
            ):
                try:
                    # When we're validating effects we
                    # periodically timeout so that we can log
                    # that a workflow might be hung, i.e., the
                    # user has a bug.
                    task = IMPORT_asyncio.create_task(
                        run_DispatchWithEscalation(
                            context,
                            validating_effects=validating_effects,
                        )
                    )
                    timeout = None if not validating_effects else 5  # seconds
                    while True:
                        done, pending = await IMPORT_asyncio.wait(
                            [task],
                            timeout=timeout,
                        )
                        # Check if we've timed out, which
                        # should only occur if we're
                        # validating effects.
                        if len(done) == 0:
                            assert validating_effects and timeout is not None
                            logger.warning(
                                f'Still waiting for method OpsTicket.DispatchWithEscalation '
                                'to complete after re-running to validate effects.'
                            )
                            timeout += 5  # seconds
                            continue
                        return task.result()
                finally:
                    if not task.done():
                        task.cancel()
                        # Need to actually await the task so if
                        # there is an exception we don't get a
                        # warning logged that the exception was
                        # never retrieved, but we don't care about
                        # the exception because we're done with
                        # the task.
                        try:
                            await task
                        except:
                            pass

            return await run_DispatchWithEscalation_workflow(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                        workflow_id=IMPORT_uuid.UUID(bytes=task.task_id.task_uuid),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                    method='DispatchWithEscalation',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'ProposeNewRule' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_ProposeNewRule(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (OpsTicketWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).ProposeNewRule(
                            bearer_token=__internal_magic_token__,
                            idempotency=IMPORT_reboot_aio_idempotency.Idempotency(
                                alias=f'Task {IMPORT_uuid.UUID(bytes=task.task_id.task_uuid)}',
                            ),
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('loopos.v1.OpsTicketMethods.ProposeNewRule', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_ProposeNewRule(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                    method='ProposeNewRule',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )
        elif 'ShowBrainSources' == task.method_name:
            if only_validate:
                # TODO(benh): validate 'task.request' is correct type.
                return (loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse(), None)

            # Use an inline method to create a new scope, so that we can use
            # variable names like `context` and `effects` in multiple branches
            # in this code (notably when there are multiple task types) without
            # hitting a mypy error that the variable's type is not consistent.
            async def run_ShowBrainSources(
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                *,
                validating_effects: bool = False,
            ):
                async with self._state_manager.task_workflow(
                    context,
                    task,
                    on_loop_iteration=on_loop_iteration,
                    validating_effects=validating_effects,
                ) as complete:
                    try:
                        response = await (OpsTicketWorkflowStub(
                            context=context,
                            state_ref=context._state_ref,
                        ).ShowBrainSources(
                            bearer_token=__internal_magic_token__,
                        ))
                        await complete(task, (response, None))
                        return (response, None)
                    except IMPORT_asyncio.CancelledError:
                        # Do not retry a task if it was cancelled by a caller.
                        if self.tasks_dispatcher.is_task_cancelled(task.task_id.task_uuid):
                            result = (
                                None,
                                IMPORT_reboot.aio.aborted.SystemAborted(
                                    IMPORT_rbt_v1alpha1.errors_pb2.Cancelled(),
                                ).to_status(),
                            )
                            await complete(task, result)
                            return result
                        else:
                            raise
                    except IMPORT_reboot.aio.aborted.Aborted as aborted:
                        error_type = f'{aborted._protobuf_error.__class__.__module__}.{aborted._protobuf_error.__class__.__qualname__}'
                        # Do not retry a task if the error was specified in the
                        # proto file.
                        if error_type in self._specified_errors_by_service_method_name.get('loopos.v1.OpsTicketMethods.ShowBrainSources', []):
                            result = (None, aborted.to_status())
                            await complete(task, result)
                            return result
                        raise


            return await run_ShowBrainSources(
                self.create_context(
                    headers=IMPORT_reboot_aio_headers.Headers(
                        application_id=self.application_id,
                        state_ref=IMPORT_reboot_aio_types.StateRef(task.task_id.state_ref),
                    ),
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                    method='ShowBrainSources',
                    context_type=IMPORT_reboot_aio_contexts.WorkflowContext,
                    task=task,
                    # Propagate state manager and state type so that
                    # `until()` calls (via `WorkflowContext.retry_reactively_until()`)
                    # can enter `reactively()` scoped to each `until()`
                    # invocation.
                    reactively_state_manager=self._state_manager,
                    reactively_state_type=(
                        self._servicer.__state_type__
                    ),
                )
            )

        # There are no tasks for this service.
        start_or_validate = "start" if not only_validate else "validate"
        raise RuntimeError(
            f"Attempted to {start_or_validate} task '{task.method_name}' "
            f"on 'OpsTicket' which does not exist"
        )

    # OpsTicket specific methods:
    async def __Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: loopos.v1.loopos_pb2.OpsTicket,
        request: loopos.v1.loopos_pb2.OpsTicketCreateRequest,
        *,
        validating_effects: bool,
    ) -> OpsTicket.CreateEffects:
        try:
            states_being_constructed.add(context.state_id)
            typed_state: OpsTicket.State = OpsTicketFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                typed_state = ongoing_transaction_states[ongoing_transaction_state_key(context)].model_copy(deep=True)
            response = (
                await self._servicer._Create(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )

            # TODO: it's premature to overwrite the state now given that the
            # writer might still "fail" and an error will get propagated back
            # to the ongoing transaction which will still see the effects of
            # this writer. What we should be doing instead is creating a
            # callback API that we invoke only after a writer completes
            # that lets us update the state _reference_ then.
            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                for field, value in typed_state.model_dump().items():
                    setattr(ongoing_transaction_states[ongoing_transaction_state_key(context)], field, value)

            OpsTicketToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [google.protobuf.empty_pb2.Empty],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='OpsTicket.Create',
                validating_effects=validating_effects,
                context=context,
            )
            return OpsTicket.CreateEffects(
                state=state,
                response=response,
                tasks=context._tasks,
                _colocated_upserts=context._colocated_upserts,
            )
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = OpsTicket.CreateAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'loopos.v1.OpsTicket.Create') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'loopos.v1.OpsTicket.Create') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'loopos.v1.OpsTicket.Create') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'loopos.v1.OpsTicket.Create') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'loopos.v1.OpsTicket.Create') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.OpsTicket.Create') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'loopos.v1.OpsTicket.Create') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.OpsTicket.Create') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'loopos.v1.OpsTicket.Create') {type(exception).__name__}: {exception}"
            )
        finally:
            states_being_constructed.remove(context.state_id)

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _Create(
        self,
        request: loopos.v1.loopos_pb2.OpsTicketCreateRequest,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='Create'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = google.protobuf.empty_pb2.Empty()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=OpsTicket.CreateAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.OpsTicketMethods.Create',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                transaction=transaction,
                from_constructor=True,
                requires_constructor=True,
            ) as (state, writer):

                effects = await self.__Create(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await writer.complete(effects)

                # TODO: We need a single `Effects` superclass for all methods, so we
                # would need to make it "partially" generic (with per-method subclasses
                # filling out the rest of the generic parameters) in order to fix this.
                return effects.response  # type: ignore[return-value]

    async def _schedule_Create(
        self,
        *,
        request: loopos.v1.loopos_pb2.OpsTicketCreateRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, google.protobuf.empty_pb2.Empty]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            method='Create',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = google.protobuf.empty_pb2.Empty()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=OpsTicket.CreateAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='Create'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.OpsTicketMethods.Create',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=True,
                requires_constructor=True
            ) as (state, writer):

                task = await OpsTicketServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).Create(
                    OpsTicketCreateRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def Create(
        self,
        request: loopos.v1.loopos_pb2.OpsTicketCreateRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> google.protobuf.empty_pb2.Empty:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> google.protobuf.empty_pb2.Empty:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_Create(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                    method='Create',
                    context_type=IMPORT_reboot_aio_contexts.WriterContext,
                )
                assert context is not None

                return await self._Create(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __Triage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: loopos.v1.loopos_pb2.OpsTicket,
        request: google.protobuf.empty_pb2.Empty,
        *,
        validating_effects: bool,
    ) -> OpsTicket.TriageEffects:
        try:
            typed_state: OpsTicket.State = OpsTicketFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                typed_state = ongoing_transaction_states[ongoing_transaction_state_key(context)].model_copy(deep=True)
            response = (
                await self._servicer._Triage(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )

            # TODO: it's premature to overwrite the state now given that the
            # writer might still "fail" and an error will get propagated back
            # to the ongoing transaction which will still see the effects of
            # this writer. What we should be doing instead is creating a
            # callback API that we invoke only after a writer completes
            # that lets us update the state _reference_ then.
            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                for field, value in typed_state.model_dump().items():
                    setattr(ongoing_transaction_states[ongoing_transaction_state_key(context)], field, value)

            OpsTicketToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [google.protobuf.empty_pb2.Empty],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='OpsTicket.Triage',
                validating_effects=validating_effects,
                context=context,
            )
            return OpsTicket.TriageEffects(
                state=state,
                response=response,
                tasks=context._tasks,
                _colocated_upserts=context._colocated_upserts,
            )
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = OpsTicket.TriageAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'loopos.v1.OpsTicket.Triage') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'loopos.v1.OpsTicket.Triage') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'loopos.v1.OpsTicket.Triage') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'loopos.v1.OpsTicket.Triage') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'loopos.v1.OpsTicket.Triage') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.OpsTicket.Triage') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'loopos.v1.OpsTicket.Triage') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.OpsTicket.Triage') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'loopos.v1.OpsTicket.Triage') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _Triage(
        self,
        request: google.protobuf.empty_pb2.Empty,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='Triage'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = google.protobuf.empty_pb2.Empty()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=OpsTicket.TriageAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.OpsTicketMethods.Triage',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                transaction=transaction,
                from_constructor=False,
                requires_constructor=True,
            ) as (state, writer):

                effects = await self.__Triage(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await writer.complete(effects)

                # TODO: We need a single `Effects` superclass for all methods, so we
                # would need to make it "partially" generic (with per-method subclasses
                # filling out the rest of the generic parameters) in order to fix this.
                return effects.response  # type: ignore[return-value]

    async def _schedule_Triage(
        self,
        *,
        request: google.protobuf.empty_pb2.Empty,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, google.protobuf.empty_pb2.Empty]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            method='Triage',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = google.protobuf.empty_pb2.Empty()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=OpsTicket.TriageAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='Triage'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.OpsTicketMethods.Triage',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await OpsTicketServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).Triage(
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def Triage(
        self,
        request: google.protobuf.empty_pb2.Empty,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> google.protobuf.empty_pb2.Empty:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> google.protobuf.empty_pb2.Empty:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_Triage(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                    method='Triage',
                    context_type=IMPORT_reboot_aio_contexts.WriterContext,
                )
                assert context is not None

                return await self._Triage(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __AcknowledgeDispatch(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: loopos.v1.loopos_pb2.OpsTicket,
        request: loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest,
        *,
        validating_effects: bool,
    ) -> OpsTicket.AcknowledgeDispatchEffects:
        try:
            typed_state: OpsTicket.State = OpsTicketFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                typed_state = ongoing_transaction_states[ongoing_transaction_state_key(context)].model_copy(deep=True)
            response = (
                await self._servicer._AcknowledgeDispatch(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )

            # TODO: it's premature to overwrite the state now given that the
            # writer might still "fail" and an error will get propagated back
            # to the ongoing transaction which will still see the effects of
            # this writer. What we should be doing instead is creating a
            # callback API that we invoke only after a writer completes
            # that lets us update the state _reference_ then.
            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                for field, value in typed_state.model_dump().items():
                    setattr(ongoing_transaction_states[ongoing_transaction_state_key(context)], field, value)

            OpsTicketToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [google.protobuf.empty_pb2.Empty],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='OpsTicket.AcknowledgeDispatch',
                validating_effects=validating_effects,
                context=context,
            )
            return OpsTicket.AcknowledgeDispatchEffects(
                state=state,
                response=response,
                tasks=context._tasks,
                _colocated_upserts=context._colocated_upserts,
            )
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = OpsTicket.AcknowledgeDispatchAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'loopos.v1.OpsTicket.AcknowledgeDispatch') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'loopos.v1.OpsTicket.AcknowledgeDispatch') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'loopos.v1.OpsTicket.AcknowledgeDispatch') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'loopos.v1.OpsTicket.AcknowledgeDispatch') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'loopos.v1.OpsTicket.AcknowledgeDispatch') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.OpsTicket.AcknowledgeDispatch') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'loopos.v1.OpsTicket.AcknowledgeDispatch') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.OpsTicket.AcknowledgeDispatch') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'loopos.v1.OpsTicket.AcknowledgeDispatch') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _AcknowledgeDispatch(
        self,
        request: loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='AcknowledgeDispatch'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = google.protobuf.empty_pb2.Empty()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=OpsTicket.AcknowledgeDispatchAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.OpsTicketMethods.AcknowledgeDispatch',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                transaction=transaction,
                from_constructor=False,
                requires_constructor=True,
            ) as (state, writer):

                effects = await self.__AcknowledgeDispatch(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await writer.complete(effects)

                # TODO: We need a single `Effects` superclass for all methods, so we
                # would need to make it "partially" generic (with per-method subclasses
                # filling out the rest of the generic parameters) in order to fix this.
                return effects.response  # type: ignore[return-value]

    async def _schedule_AcknowledgeDispatch(
        self,
        *,
        request: loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, google.protobuf.empty_pb2.Empty]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            method='AcknowledgeDispatch',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = google.protobuf.empty_pb2.Empty()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=OpsTicket.AcknowledgeDispatchAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='AcknowledgeDispatch'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.OpsTicketMethods.AcknowledgeDispatch',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await OpsTicketServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).AcknowledgeDispatch(
                    OpsTicketAcknowledgeDispatchRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def AcknowledgeDispatch(
        self,
        request: loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> google.protobuf.empty_pb2.Empty:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> google.protobuf.empty_pb2.Empty:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_AcknowledgeDispatch(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                    method='AcknowledgeDispatch',
                    context_type=IMPORT_reboot_aio_contexts.WriterContext,
                )
                assert context is not None

                return await self._AcknowledgeDispatch(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __DispatchWithEscalation(
        self,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationRequest,
        *,
        validating_effects: bool,
    ) -> loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse:
        try:
            # We should have an asyncio task and thus context per request,
            # let's confirm this assumption by making sure that
            # `__servicer__ is None`.
            assert self._servicer.__servicer__.get() is None
            self._servicer.__servicer__.set(self._servicer)
            response = (
                await self._servicer._DispatchWithEscalation(
                    context=context,
                    request=request
                )
            )



            IMPORT_reboot_aio_types.assert_type(
                response,
                [loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='OpsTicket.DispatchWithEscalation',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = OpsTicket.DispatchWithEscalationAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'loopos.v1.OpsTicket.DispatchWithEscalation') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'loopos.v1.OpsTicket.DispatchWithEscalation') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'loopos.v1.OpsTicket.DispatchWithEscalation') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'loopos.v1.OpsTicket.DispatchWithEscalation') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'loopos.v1.OpsTicket.DispatchWithEscalation') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.OpsTicket.DispatchWithEscalation') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'loopos.v1.OpsTicket.DispatchWithEscalation') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.OpsTicket.DispatchWithEscalation') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'loopos.v1.OpsTicket.DispatchWithEscalation') {type(exception).__name__}: {exception}"
            )
        finally:
            self._servicer.__servicer__.set(None)


    async def _schedule_DispatchWithEscalation(
        self,
        *,
        request: loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationRequest,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            method='DispatchWithEscalation',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=OpsTicket.DispatchWithEscalationAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='DispatchWithEscalation'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.OpsTicketMethods.DispatchWithEscalation',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await OpsTicketServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).DispatchWithEscalation(
                    OpsTicketDispatchWithEscalationRequestFromProto(request),
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def DispatchWithEscalation(
        self,
        request: loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationRequest,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                # reboot.aio.contexts.WorkflowContext must be scheduled!
                assert headers.task_schedule is not None
                context, response = await self._schedule_DispatchWithEscalation(
                    headers=headers,
                    request=request,
                    grpc_context=grpc_context,
                )
                return response

            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __ProposeNewRule(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: loopos.v1.loopos_pb2.OpsTicket,
        request: google.protobuf.empty_pb2.Empty,
        *,
        validating_effects: bool,
    ) -> OpsTicket.ProposeNewRuleEffects:
        try:
            typed_state: OpsTicket.State = OpsTicketFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                typed_state = ongoing_transaction_states[ongoing_transaction_state_key(context)].model_copy(deep=True)
            response = (
                await self._servicer._ProposeNewRule(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )

            # TODO: it's premature to overwrite the state now given that the
            # writer might still "fail" and an error will get propagated back
            # to the ongoing transaction which will still see the effects of
            # this writer. What we should be doing instead is creating a
            # callback API that we invoke only after a writer completes
            # that lets us update the state _reference_ then.
            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                for field, value in typed_state.model_dump().items():
                    setattr(ongoing_transaction_states[ongoing_transaction_state_key(context)], field, value)

            OpsTicketToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='OpsTicket.ProposeNewRule',
                validating_effects=validating_effects,
                context=context,
            )
            return OpsTicket.ProposeNewRuleEffects(
                state=state,
                response=response,
                tasks=context._tasks,
                _colocated_upserts=context._colocated_upserts,
            )
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = OpsTicket.ProposeNewRuleAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'loopos.v1.OpsTicket.ProposeNewRule') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'loopos.v1.OpsTicket.ProposeNewRule') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'loopos.v1.OpsTicket.ProposeNewRule') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'loopos.v1.OpsTicket.ProposeNewRule') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'loopos.v1.OpsTicket.ProposeNewRule') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.OpsTicket.ProposeNewRule') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'loopos.v1.OpsTicket.ProposeNewRule') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.OpsTicket.ProposeNewRule') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'loopos.v1.OpsTicket.ProposeNewRule') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _ProposeNewRule(
        self,
        request: google.protobuf.empty_pb2.Empty,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='ProposeNewRule'
        )

        # Check if we already have performed this mutation!
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response = loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse()
            response.ParseFromString(idempotent_mutation.response)
            return response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=OpsTicket.ProposeNewRuleAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.OpsTicketMethods.ProposeNewRule',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                transaction=transaction,
                from_constructor=False,
                requires_constructor=True,
            ) as (state, writer):

                effects = await self.__ProposeNewRule(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )

                await writer.complete(effects)

                # TODO: We need a single `Effects` superclass for all methods, so we
                # would need to make it "partially" generic (with per-method subclasses
                # filling out the rest of the generic parameters) in order to fix this.
                return effects.response  # type: ignore[return-value]

    async def _schedule_ProposeNewRule(
        self,
        *,
        request: google.protobuf.empty_pb2.Empty,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            method='ProposeNewRule',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=OpsTicket.ProposeNewRuleAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='ProposeNewRule'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.OpsTicketMethods.ProposeNewRule',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await OpsTicketServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).ProposeNewRule(
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def ProposeNewRule(
        self,
        request: google.protobuf.empty_pb2.Empty,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_ProposeNewRule(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                    method='ProposeNewRule',
                    context_type=IMPORT_reboot_aio_contexts.WriterContext,
                )
                assert context is not None

                return await self._ProposeNewRule(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    async def __ShowBrainSources(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: loopos.v1.loopos_pb2.OpsTicket,
        request: google.protobuf.empty_pb2.Empty,
        *,
        validating_effects: bool,
    ) -> loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse:
        try:
            typed_state: OpsTicket.State = OpsTicketFromProto(state, is_initial_state=(context.state_id in states_being_constructed))

            if ongoing_transaction_state_key(context) in ongoing_transaction_states:
                typed_state = ongoing_transaction_states[ongoing_transaction_state_key(context)].model_copy(deep=True)
            response = (
                await self._servicer._ShowBrainSources(
                    context=context,
                    state=typed_state,
                    request=request
                )
            )


            OpsTicketToProto(typed_state, state)

            IMPORT_reboot_aio_types.assert_type(
                response,
                [loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse],
            )
            self.maybe_raise_effect_validation_retry(
                logger=logger,
                idempotency_manager=context,
                method_name='OpsTicket.ShowBrainSources',
                validating_effects=validating_effects,
                context=context,
            )
            return response
        except IMPORT_reboot_aio_contexts.EffectValidationRetry:
            # Doing effect validation, just let this propagate.
            raise
        except IMPORT_reboot.aio.aborted.Aborted as aborted:
            # If the caller aborted due to a retryable error, just
            # propagate the aborted instead of propagating `Unknown`
            # so that a client can transparently retry.
            if IMPORT_reboot.aio.aborted.is_retryable(aborted):
                raise aborted
            # Log any _unhandled_ abort stack traces to make it
            # easier for debugging.
            #
            # NOTE: we don't log if we're a task as it will be logged
            # in `public/reboot/aio/internals/tasks_dispatcher.py` instead.
            aborted_type: IMPORT_typing.Optional[type] = None
            aborted_type = OpsTicket.ShowBrainSourcesAborted
            if isinstance(aborted, IMPORT_reboot.aio.aborted.SystemAborted):
                # Not logging when within `node` as we already log there.
                if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                    logger.warning(
                        f"Unhandled (in 'loopos.v1.OpsTicket.ShowBrainSources') {aborted}; propagating as 'Unknown'\n" +
                        ''.join(IMPORT_traceback.format_exception(aborted))
                    )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                    # TODO(benh): consider whether or not we want to
                    # include the 'package.service.method' which may
                    # get concatenated together forming a kind of
                    # "stack trace"; while it's super helpful for
                    # debugging, it does expose implementation
                    # information.
                    message=f"unhandled (in 'loopos.v1.OpsTicket.ShowBrainSources') {aborted}"
                )
            else:
                if (
                    aborted_type is not None and
                    not isinstance(aborted, aborted_type) and
                    aborted_type.is_declared_error(aborted.error)
                ):
                    # We propagate declared errors that might have
                    # come from another call, i.e., we might have an
                    # `Aborted` but not for this method but the
                    # `Aborted` that we have has an error that this
                    # method declared. This allows a developer to
                    # simply add the declared error to their `.proto`
                    # file rather than having to catch and re-raise
                    # the error with their own aborted type.
                    if context.task is None:
                        logger.warning(
                            f"Propagating unhandled but declared error (in 'loopos.v1.OpsTicket.ShowBrainSources') {aborted}"
                        )
                elif (
                    aborted_type is None or
                    not isinstance(aborted, aborted_type)
                ):
                    # Not logging when within `node` as we already log there.
                    if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                        logger.warning(
                            f"Unhandled (in 'loopos.v1.OpsTicket.ShowBrainSources') {aborted}; propagating as 'Unknown'\n" +
                            ''.join(IMPORT_traceback.format_exception(aborted))
                        )
                    # If this wasn't a declared error than we
                    # propagate it as `Unknown`.
                    raise IMPORT_reboot.aio.aborted.SystemAborted(
                        IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        # TODO(benh): consider whether or not we want to
                        # include the 'package.service.method' which may
                        # get concatenated together forming a kind of
                        # "stack trace"; while it's super helpful for
                        # debugging, it does expose implementation
                        # information.
                        message=f"unhandled (in 'loopos.v1.OpsTicket.ShowBrainSources') {aborted}"
                    )

            raise
        except IMPORT_asyncio.CancelledError:
            # It's pretty normal for an RPC to be cancelled; it's not useful to
            # print a stack trace.
            raise
        except IMPORT_google_protobuf_message.DecodeError as decode_error:
            # We usually see this error when we are trying to construct a proto
            # message which is too deeply nested: protobuf has a limit of 100
            # nested messages. See the limits here:
            #   https://protobuf.dev/programming-guides/proto-limits/

            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.OpsTicket.ShowBrainSources') "
                    f"{type(decode_error).__name__}{': ' + str(decode_error) if len(str(decode_error)) > 0 else ''}; "
                    "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                    "See the limits here: https://protobuf.dev/programming-guides/proto-limits/" +
                    ''.join(IMPORT_traceback.format_exception(decode_error))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"unhandled (in 'loopos.v1.OpsTicket.ShowBrainSources') {decode_error}; "
                        "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
            )
        except BaseException as exception:
            # Not logging when within `node` as we already log there.
            if IMPORT_reboot_nodejs_python.should_print_stacktrace():
                logger.warning(
                    "Unhandled (in 'loopos.v1.OpsTicket.ShowBrainSources') "
                    f"{type(exception).__name__}{': ' + str(exception) if len(str(exception)) > 0 else ''}; "
                    "propagating as 'Unknown'\n" +
                    ''.join(IMPORT_traceback.format_exception(exception))
                )
            raise IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                # TODO(benh): consider whether or not we want to
                # include the 'package.service.method' which may
                # get concatenated together forming a kind of
                # "stack trace"; while it's super helpful for
                # debugging, it does expose implementation
                # information.
                message=f"unhandled (in 'loopos.v1.OpsTicket.ShowBrainSources') {type(exception).__name__}: {exception}"
            )
        finally:
            pass

    @IMPORT_reboot_aio_tracing.function_span(
        # We expect an `EffectValidationRetry` exception; that's not an error.
        set_status_on_exception=False
    )
    async def _ShowBrainSources(
        self,
        request: google.protobuf.empty_pb2.Empty,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        *,
        validating_effects: bool,
        grpc_context: IMPORT_typing.Optional[IMPORT_grpc.aio.ServicerContext] = None,
    ) -> loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse:
        # Try to verify the token if a token verifier exists.
        context.auth = await self._maybe_verify_token(
            headers=context._headers, method='ShowBrainSources'
        )


        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=OpsTicket.ShowBrainSourcesAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )
            authorizer = self._maybe_authorize(
                method_name='loopos.v1.OpsTicketMethods.ShowBrainSources',
                headers=context._headers,
                auth=context.auth,
                request=request,
            )
            async with self._state_manager.reader(
                context,
                self._servicer.__state_type__,
                authorize=authorizer,
            ) as state:
                response = await self.__ShowBrainSources(
                    context,
                    state,
                    request,
                    validating_effects=validating_effects,
                )
                return response

    async def _schedule_ShowBrainSources(
        self,
        *,
        request: google.protobuf.empty_pb2.Empty,
        headers: IMPORT_reboot_aio_headers.Headers,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> tuple[IMPORT_reboot_aio_contexts.WriterContext, loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse]:
        context: IMPORT_reboot_aio_contexts.WriterContext = self.create_context(
            headers=headers,
            state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            method='ShowBrainSources',
            context_type=IMPORT_reboot_aio_contexts.WriterContext,
        )
        response = loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse()

        # Check if we already have performed this schedule! Note that
        # we need to do this for all kinds of methods because this is
        # effectively a mutation (actually a `writer`, see below).
        #
        # We do this _before_ calling 'transactionally()' because
        # if this call is for a transaction method _and_ we've
        # already performed the transaction then we don't want to
        # become a transaction participant (again) we just want to
        # return the transaction's response.
        idempotent_mutation = await self._state_manager.check_for_idempotent_mutation(
            context
        )

        if idempotent_mutation is not None:
            response.ParseFromString(idempotent_mutation.response)

            # We should have only scheduled a single task!
            assert len(idempotent_mutation.task_ids) == 1
            assert grpc_context is not None
            grpc_context.set_trailing_metadata(
                grpc_context.trailing_metadata() +
                (
                    (
                        IMPORT_reboot_aio_headers.TASK_ID_UUID,
                        str(IMPORT_uuid.UUID(bytes=idempotent_mutation.task_ids[0].task_uuid))
                    ),
                )
            )

            return context, response

        async with self._state_manager.transactionally(
            context,
            self.tasks_dispatcher,
            aborted_type=OpsTicket.ShowBrainSourcesAborted,
        ) as transaction:
            if transaction is not None:
                context.participants.add(
                    self._servicer.__state_type_name__, context._state_ref
                )

            # Try to verify the token if a token verifier exists.
            context.auth = await self._maybe_verify_token(
                headers=headers, method='ShowBrainSources'
            )

            async with self._state_manager.writer(
                context,
                self._servicer.__state_type__,
                self.tasks_dispatcher,
                transaction=transaction,
                authorize=self._maybe_authorize(
                    method_name='loopos.v1.OpsTicketMethods.ShowBrainSources',
                    headers=context._headers,
                    auth=context.auth,
                    request=request,
                ),
                from_constructor=False,
                requires_constructor=True
            ) as (state, writer):

                task = await OpsTicketServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ).ShowBrainSources(
                    schedule=context._headers.task_schedule,
                )

                effects = IMPORT_reboot_aio_state_managers.Effects(
                    response=response,
                    state=state,
                    tasks=[task],
                )

                assert effects.tasks is not None

                await writer.complete(effects)

                assert grpc_context is not None

                grpc_context.set_trailing_metadata(
                    grpc_context.trailing_metadata() +
                    (
                        (
                            IMPORT_reboot_aio_headers.TASK_ID_UUID,
                            str(IMPORT_uuid.UUID(bytes=task.task_id.task_uuid))
                        ),
                    )
                )

                return context, response

        return context, response


    # Entrypoint for non-reactive network calls (i.e. typical gRPC calls).
    async def ShowBrainSources(
        self,
        request: google.protobuf.empty_pb2.Empty,
        grpc_context: IMPORT_grpc.aio.ServicerContext,
    ) -> loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse:
        headers = IMPORT_reboot_aio_headers.Headers.from_grpc_context(grpc_context)
        assert headers.application_id is not None  # Guaranteed by `Headers`.

        # Confirm whether this is the right server to be serving this
        # request.
        try:
            authoritative_server = self.placement_client.server_for_actor(
                headers.application_id,
                headers.state_ref,
            )
        except IMPORT_reboot_aio_placement.UnknownApplicationError:
            # It's possible that the user did indeed type an application ID
            # that doesn't exist, but it's also quite possible that this
            # request reached us before the placement planner had gossipped
            # out the information about which applications exist (we see
            # this e.g. after `rbt dev`'s chaos monkey restarts). For that
            # reason, abort with a retryable error.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Application '{headers.application_id}' not found. If you "
                "are confident the application exists, this may be because "
                "the system is still starting.",
            )
            raise  # Unreachable but necessary for mypy.
        if authoritative_server != self.server_id:
            # This is NOT the correct server. Fail.
            await grpc_context.abort(
                IMPORT_grpc.StatusCode.UNAVAILABLE,
                f"Server '{self.server_id}' is not authoritative for this "
                f"request; server '{authoritative_server}' is.",
            )
            raise  # Unreachable but necessary for mypy.

        @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
        async def _run(
            validating_effects: bool,
        ) -> loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse:
            context: IMPORT_typing.Optional[IMPORT_reboot_aio_contexts.Context] = None
            try:
                if headers.task_schedule is not None:
                    context, response = await self._schedule_ShowBrainSources(
                        headers=headers,
                        request=request,
                        grpc_context=grpc_context,
                    )
                    return response

                context = self.create_context(
                    headers=headers,
                    state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                    method='ShowBrainSources',
                    context_type=IMPORT_reboot_aio_contexts.ReaderContext,
                )
                assert context is not None

                return await self._ShowBrainSources(
                    request,
                    context,
                    validating_effects=validating_effects,
                    grpc_context=grpc_context,
                )
            except IMPORT_reboot_aio_contexts.EffectValidationRetry:
                # Doing effect validation, just let this propagate.
                raise
            except IMPORT_reboot.aio.aborted.Aborted as aborted:
                status = IMPORT_rpc_status_sync.to_status(aborted.to_status())
                # Need to add transaction participants here because
                # calling `grpc_context.abort_with_status()` will
                # ignore any other trailing metadata. Only propagate
                # transaction participants metadata if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    status = status._replace(
                        trailing_metadata=status.trailing_metadata + context.participants.to_grpc_metadata()
                    )
                await grpc_context.abort_with_status(status)
                raise  # Unreachable but necessary for mypy.
            except IMPORT_asyncio.CancelledError:
                # It's pretty normal for an RPC to be cancelled; it's not useful to
                # print a stack trace.
                raise
            except BaseException as exception:
                # Print the exception stack trace for easier debugging. Note
                # that we don't include the stack trace in an error message
                # for the same reason that gRPC doesn't do so by default,
                # see https://github.com/grpc/grpc/issues/14897, but since this
                # should only get logged on the server side it is safe.
                logger.warning(
                    'Unhandled exception\n' +
                    ''.join(IMPORT_traceback.format_exc() if IMPORT_reboot_nodejs_python.should_print_stacktrace() else [f"{type(exception).__name__}: {exception}"])
                )

                # Re-raise the exception for gRPC to handle!
                #
                # TODO: gRPC will print a stack trace from this
                # exception which we don't want if we're executing via
                # Node.js.
                raise
            finally:
                # Propagate transaction participants, if the caller cares.
                # Callers that care are those that are themselves transactions.
                # It's important to not just send this information to everyone;
                # some clients can't tolerate trailers, see:
                #   https://github.com/reboot-dev/mono/issues/5081
                if context is not None and headers.transaction_ids is not None:
                    assert context.transaction_id is not None
                    grpc_context.set_trailing_metadata(
                        grpc_context.trailing_metadata() +
                        context.participants.to_grpc_metadata()
                    )

        with IMPORT_reboot_aio_tracing.context_from_headers(headers):
            return await _run()

    def _maybe_authorize(
        self,
        *,
        method_name: str,
        headers: IMPORT_reboot_aio_headers.Headers,
        auth: IMPORT_typing.Optional[IMPORT_reboot_aio_auth.Auth],
        request: IMPORT_typing.Optional[OpsTicketRequestTypes] = None,
    ) -> IMPORT_typing.Optional[IMPORT_typing.Callable[[IMPORT_typing.Optional[OpsTicketStateType]], IMPORT_typing.Awaitable[None]]]:
        """Returns a function to check authorization for the given method.

        Raises `PermissionDenied` in case Authorizer is present but the request
        is not authorized.
        """
        # To authorize internal calls, we use an internal magic token.
        if headers.bearer_token == __internal_magic_token__:
            return None

        assert self._authorizer is not None

        async def authorize(state: IMPORT_typing.Optional[OpsTicketStateType]) -> None:
            # Create context for the authorizer. This is a `ReaderContext`
            # independently of the calling context.
            with self.use_context(
                headers=(
                    # Get headers suitable for doing authorization.
                    headers.copy_for_token_verification_and_authorization()
                ),
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                method=method_name,
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            ) as context:
                context.auth = auth

                # Get the authorizer decision.
                authorization_decision = await self._authorizer.authorize(
                    method_name=method_name,
                    context=context,
                    state=state,
                    request=request,
                )

            # Enforce correct authorizer decision type.
            try:
                IMPORT_reboot_aio_types.assert_type(
                    authorization_decision,
                    [
                        IMPORT_rbt_v1alpha1.errors_pb2.Ok,
                        IMPORT_rbt_v1alpha1.errors_pb2.Unauthenticated,
                        IMPORT_rbt_v1alpha1.errors_pb2.PermissionDenied,
                    ]
                )
            except TypeError as e:
                # Retyping.cast the exception to provide more context.
                authorizer_type = f"{type(self._authorizer).__module__}.{type(self._authorizer).__name__}"
                raise TypeError(
                    f"Authorizer '{authorizer_type}' "
                    f"returned unexpected type '{type(authorization_decision).__name__}' "
                    f"for method '{method_name}' on "
                    f"`loopos.v1.OpsTicket('{headers.state_ref.id}')`"
                ) from e

            # If the decision is not `True`, raise a `SystemAborted` with either a
            # `PermissionDenied` error (in case of `False`) or an `Unauthenticated`
            # error.
            if not isinstance(authorization_decision, IMPORT_rbt_v1alpha1.errors_pb2.Ok):
                if isinstance(authorization_decision, IMPORT_rbt_v1alpha1.errors_pb2.Unauthenticated):
                    logger.warning(
                        f"Unauthenticated call to '{method_name}' on "
                        f"`loopos.v1.OpsTicket('{headers.state_ref.id}')`"
                    )

                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    authorization_decision,
                    message=
                    f"You are not authorized to call '{method_name}' on "
                    f"`loopos.v1.OpsTicket('{headers.state_ref.id}')`"
                )

        return authorize

    async def _maybe_verify_token(
        self,
        *,
        headers: IMPORT_reboot_aio_headers.Headers,
        method: str,
    ) -> IMPORT_typing.Optional[IMPORT_reboot_aio_auth.Auth]:
        """Verify the bearer token and if a token verifier is present.

        Returns the (optional) `reboot.aio.auth.Auth` object
        produced by the token verifier if the token can be verified.
        """
        if self._token_verifier is not None:
            if headers.bearer_token == __internal_magic_token__:
                return None

            with self.use_context(
                headers=(
                    # Get headers suitable for doing token verification.
                    headers.copy_for_token_verification_and_authorization()
                ),
                state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                method=method,
                context_type=IMPORT_reboot_aio_contexts.ReaderContext,
            ) as context:
                result = await self._token_verifier.verify_token(
                    context=context,
                    token=headers.bearer_token,
                )
                if isinstance(result, IMPORT_rbt_v1alpha1.errors_pb2.Unauthenticated):
                    raise IMPORT_reboot_aio_aborted.SystemAborted(
                        result,
                        message=result.message or None,
                    )
                return result

        return None


############################ Client Stubs ############################
# This section is relevant for clients accessing a Reboot service. Since
# servicers are themselves often clients also, this code is generated for
# them also.


class _UserStub(IMPORT_reboot_aio_stubs.Stub):

    __state_type_name__ = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User')

    def __init__(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        # Within a Reboot context we do not pass on the caller's bearer token, as that might
        # have security implications - we cannot simply trust any service we are calling with
        # the user's credentials. Instead, the developer can rely on the default app-internal
        # auth, or override that and set an explicit bearer token.
        #
        # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
        # by the developer for the purpose of making these calls.
        caller_id: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None
        if isinstance(context, IMPORT_reboot_aio_external.ExternalContext):
            # Note that only `ExternalContext` even has a `bearer_token` field.
            bearer_token = context.bearer_token
            # If the creator of the `ExternalContext` set an explicit caller ID, obey it.
            caller_id = context.caller_id
        else:
            caller_id = IMPORT_reboot_aio_caller_id.CallerID(
                application_id=context.application_id,
            )

        super().__init__(
            channel_manager=context.channel_manager,
            idempotency_manager=context,
            state_ref=state_ref,
            context=context if isinstance(context, IMPORT_reboot_aio_contexts.Context) else None,
            bearer_token=bearer_token,
            caller_id=caller_id,
        )

        # All the channels for all services of this state will go to the same
        # place, so we can just get a single channel and share it across all
        # stubs.
        channel = self._channel_manager.get_channel_to_state(
            self.__state_type_name__, state_ref
        )
        self._loopos_v1_usermethods_stub = loopos.v1.loopos_pb2_grpc.UserMethodsStub(channel)


class UserReaderStub(_UserStub):

    def __init__(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        *,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext, IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
        super().__init__(
            context=context,
            state_ref=state_ref,
            bearer_token=bearer_token,
        )

    # User specific methods:


    async def ListTickets(
        self,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> loopos.v1.loopos_pb2.UserListTicketsResponse:
        state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User')
        service_name = IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods')
        method = 'ListTickets'

        proto_request = UserListTicketsRequestToProto(
        )

        async def call():
            async with self._call(
                state_type_name,
                service_name,
                method,
                self._loopos_v1_usermethods_stub.ListTickets,
                proto_request,
                unary=True,
                reader=True,
                response_type=loopos.v1.loopos_pb2.UserListTicketsResponse,
                aborted_type=User.ListTicketsAborted,
                metadata=metadata,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable)
                return await call

        if isinstance(self._context, IMPORT_reboot_aio_contexts.WorkflowContext):
            # Use the idempotency manager to make sure that this
            # reader is being called following the rules.
            with self._context.idempotently(
                state_type_name=state_type_name,
                state_ref=self._headers.state_ref,
                service_name=service_name,
                method=method,
                mutation=False,
                request=proto_request,
                metadata=metadata,
                idempotency=idempotency,
                # Only need to pass `aborted_type` for mutations.
                aborted_type=None,
            ) as idempotency_key:
                assert idempotency is not None
                # Check if this reader is from an `.always()` and if
                # so, don't memoize!
                if idempotency.always:
                    return await call()

                assert idempotency_key is not None
                return await IMPORT_reboot_aio_workflows.at_least_once(
                    (
                        # TODO: for easier debugging include the
                        # original alias (or generated alias in the
                        # case of `.per_iteration()` w/o an alias)
                        # instead of just `idempotency_key`.
                        f'{ service_name }.{ method } ({str(idempotency_key)})',
                        # NOTE: we want this to be `PER_WORKFLOW`
                        # because any per iteration concerns should
                        # have already been taken care of by caller
                        # using `.per_iteration()`.
                        IMPORT_reboot_aio_workflows.PER_WORKFLOW
                    ),
                    self._context,
                    call,
                    type=loopos.v1.loopos_pb2.UserListTicketsResponse,
                )
        return await call()

    async def QueryBrain(
        self,
        request: User.QueryBrainRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> loopos.v1.loopos_pb2.UserQueryBrainResponse:
        state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User')
        service_name = IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods')
        method = 'QueryBrain'

        proto_request = UserQueryBrainRequestToProto(
            request,
        )

        async def call():
            async with self._call(
                state_type_name,
                service_name,
                method,
                self._loopos_v1_usermethods_stub.QueryBrain,
                proto_request,
                unary=True,
                reader=True,
                response_type=loopos.v1.loopos_pb2.UserQueryBrainResponse,
                aborted_type=User.QueryBrainAborted,
                metadata=metadata,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable)
                return await call

        if isinstance(self._context, IMPORT_reboot_aio_contexts.WorkflowContext):
            # Use the idempotency manager to make sure that this
            # reader is being called following the rules.
            with self._context.idempotently(
                state_type_name=state_type_name,
                state_ref=self._headers.state_ref,
                service_name=service_name,
                method=method,
                mutation=False,
                request=proto_request,
                metadata=metadata,
                idempotency=idempotency,
                # Only need to pass `aborted_type` for mutations.
                aborted_type=None,
            ) as idempotency_key:
                assert idempotency is not None
                # Check if this reader is from an `.always()` and if
                # so, don't memoize!
                if idempotency.always:
                    return await call()

                assert idempotency_key is not None
                return await IMPORT_reboot_aio_workflows.at_least_once(
                    (
                        # TODO: for easier debugging include the
                        # original alias (or generated alias in the
                        # case of `.per_iteration()` w/o an alias)
                        # instead of just `idempotency_key`.
                        f'{ service_name }.{ method } ({str(idempotency_key)})',
                        # NOTE: we want this to be `PER_WORKFLOW`
                        # because any per iteration concerns should
                        # have already been taken care of by caller
                        # using `.per_iteration()`.
                        IMPORT_reboot_aio_workflows.PER_WORKFLOW
                    ),
                    self._context,
                    call,
                    type=loopos.v1.loopos_pb2.UserQueryBrainResponse,
                )
        return await call()

    async def LiveState(
        self,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> loopos.v1.loopos_pb2.UserLiveStateResponse:
        state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User')
        service_name = IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods')
        method = 'LiveState'

        proto_request = UserLiveStateRequestToProto(
        )

        async def call():
            async with self._call(
                state_type_name,
                service_name,
                method,
                self._loopos_v1_usermethods_stub.LiveState,
                proto_request,
                unary=True,
                reader=True,
                response_type=loopos.v1.loopos_pb2.UserLiveStateResponse,
                aborted_type=User.LiveStateAborted,
                metadata=metadata,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable)
                return await call

        if isinstance(self._context, IMPORT_reboot_aio_contexts.WorkflowContext):
            # Use the idempotency manager to make sure that this
            # reader is being called following the rules.
            with self._context.idempotently(
                state_type_name=state_type_name,
                state_ref=self._headers.state_ref,
                service_name=service_name,
                method=method,
                mutation=False,
                request=proto_request,
                metadata=metadata,
                idempotency=idempotency,
                # Only need to pass `aborted_type` for mutations.
                aborted_type=None,
            ) as idempotency_key:
                assert idempotency is not None
                # Check if this reader is from an `.always()` and if
                # so, don't memoize!
                if idempotency.always:
                    return await call()

                assert idempotency_key is not None
                return await IMPORT_reboot_aio_workflows.at_least_once(
                    (
                        # TODO: for easier debugging include the
                        # original alias (or generated alias in the
                        # case of `.per_iteration()` w/o an alias)
                        # instead of just `idempotency_key`.
                        f'{ service_name }.{ method } ({str(idempotency_key)})',
                        # NOTE: we want this to be `PER_WORKFLOW`
                        # because any per iteration concerns should
                        # have already been taken care of by caller
                        # using `.per_iteration()`.
                        IMPORT_reboot_aio_workflows.PER_WORKFLOW
                    ),
                    self._context,
                    call,
                    type=loopos.v1.loopos_pb2.UserLiveStateResponse,
                )
        return await call()




class UserWriterStub(_UserStub):

    def __init__(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        *,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.TransactionContext, IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
        super().__init__(
            context=context,
            state_ref=state_ref,
            bearer_token=bearer_token,
        )

    # User specific methods:


    async def ListTickets(
        self,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> loopos.v1.loopos_pb2.UserListTicketsResponse:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            'ListTickets',
            self._loopos_v1_usermethods_stub.ListTickets,
            UserListTicketsRequestToProto(
            ),
            unary=True,
            reader=True,
            response_type=loopos.v1.loopos_pb2.UserListTicketsResponse,
            aborted_type=User.ListTicketsAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def QueryBrain(
        self,
        request: User.QueryBrainRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> loopos.v1.loopos_pb2.UserQueryBrainResponse:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            'QueryBrain',
            self._loopos_v1_usermethods_stub.QueryBrain,
            UserQueryBrainRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=loopos.v1.loopos_pb2.UserQueryBrainResponse,
            aborted_type=User.QueryBrainAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def LiveState(
        self,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> loopos.v1.loopos_pb2.UserLiveStateResponse:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            'LiveState',
            self._loopos_v1_usermethods_stub.LiveState,
            UserLiveStateRequestToProto(
            ),
            unary=True,
            reader=True,
            response_type=loopos.v1.loopos_pb2.UserLiveStateResponse,
            aborted_type=User.LiveStateAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def Create(
        self,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        proto_request = UserCreateRequestToProto(
        )
        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            method='Create',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=User.CreateAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
                'Create',
                self._loopos_v1_usermethods_stub.Create,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=User.CreateAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call


class UserWorkflowStub(_UserStub):

    def __init__(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.TransactionContext, IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
        super().__init__(
            context=context,
            state_ref=state_ref,
            bearer_token=bearer_token,
        )

    # User specific methods:
    async def IngestTextMessage(
        self,
        request: User.IngestTextMessageRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> loopos.v1.loopos_pb2.UserIngestTextMessageResponse:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = UserIngestTextMessageRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            method='IngestTextMessage',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=User.IngestTextMessageAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
                'IngestTextMessage',
                self._loopos_v1_usermethods_stub.IngestTextMessage,
                proto_request,
                unary=True,
                reader=False,
                response_type=loopos.v1.loopos_pb2.UserIngestTextMessageResponse,
                aborted_type=User.IngestTextMessageAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def IngestVoiceNote(
        self,
        request: User.IngestVoiceNoteRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = UserIngestVoiceNoteRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            method='IngestVoiceNote',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=User.IngestVoiceNoteAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
                'IngestVoiceNote',
                self._loopos_v1_usermethods_stub.IngestVoiceNote,
                proto_request,
                unary=True,
                reader=False,
                response_type=loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse,
                aborted_type=User.IngestVoiceNoteAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def ListTickets(
        self,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> loopos.v1.loopos_pb2.UserListTicketsResponse:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            'ListTickets',
            self._loopos_v1_usermethods_stub.ListTickets,
            UserListTicketsRequestToProto(
            ),
            unary=True,
            reader=True,
            response_type=loopos.v1.loopos_pb2.UserListTicketsResponse,
            aborted_type=User.ListTicketsAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def QueryBrain(
        self,
        request: User.QueryBrainRequest,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> loopos.v1.loopos_pb2.UserQueryBrainResponse:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            'QueryBrain',
            self._loopos_v1_usermethods_stub.QueryBrain,
            UserQueryBrainRequestToProto(
                request,
            ),
            unary=True,
            reader=True,
            response_type=loopos.v1.loopos_pb2.UserQueryBrainResponse,
            aborted_type=User.QueryBrainAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def LiveState(
        self,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> loopos.v1.loopos_pb2.UserLiveStateResponse:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            'LiveState',
            self._loopos_v1_usermethods_stub.LiveState,
            UserLiveStateRequestToProto(
            ),
            unary=True,
            reader=True,
            response_type=loopos.v1.loopos_pb2.UserLiveStateResponse,
            aborted_type=User.LiveStateAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call

    async def Create(
        self,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = UserCreateRequestToProto(
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            method='Create',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=User.CreateAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
                'Create',
                self._loopos_v1_usermethods_stub.Create,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=User.CreateAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call



class UserTasksStub(_UserStub):

    def __init__(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        *,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.TransactionContext, IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
        super().__init__(
            context=context,
            state_ref=state_ref,
            bearer_token=bearer_token,
        )

    # User specific methods:
    async def IngestTextMessage(
        self,
        request: User.IngestTextMessageRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = UserIngestTextMessageRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            method='IngestTextMessage',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=User.IngestTextMessageAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
                'IngestTextMessage',
                self._loopos_v1_usermethods_stub.IngestTextMessage,
                proto_request,
                unary=True,
                reader=False,
                response_type=loopos.v1.loopos_pb2.UserIngestTextMessageResponse,
                aborted_type=User.IngestTextMessageAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def IngestVoiceNote(
        self,
        request: User.IngestVoiceNoteRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = UserIngestVoiceNoteRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            method='IngestVoiceNote',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=User.IngestVoiceNoteAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
                'IngestVoiceNote',
                self._loopos_v1_usermethods_stub.IngestVoiceNote,
                proto_request,
                unary=True,
                reader=False,
                response_type=loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse,
                aborted_type=User.IngestVoiceNoteAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def ListTickets(
        self,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = UserListTicketsRequestToProto(
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            method='ListTickets',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=User.ListTicketsAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
                'ListTickets',
                self._loopos_v1_usermethods_stub.ListTickets,
                proto_request,
                unary=True,
                reader=False,
                response_type=loopos.v1.loopos_pb2.UserListTicketsResponse,
                aborted_type=User.ListTicketsAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def QueryBrain(
        self,
        request: User.QueryBrainRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = UserQueryBrainRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            method='QueryBrain',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=User.QueryBrainAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
                'QueryBrain',
                self._loopos_v1_usermethods_stub.QueryBrain,
                proto_request,
                unary=True,
                reader=False,
                response_type=loopos.v1.loopos_pb2.UserQueryBrainResponse,
                aborted_type=User.QueryBrainAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def LiveState(
        self,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = UserLiveStateRequestToProto(
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
            method='LiveState',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=User.LiveStateAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
                'LiveState',
                self._loopos_v1_usermethods_stub.LiveState,
                proto_request,
                unary=True,
                reader=False,
                response_type=loopos.v1.loopos_pb2.UserLiveStateResponse,
                aborted_type=User.LiveStateAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )


class UserServicerTasks:

    _context: IMPORT_reboot_aio_contexts.WriterContext

    def __init__(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.WriterContext])
        self._context = context
        self._state_ref = state_ref

    # User specific methods:
    async def IngestTextMessage(
        self,
        request: User.IngestTextMessageRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._state_ref,
            method_name='IngestTextMessage',
            request=UserIngestTextMessageRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def IngestVoiceNote(
        self,
        request: User.IngestVoiceNoteRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._state_ref,
            method_name='IngestVoiceNote',
            request=UserIngestVoiceNoteRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def ListTickets(
        self,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._state_ref,
            method_name='ListTickets',
            request=UserListTicketsRequestToProto(
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def QueryBrain(
        self,
        request: User.QueryBrainRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._state_ref,
            method_name='QueryBrain',
            request=UserQueryBrainRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def LiveState(
        self,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._state_ref,
            method_name='LiveState',
            request=UserLiveStateRequestToProto(
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def Create(
        self,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
            state_ref=self._state_ref,
            method_name='Create',
            request=UserCreateRequestToProto(
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task


class _OpsTicketStub(IMPORT_reboot_aio_stubs.Stub):

    __state_type_name__ = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket')

    def __init__(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        # Within a Reboot context we do not pass on the caller's bearer token, as that might
        # have security implications - we cannot simply trust any service we are calling with
        # the user's credentials. Instead, the developer can rely on the default app-internal
        # auth, or override that and set an explicit bearer token.
        #
        # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
        # by the developer for the purpose of making these calls.
        caller_id: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None
        if isinstance(context, IMPORT_reboot_aio_external.ExternalContext):
            # Note that only `ExternalContext` even has a `bearer_token` field.
            bearer_token = context.bearer_token
            # If the creator of the `ExternalContext` set an explicit caller ID, obey it.
            caller_id = context.caller_id
        else:
            caller_id = IMPORT_reboot_aio_caller_id.CallerID(
                application_id=context.application_id,
            )

        super().__init__(
            channel_manager=context.channel_manager,
            idempotency_manager=context,
            state_ref=state_ref,
            context=context if isinstance(context, IMPORT_reboot_aio_contexts.Context) else None,
            bearer_token=bearer_token,
            caller_id=caller_id,
        )

        # All the channels for all services of this state will go to the same
        # place, so we can just get a single channel and share it across all
        # stubs.
        channel = self._channel_manager.get_channel_to_state(
            self.__state_type_name__, state_ref
        )
        self._loopos_v1_opsticketmethods_stub = loopos.v1.loopos_pb2_grpc.OpsTicketMethodsStub(channel)


class OpsTicketReaderStub(_OpsTicketStub):

    def __init__(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        *,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext, IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
        super().__init__(
            context=context,
            state_ref=state_ref,
            bearer_token=bearer_token,
        )

    # OpsTicket specific methods:





    async def ShowBrainSources(
        self,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse:
        state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket')
        service_name = IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods')
        method = 'ShowBrainSources'

        proto_request = OpsTicketShowBrainSourcesRequestToProto(
        )

        async def call():
            async with self._call(
                state_type_name,
                service_name,
                method,
                self._loopos_v1_opsticketmethods_stub.ShowBrainSources,
                proto_request,
                unary=True,
                reader=True,
                response_type=loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse,
                aborted_type=OpsTicket.ShowBrainSourcesAborted,
                metadata=metadata,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable)
                return await call

        if isinstance(self._context, IMPORT_reboot_aio_contexts.WorkflowContext):
            # Use the idempotency manager to make sure that this
            # reader is being called following the rules.
            with self._context.idempotently(
                state_type_name=state_type_name,
                state_ref=self._headers.state_ref,
                service_name=service_name,
                method=method,
                mutation=False,
                request=proto_request,
                metadata=metadata,
                idempotency=idempotency,
                # Only need to pass `aborted_type` for mutations.
                aborted_type=None,
            ) as idempotency_key:
                assert idempotency is not None
                # Check if this reader is from an `.always()` and if
                # so, don't memoize!
                if idempotency.always:
                    return await call()

                assert idempotency_key is not None
                return await IMPORT_reboot_aio_workflows.at_least_once(
                    (
                        # TODO: for easier debugging include the
                        # original alias (or generated alias in the
                        # case of `.per_iteration()` w/o an alias)
                        # instead of just `idempotency_key`.
                        f'{ service_name }.{ method } ({str(idempotency_key)})',
                        # NOTE: we want this to be `PER_WORKFLOW`
                        # because any per iteration concerns should
                        # have already been taken care of by caller
                        # using `.per_iteration()`.
                        IMPORT_reboot_aio_workflows.PER_WORKFLOW
                    ),
                    self._context,
                    call,
                    type=loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse,
                )
        return await call()



class OpsTicketWriterStub(_OpsTicketStub):

    def __init__(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        *,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.TransactionContext, IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
        super().__init__(
            context=context,
            state_ref=state_ref,
            bearer_token=bearer_token,
        )

    # OpsTicket specific methods:
    async def Create(
        self,
        request: OpsTicket.CreateRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        proto_request = OpsTicketCreateRequestToProto(
            request,
        )
        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='Create',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.CreateAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'Create',
                self._loopos_v1_opsticketmethods_stub.Create,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=OpsTicket.CreateAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def Triage(
        self,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        proto_request = OpsTicketTriageRequestToProto(
        )
        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='Triage',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.TriageAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'Triage',
                self._loopos_v1_opsticketmethods_stub.Triage,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=OpsTicket.TriageAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def AcknowledgeDispatch(
        self,
        request: OpsTicket.AcknowledgeDispatchRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        proto_request = OpsTicketAcknowledgeDispatchRequestToProto(
            request,
        )
        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='AcknowledgeDispatch',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.AcknowledgeDispatchAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'AcknowledgeDispatch',
                self._loopos_v1_opsticketmethods_stub.AcknowledgeDispatch,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=OpsTicket.AcknowledgeDispatchAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call


    async def ProposeNewRule(
        self,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse:
        proto_request = OpsTicketProposeNewRuleRequestToProto(
        )
        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='ProposeNewRule',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.ProposeNewRuleAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'ProposeNewRule',
                self._loopos_v1_opsticketmethods_stub.ProposeNewRule,
                proto_request,
                unary=True,
                reader=False,
                response_type=loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse,
                aborted_type=OpsTicket.ProposeNewRuleAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def ShowBrainSources(
        self,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            'ShowBrainSources',
            self._loopos_v1_opsticketmethods_stub.ShowBrainSources,
            OpsTicketShowBrainSourcesRequestToProto(
            ),
            unary=True,
            reader=True,
            response_type=loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse,
            aborted_type=OpsTicket.ShowBrainSourcesAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call


class OpsTicketWorkflowStub(_OpsTicketStub):

    def __init__(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.TransactionContext, IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
        super().__init__(
            context=context,
            state_ref=state_ref,
            bearer_token=bearer_token,
        )

    # OpsTicket specific methods:
    async def Create(
        self,
        request: OpsTicket.CreateRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = OpsTicketCreateRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='Create',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.CreateAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'Create',
                self._loopos_v1_opsticketmethods_stub.Create,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=OpsTicket.CreateAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def Triage(
        self,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = OpsTicketTriageRequestToProto(
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='Triage',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.TriageAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'Triage',
                self._loopos_v1_opsticketmethods_stub.Triage,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=OpsTicket.TriageAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def AcknowledgeDispatch(
        self,
        request: OpsTicket.AcknowledgeDispatchRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> google.protobuf.empty_pb2.Empty:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = OpsTicketAcknowledgeDispatchRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='AcknowledgeDispatch',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.AcknowledgeDispatchAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'AcknowledgeDispatch',
                self._loopos_v1_opsticketmethods_stub.AcknowledgeDispatch,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=OpsTicket.AcknowledgeDispatchAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call


    async def ProposeNewRule(
        self,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = OpsTicketProposeNewRuleRequestToProto(
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='ProposeNewRule',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.ProposeNewRuleAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'ProposeNewRule',
                self._loopos_v1_opsticketmethods_stub.ProposeNewRule,
                proto_request,
                unary=True,
                reader=False,
                response_type=loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse,
                aborted_type=OpsTicket.ProposeNewRuleAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                return await call

    async def ShowBrainSources(
        self,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse:
        async with self._call(
            IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            'ShowBrainSources',
            self._loopos_v1_opsticketmethods_stub.ShowBrainSources,
            OpsTicketShowBrainSourcesRequestToProto(
            ),
            unary=True,
            reader=True,
            response_type=loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse,
            aborted_type=OpsTicket.ShowBrainSourcesAborted,
            metadata=metadata,
            bearer_token=bearer_token,
        ) as call:
            assert isinstance(call, IMPORT_typing.Awaitable), type(call)
            return await call



class OpsTicketTasksStub(_OpsTicketStub):

    def __init__(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        *,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.TransactionContext, IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
        super().__init__(
            context=context,
            state_ref=state_ref,
            bearer_token=bearer_token,
        )

    # OpsTicket specific methods:
    async def Triage(
        self,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = OpsTicketTriageRequestToProto(
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='Triage',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.TriageAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'Triage',
                self._loopos_v1_opsticketmethods_stub.Triage,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=OpsTicket.TriageAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def AcknowledgeDispatch(
        self,
        request: OpsTicket.AcknowledgeDispatchRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = OpsTicketAcknowledgeDispatchRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='AcknowledgeDispatch',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.AcknowledgeDispatchAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'AcknowledgeDispatch',
                self._loopos_v1_opsticketmethods_stub.AcknowledgeDispatch,
                proto_request,
                unary=True,
                reader=False,
                response_type=google.protobuf.empty_pb2.Empty,
                aborted_type=OpsTicket.AcknowledgeDispatchAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def DispatchWithEscalation(
        self,
        request: OpsTicket.DispatchWithEscalationRequest,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = OpsTicketDispatchWithEscalationRequestToProto(
            request,
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='DispatchWithEscalation',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.DispatchWithEscalationAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'DispatchWithEscalation',
                self._loopos_v1_opsticketmethods_stub.DispatchWithEscalation,
                proto_request,
                unary=True,
                reader=False,
                response_type=loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse,
                aborted_type=OpsTicket.DispatchWithEscalationAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def ProposeNewRule(
        self,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = OpsTicketProposeNewRuleRequestToProto(
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='ProposeNewRule',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.ProposeNewRuleAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'ProposeNewRule',
                self._loopos_v1_opsticketmethods_stub.ProposeNewRule,
                proto_request,
                unary=True,
                reader=False,
                response_type=loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse,
                aborted_type=OpsTicket.ProposeNewRuleAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )
    async def ShowBrainSources(
        self,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
        idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
        proto_request = OpsTicketShowBrainSourcesRequestToProto(
        )

        with self._idempotency_manager.idempotently(
            state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._headers.state_ref,
            service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
            method='ShowBrainSources',
            mutation=True,
            request=proto_request,
            metadata=metadata,
            idempotency=idempotency,
            aborted_type=OpsTicket.ShowBrainSourcesAborted,
        ) as idempotency_key:
            async with self._call(
                IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                'ShowBrainSources',
                self._loopos_v1_opsticketmethods_stub.ShowBrainSources,
                proto_request,
                unary=True,
                reader=False,
                response_type=loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse,
                aborted_type=OpsTicket.ShowBrainSourcesAborted,
                metadata=metadata,
                idempotency_key=idempotency_key,
                per_iteration=idempotency.per_iteration if idempotency is not None else False,
                bearer_token=bearer_token,
            ) as call:
                assert isinstance(call, IMPORT_typing.Awaitable), type(call)
                await call
                for (key, value) in await call.trailing_metadata():  # type: ignore[misc, attr-defined]
                    if key == IMPORT_reboot_aio_headers.TASK_ID_UUID:
                        return IMPORT_rbt_v1alpha1.tasks_pb2.TaskId(
                            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                            state_ref=self._headers.state_ref.to_str(),
                            task_uuid=IMPORT_uuid.UUID(value).bytes,
                        )
                raise IMPORT_reboot.aio.aborted.SystemAborted(
                    IMPORT_rbt_v1alpha1.errors_pb2.Internal(),
                    message='Trailing metadata missing for task schedule',
                )


class OpsTicketServicerTasks:

    _context: IMPORT_reboot_aio_contexts.WriterContext

    def __init__(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        *,
        state_ref: IMPORT_reboot_aio_types.StateRef,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ):
        IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.WriterContext])
        self._context = context
        self._state_ref = state_ref

    # OpsTicket specific methods:
    async def Create(
        self,
        request: OpsTicket.CreateRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._state_ref,
            method_name='Create',
            request=OpsTicketCreateRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def Triage(
        self,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._state_ref,
            method_name='Triage',
            request=OpsTicketTriageRequestToProto(
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def AcknowledgeDispatch(
        self,
        request: OpsTicket.AcknowledgeDispatchRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._state_ref,
            method_name='AcknowledgeDispatch',
            request=OpsTicketAcknowledgeDispatchRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def DispatchWithEscalation(
        self,
        request: OpsTicket.DispatchWithEscalationRequest,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._state_ref,
            method_name='DispatchWithEscalation',
            request=OpsTicketDispatchWithEscalationRequestToProto(
                request,
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def ProposeNewRule(
        self,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._state_ref,
            method_name='ProposeNewRule',
            request=OpsTicketProposeNewRuleRequestToProto(
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task

    async def ShowBrainSources(
        self,
        *,
        schedule: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
    ) -> IMPORT_reboot_aio_tasks.TaskEffect:
        schedule = ensure_has_timezone(when=schedule)
        task = IMPORT_reboot_aio_tasks.TaskEffect(
            state_type=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
            state_ref=self._state_ref,
            method_name='ShowBrainSources',
            request=OpsTicketShowBrainSourcesRequestToProto(
            ),
            schedule=(IMPORT_reboot_time_DateTimeWithTimeZone.now() + schedule) if isinstance(
                schedule, IMPORT_datetime_timedelta
            ) else schedule,
        )

        self._context._tasks.append(task)

        return task



############################ Authorizers ############################
# Relevant to servicers; irrelevant to clients.

UserStateType: IMPORT_typing.TypeAlias = loopos.v1.loopos_pb2.User
UserRequestTypes: IMPORT_typing.TypeAlias = \
        loopos.v1.loopos_pb2.UserIngestTextMessageRequest \
        | loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest \
        | google.protobuf.empty_pb2.Empty \
        | loopos.v1.loopos_pb2.UserQueryBrainRequest

class UserAuthorizer(
    IMPORT_reboot_aio_auth_authorizers.Authorizer[UserStateType, UserRequestTypes],
):
    StateType: IMPORT_typing.TypeAlias = UserStateType
    RequestTypes: IMPORT_typing.TypeAlias = UserRequestTypes
    Decision: IMPORT_typing.TypeAlias = IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision

    def __init__(
        self,
        *,
        IngestTextMessage: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              User.State,
              User.IngestTextMessageRequest,
            ]
        ] = None,
        ingest_text_message: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              User.State,
              User.IngestTextMessageRequest,
            ]
        ] = None,
        IngestVoiceNote: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              User.State,
              User.IngestVoiceNoteRequest,
            ]
        ] = None,
        ingest_voice_note: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              User.State,
              User.IngestVoiceNoteRequest,
            ]
        ] = None,
        ListTickets: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              User.State,
              None
            ]
        ] = None,
        list_tickets: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              User.State,
              None
            ]
        ] = None,
        QueryBrain: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              User.State,
              User.QueryBrainRequest,
            ]
        ] = None,
        query_brain: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              User.State,
              User.QueryBrainRequest,
            ]
        ] = None,
        LiveState: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              User.State,
              None
            ]
        ] = None,
        live_state: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              User.State,
              None
            ]
        ] = None,
        Create: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              User.State,
              None
            ]
        ] = None,
        create: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              User.State,
              None
            ]
        ] = None,
        # NOTE: using `_` prefix for `_default` so as not to collide
        # with any method names since a prefixed `_` is forbidden by
        # our protoc plugins.
        _default: IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
            loopos.v1.loopos_pb2.User,
            IMPORT_google_protobuf_message.Message,
        ] = IMPORT_reboot_aio_auth_authorizers.allow_if(
            any=[
                IMPORT_reboot_aio_auth_authorizers.state_id_is_user_id,
                IMPORT_reboot_aio_auth_authorizers.is_app_internal,
            ],
        ),
    ):
        if ingest_text_message is not None and IngestTextMessage is not None:
            raise ValueError(
                f"Cannot specify both 'IngestTextMessage' and 'ingest_text_message' authorizer rules"
            )
        self._ingest_text_message = ingest_text_message or IngestTextMessage
        if ingest_voice_note is not None and IngestVoiceNote is not None:
            raise ValueError(
                f"Cannot specify both 'IngestVoiceNote' and 'ingest_voice_note' authorizer rules"
            )
        self._ingest_voice_note = ingest_voice_note or IngestVoiceNote
        if list_tickets is not None and ListTickets is not None:
            raise ValueError(
                f"Cannot specify both 'ListTickets' and 'list_tickets' authorizer rules"
            )
        self._list_tickets = list_tickets or ListTickets
        if query_brain is not None and QueryBrain is not None:
            raise ValueError(
                f"Cannot specify both 'QueryBrain' and 'query_brain' authorizer rules"
            )
        self._query_brain = query_brain or QueryBrain
        if live_state is not None and LiveState is not None:
            raise ValueError(
                f"Cannot specify both 'LiveState' and 'live_state' authorizer rules"
            )
        self._live_state = live_state or LiveState
        if create is not None and Create is not None:
            raise ValueError(
                f"Cannot specify both 'Create' and 'create' authorizer rules"
            )
        self._create = create or Create
        self.__default = _default

    async def authorize(
        self,
        *,
        method_name: str,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: IMPORT_typing.Optional[UserStateType],
        request: IMPORT_typing.Optional[UserRequestTypes],
        **kwargs,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        if method_name == 'loopos.v1.UserMethods.IngestTextMessage':
            return await self.IngestTextMessage(
                context=context,
                state=UserFromProto(state, is_initial_state=(context.state_id in states_being_constructed)) if state is not None else state,
                request=UserIngestTextMessageRequestFromProto(request) if request is not None else request,
            )
        elif method_name == 'loopos.v1.UserMethods.IngestVoiceNote':
            return await self.IngestVoiceNote(
                context=context,
                state=UserFromProto(state, is_initial_state=(context.state_id in states_being_constructed)) if state is not None else state,
                request=UserIngestVoiceNoteRequestFromProto(request) if request is not None else request,
            )
        elif method_name == 'loopos.v1.UserMethods.ListTickets':
            return await self.ListTickets(
                context=context,
                state=UserFromProto(state, is_initial_state=(context.state_id in states_being_constructed)) if state is not None else state,
            )
        elif method_name == 'loopos.v1.UserMethods.QueryBrain':
            return await self.QueryBrain(
                context=context,
                state=UserFromProto(state, is_initial_state=(context.state_id in states_being_constructed)) if state is not None else state,
                request=UserQueryBrainRequestFromProto(request) if request is not None else request,
            )
        elif method_name == 'loopos.v1.UserMethods.LiveState':
            return await self.LiveState(
                context=context,
                state=UserFromProto(state, is_initial_state=(context.state_id in states_being_constructed)) if state is not None else state,
            )
        elif method_name == 'loopos.v1.UserMethods.Create':
            return await self.Create(
                context=context,
            )
        else:
            return IMPORT_rbt_v1alpha1.errors_pb2.PermissionDenied()

    # For 'loopos.v1.UserMethods.IngestTextMessage'.
    async def IngestTextMessage(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: User.IngestTextMessageRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._ingest_text_message or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'loopos.v1.UserMethods.IngestVoiceNote'.
    async def IngestVoiceNote(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: User.IngestVoiceNoteRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._ingest_voice_note or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'loopos.v1.UserMethods.ListTickets'.
    async def ListTickets(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._list_tickets or self.__default).execute(
            context=context,
            state=state,
            # TODO: Don't pass 'None' if user specified 'request=None'
            # in the Pydantic API. We need to support dropping 'request'
            # argument in the default 'AuthorizerRule's before.
            request=None,
        )

    # For 'loopos.v1.UserMethods.QueryBrain'.
    async def QueryBrain(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: User.QueryBrainRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._query_brain or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'loopos.v1.UserMethods.LiveState'.
    async def LiveState(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._live_state or self.__default).execute(
            context=context,
            state=state,
            # TODO: Don't pass 'None' if user specified 'request=None'
            # in the Pydantic API. We need to support dropping 'request'
            # argument in the default 'AuthorizerRule's before.
            request=None,
        )

    # For 'loopos.v1.UserMethods.Create'.
    async def Create(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._create or self.__default).execute(
            context=context,
            state=None,
            # TODO: Don't pass 'None' if user specified 'request=None'
            # in the Pydantic API. We need to support dropping 'request'
            # argument in the default 'AuthorizerRule's before.
            request=None,
        )


OpsTicketStateType: IMPORT_typing.TypeAlias = loopos.v1.loopos_pb2.OpsTicket
OpsTicketRequestTypes: IMPORT_typing.TypeAlias = \
        loopos.v1.loopos_pb2.OpsTicketCreateRequest \
        | google.protobuf.empty_pb2.Empty \
        | loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest \
        | loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationRequest

class OpsTicketAuthorizer(
    IMPORT_reboot_aio_auth_authorizers.Authorizer[OpsTicketStateType, OpsTicketRequestTypes],
):
    StateType: IMPORT_typing.TypeAlias = OpsTicketStateType
    RequestTypes: IMPORT_typing.TypeAlias = OpsTicketRequestTypes
    Decision: IMPORT_typing.TypeAlias = IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision

    def __init__(
        self,
        *,
        Create: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              OpsTicket.State,
              OpsTicket.CreateRequest,
            ]
        ] = None,
        create: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              OpsTicket.State,
              OpsTicket.CreateRequest,
            ]
        ] = None,
        Triage: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              OpsTicket.State,
              None
            ]
        ] = None,
        triage: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              OpsTicket.State,
              None
            ]
        ] = None,
        AcknowledgeDispatch: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              OpsTicket.State,
              OpsTicket.AcknowledgeDispatchRequest,
            ]
        ] = None,
        acknowledge_dispatch: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              OpsTicket.State,
              OpsTicket.AcknowledgeDispatchRequest,
            ]
        ] = None,
        DispatchWithEscalation: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              OpsTicket.State,
              OpsTicket.DispatchWithEscalationRequest,
            ]
        ] = None,
        dispatch_with_escalation: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              OpsTicket.State,
              OpsTicket.DispatchWithEscalationRequest,
            ]
        ] = None,
        ProposeNewRule: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              OpsTicket.State,
              None
            ]
        ] = None,
        propose_new_rule: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              OpsTicket.State,
              None
            ]
        ] = None,
        ShowBrainSources: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              OpsTicket.State,
              None
            ]
        ] = None,
        show_brain_sources: IMPORT_typing.Optional[
            IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
              OpsTicket.State,
              None
            ]
        ] = None,
        # NOTE: using `_` prefix for `_default` so as not to collide
        # with any method names since a prefixed `_` is forbidden by
        # our protoc plugins.
        _default: IMPORT_reboot_aio_auth_authorizers.AuthorizerRule[
            loopos.v1.loopos_pb2.OpsTicket,
            IMPORT_google_protobuf_message.Message,
        ] = IMPORT_reboot_aio_auth_authorizers.allow_if(
            all=[IMPORT_reboot_aio_auth_authorizers.is_app_internal],
        ),
    ):
        if create is not None and Create is not None:
            raise ValueError(
                f"Cannot specify both 'Create' and 'create' authorizer rules"
            )
        self._create = create or Create
        if triage is not None and Triage is not None:
            raise ValueError(
                f"Cannot specify both 'Triage' and 'triage' authorizer rules"
            )
        self._triage = triage or Triage
        if acknowledge_dispatch is not None and AcknowledgeDispatch is not None:
            raise ValueError(
                f"Cannot specify both 'AcknowledgeDispatch' and 'acknowledge_dispatch' authorizer rules"
            )
        self._acknowledge_dispatch = acknowledge_dispatch or AcknowledgeDispatch
        if dispatch_with_escalation is not None and DispatchWithEscalation is not None:
            raise ValueError(
                f"Cannot specify both 'DispatchWithEscalation' and 'dispatch_with_escalation' authorizer rules"
            )
        self._dispatch_with_escalation = dispatch_with_escalation or DispatchWithEscalation
        if propose_new_rule is not None and ProposeNewRule is not None:
            raise ValueError(
                f"Cannot specify both 'ProposeNewRule' and 'propose_new_rule' authorizer rules"
            )
        self._propose_new_rule = propose_new_rule or ProposeNewRule
        if show_brain_sources is not None and ShowBrainSources is not None:
            raise ValueError(
                f"Cannot specify both 'ShowBrainSources' and 'show_brain_sources' authorizer rules"
            )
        self._show_brain_sources = show_brain_sources or ShowBrainSources
        self.__default = _default

    async def authorize(
        self,
        *,
        method_name: str,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: IMPORT_typing.Optional[OpsTicketStateType],
        request: IMPORT_typing.Optional[OpsTicketRequestTypes],
        **kwargs,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        if method_name == 'loopos.v1.OpsTicketMethods.Create':
            return await self.Create(
                context=context,
                request=OpsTicketCreateRequestFromProto(request) if request is not None else request,
            )
        elif method_name == 'loopos.v1.OpsTicketMethods.Triage':
            return await self.Triage(
                context=context,
                state=OpsTicketFromProto(state, is_initial_state=(context.state_id in states_being_constructed)) if state is not None else state,
            )
        elif method_name == 'loopos.v1.OpsTicketMethods.AcknowledgeDispatch':
            return await self.AcknowledgeDispatch(
                context=context,
                state=OpsTicketFromProto(state, is_initial_state=(context.state_id in states_being_constructed)) if state is not None else state,
                request=OpsTicketAcknowledgeDispatchRequestFromProto(request) if request is not None else request,
            )
        elif method_name == 'loopos.v1.OpsTicketMethods.DispatchWithEscalation':
            return await self.DispatchWithEscalation(
                context=context,
                state=OpsTicketFromProto(state, is_initial_state=(context.state_id in states_being_constructed)) if state is not None else state,
                request=OpsTicketDispatchWithEscalationRequestFromProto(request) if request is not None else request,
            )
        elif method_name == 'loopos.v1.OpsTicketMethods.ProposeNewRule':
            return await self.ProposeNewRule(
                context=context,
                state=OpsTicketFromProto(state, is_initial_state=(context.state_id in states_being_constructed)) if state is not None else state,
            )
        elif method_name == 'loopos.v1.OpsTicketMethods.ShowBrainSources':
            return await self.ShowBrainSources(
                context=context,
                state=OpsTicketFromProto(state, is_initial_state=(context.state_id in states_being_constructed)) if state is not None else state,
            )
        else:
            return IMPORT_rbt_v1alpha1.errors_pb2.PermissionDenied()

    # For 'loopos.v1.OpsTicketMethods.Create'.
    async def Create(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: OpsTicket.CreateRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._create or self.__default).execute(
            context=context,
            state=None,
            request=request,
        )

    # For 'loopos.v1.OpsTicketMethods.Triage'.
    async def Triage(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: OpsTicket.State,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._triage or self.__default).execute(
            context=context,
            state=state,
            # TODO: Don't pass 'None' if user specified 'request=None'
            # in the Pydantic API. We need to support dropping 'request'
            # argument in the default 'AuthorizerRule's before.
            request=None,
        )

    # For 'loopos.v1.OpsTicketMethods.AcknowledgeDispatch'.
    async def AcknowledgeDispatch(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: OpsTicket.State,
        request: OpsTicket.AcknowledgeDispatchRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._acknowledge_dispatch or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'loopos.v1.OpsTicketMethods.DispatchWithEscalation'.
    async def DispatchWithEscalation(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: OpsTicket.State,
        request: OpsTicket.DispatchWithEscalationRequest,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._dispatch_with_escalation or self.__default).execute(
            context=context,
            state=state,
            request=request,
        )

    # For 'loopos.v1.OpsTicketMethods.ProposeNewRule'.
    async def ProposeNewRule(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: OpsTicket.State,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._propose_new_rule or self.__default).execute(
            context=context,
            state=state,
            # TODO: Don't pass 'None' if user specified 'request=None'
            # in the Pydantic API. We need to support dropping 'request'
            # argument in the default 'AuthorizerRule's before.
            request=None,
        )

    # For 'loopos.v1.OpsTicketMethods.ShowBrainSources'.
    async def ShowBrainSources(
        self,
        *,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: OpsTicket.State,
    ) -> IMPORT_reboot_aio_auth_authorizers.Authorizer.Decision:
        return await (self._show_brain_sources or self.__default).execute(
            context=context,
            state=state,
            # TODO: Don't pass 'None' if user specified 'request=None'
            # in the Pydantic API. We need to support dropping 'request'
            # argument in the default 'AuthorizerRule's before.
            request=None,
        )



############################ Reboot Servicers ############################
# Base classes for server-side implementations of Reboot servicers.
# Irrelevant to clients.

class UserBaseServicer(IMPORT_reboot_aio_servicers.Servicer):
    Authorizer: IMPORT_typing.TypeAlias = UserAuthorizer
    # Node-backed servicers attach a JS bridge object at runtime. We declare it
    # on the base class so mypy accepts accesses when a servicer is typed as
    # `UserBaseServicer` (e.g. in classmethod workflows).
    _js_servicer_reference: IMPORT_typing.Any

    __servicer__: IMPORT_contextvars.ContextVar[IMPORT_typing.Optional[UserBaseServicer]] = IMPORT_contextvars.ContextVar(
        'Provides access to a servicer in the current asyncio context. '
        'We need that to be able to do inline writes and reads inside '
        'a workflow',
        default=None,
    )

    __service_names__ = [
        IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
    ]
    __state_type_name__ = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User')
    __state_type__ = loopos.v1.loopos_pb2.User
    __file_descriptor__ = loopos.v1.loopos_pb2.DESCRIPTOR

    def __init__(self):
        super().__init__()
        # NOTE: need to hold on to the middleware so we can do inline
        # writes (see 'self.write(...)').
        #
        # Because '_middleware' is not really private this does mean
        # users may do possibly dangerous things, but this is no more
        # likely given they could have already overridden
        # 'create_middleware()'.
        self._middleware: IMPORT_typing.Optional[UserServicerMiddleware] = None

    def create_middleware(
        self,
        *,
        application_id: IMPORT_reboot_aio_types.ApplicationId,
        server_id: IMPORT_reboot_aio_types.ServerId,
        state_manager: IMPORT_reboot_aio_state_managers.StateManager,
        placement_client: IMPORT_reboot_aio_placement.PlacementClient,
        channel_manager: IMPORT_reboot_aio_internals_channel_manager._ChannelManager,
        tasks_cache: IMPORT_reboot_aio_internals_tasks_cache.TasksCache,
        token_verifier: IMPORT_typing.Optional[IMPORT_reboot_aio_auth_token_verifiers.TokenVerifier],
        effect_validation: IMPORT_reboot_aio_contexts.EffectValidation,
        ready: IMPORT_asyncio.Event,
    ) -> UserServicerMiddleware:
        self._middleware = UserServicerMiddleware(
            servicer=self,
            application_id=application_id,
            server_id=server_id,
            state_manager=state_manager,
            placement_client=placement_client,
            channel_manager=channel_manager,
            tasks_cache=tasks_cache,
            token_verifier=token_verifier,
            effect_validation=effect_validation,
            ready=ready,
        )
        return self._middleware

    def authorizer(self) -> IMPORT_typing.Optional[IMPORT_reboot_aio_auth_authorizers.Authorizer | IMPORT_reboot_aio_auth_authorizers.AuthorizerRule]:
        return None

    def token_verifier(self) -> IMPORT_typing.Optional[IMPORT_reboot_aio_auth_token_verifiers.TokenVerifier]:
        return None

    _is_auto_construct = True


    @staticmethod
    def _mcp_tool_names() -> list[str]:
        """Return the MCP tool names this servicer registers."""
        return [
            "ingest_text_message",
            "ingest_voice_note",
            "list_tickets",
            "query_brain",
            "live_state",
            "show_loopos_dashboard",
        ]

    @staticmethod
    def _add_mcp(
        mcp: IMPORT_mcp_server_fastmcp.FastMCP,
        auto_construct_state_type_full_names: list[IMPORT_reboot_aio_types.StateTypeName],
    ) -> None:
        """Register MCP tools and resources for User."""

        # Tool for 'loopos.v1.UserMethods.IngestTextMessage'.
        @mcp.tool(
            name="ingest_text_message",
            title="IngestTextMessage",
            description="Create a new OpsTicket from a text message (the demo path). Pass the property_id, the raw text in the operator's native language, and the sender_user_id. Triage runs synchronously and the ticket is returned ready to display.",
        )
        async def ingest_text_message_tool(
            ctx: IMPORT_mcp_server_fastmcp.Context,
            *,
            request: User.IngestTextMessageRequest,
        ):
            reboot_context = IMPORT_reboot_mcp_context.get_reboot_context(ctx)
            id = IMPORT_reboot_mcp_context.get_mcp_user_id(ctx)
            # State is auto-constructed on session init;
            # see `_auto_construct`.
            ref = User.ref(id)
            return await ref.ingest_text_message(reboot_context, request)


        # Tool for 'loopos.v1.UserMethods.IngestVoiceNote'.
        @mcp.tool(
            name="ingest_voice_note",
            title="IngestVoiceNote",
            description="Create a new OpsTicket from a voice note URL. Whisper transcribes via Runpod (or hardcoded fallback dict for stage), then triage runs.",
        )
        async def ingest_voice_note_tool(
            ctx: IMPORT_mcp_server_fastmcp.Context,
            *,
            request: User.IngestVoiceNoteRequest,
        ):
            reboot_context = IMPORT_reboot_mcp_context.get_reboot_context(ctx)
            id = IMPORT_reboot_mcp_context.get_mcp_user_id(ctx)
            # State is auto-constructed on session init;
            # see `_auto_construct`.
            ref = User.ref(id)
            return await ref.ingest_voice_note(reboot_context, request)


        # Tool for 'loopos.v1.UserMethods.ListTickets'.
        @mcp.tool(
            name="list_tickets",
            title="ListTickets",
            description="List all tickets visible to this user, with one-line summaries. Use this to show what's open, what's resolved, and where cost has gone.",
        )
        async def list_tickets_tool(
            ctx: IMPORT_mcp_server_fastmcp.Context,
        ):
            reboot_context = IMPORT_reboot_mcp_context.get_reboot_context(ctx)
            id = IMPORT_reboot_mcp_context.get_mcp_user_id(ctx)
            # State is auto-constructed on session init;
            # see `_auto_construct`.
            ref = User.ref(id)
            return await ref.list_tickets(reboot_context)


        # Tool for 'loopos.v1.UserMethods.QueryBrain'.
        @mcp.tool(
            name="query_brain",
            title="QueryBrain",
            description="Query the four-layer Company Brain (founder voice memos + SOPs + historical resolutions) for context relevant to a question. Optional property_id narrows the scope.",
        )
        async def query_brain_tool(
            ctx: IMPORT_mcp_server_fastmcp.Context,
            *,
            request: User.QueryBrainRequest,
        ):
            reboot_context = IMPORT_reboot_mcp_context.get_reboot_context(ctx)
            id = IMPORT_reboot_mcp_context.get_mcp_user_id(ctx)
            # State is auto-constructed on session init;
            # see `_auto_construct`.
            ref = User.ref(id)
            return await ref.query_brain(reboot_context, request)


        # Tool for 'loopos.v1.UserMethods.LiveState'.
        @mcp.tool(
            name="live_state",
            title="LiveState",
            description="Return the current dashboard state — active ticket summaries and the last 20 events. Used by the UI and by AI clients to introspect the system.",
        )
        async def live_state_tool(
            ctx: IMPORT_mcp_server_fastmcp.Context,
        ):
            reboot_context = IMPORT_reboot_mcp_context.get_reboot_context(ctx)
            id = IMPORT_reboot_mcp_context.get_mcp_user_id(ctx)
            # State is auto-constructed on session init;
            # see `_auto_construct`.
            ref = User.ref(id)
            return await ref.live_state(reboot_context)


        # Map UI method names to web app paths.
        _ui_app_paths: dict[str, str] = {
            "show_loopos_dashboard": "web/ui/loopos-ui",
        }

        # Optional artifact path overrides (only populated
        # when `artifact_path` is explicitly set).
        _ui_artifact_paths: dict[str, str] = {
        }

        # Cache-bust tokens: content hash of each UI's built
        # `index.html`, or a per-process random fallback if the
        # artifact is missing. Embedded in the tool's
        # `_meta.ui.resourceUri` below and in the `@mcp.resource`
        # URI template so hosts that aggressively cache MCP
        # resource reads (e.g., ChatGPT) see a fresh URI after
        # each rebuild. Computed once at servicer setup since
        # `--config=dist` restarts the process on deploy. The
        # helper falls back to a per-process token (and logs)
        # rather than raising if the project root can't be
        # located — deploys with unusual filesystem layouts
        # still start cleanly.
        _ui_cache_busts: dict[str, str] = (
            IMPORT_reboot_mcp_ui.compute_ui_cache_busts(
                __file__,
                _ui_app_paths,
                _ui_artifact_paths,
            )
        )

        # Register each UI tool's recompute inputs in the
        # server-local registry. The patched `FastMCP.list_tools`
        # in `reboot.mcp.factories` reads this on every
        # `tools/list` call and rewrites `_meta.ui.resourceUri`
        # with a freshly-hashed token so clients that re-list on
        # new sessions (Claude, MCPJam) pick up dist-file changes
        # without a server restart. Kept server-local — never
        # rides on the wire in `_meta`.
        IMPORT_reboot_mcp_ui.register_ui_tool_for_cache_bust(
            tool_name="show_loopos_dashboard",
            caller_file=__file__,
            ui_path=_ui_app_paths["show_loopos_dashboard"],
            ui_name="show_loopos_dashboard",
            artifact_path=_ui_artifact_paths.get("show_loopos_dashboard"),
            uri_prefix=(
                "ui://user/"
                "show_loopos_dashboard/"
            ),
        )

        # UI tools — allow AI to trigger loading of UIs.
        @mcp.tool(
            name="show_loopos_dashboard",
            title="Show LoopOS",
            description="Open the LoopOS three-pane dashboard inside this chat: ticket list (left), chat/triage detail (center), per-property cost ticker (right). Use this once at the start of a session.",
            meta={
                "ui": {
                    "resourceUri": (
                        "ui://user/show_loopos_dashboard/"
                        + _ui_cache_busts["show_loopos_dashboard"]
                    ),
                },
            },
        )
        async def show_loopos_dashboard_tool(
            ctx: IMPORT_mcp_server_fastmcp.Context,
        ) -> dict:
            _ids: dict[IMPORT_reboot_aio_types.StateTypeName, str | None] = {
                IMPORT_reboot_aio_types.StateTypeName("loopos.v1.User"):
                    IMPORT_reboot_mcp_context.get_mcp_user_id(ctx),
            }
            # Also include all other auto-construct IDs.
            for _auto_construct_full_name in auto_construct_state_type_full_names:
                if _auto_construct_full_name not in _ids:
                    _ids[_auto_construct_full_name] = (
                        IMPORT_reboot_mcp_context.get_mcp_user_id(ctx))
            result: dict = {"ids": _ids}

            # Invariant: the MCP server has a bearer token for this tool
            # call because opening a UI requires an authenticated
            # session. This is the only place where the token can be
            # passed to the iframe, since the iframe has no other access
            # to the MCP session credentials. The token is read by
            # `McpConnector` from `ontoolresult` and forwarded to
            # `RebootClientProvider` for all Reboot calls inside the
            # app. This tool is also invoked by `McpConnector` to
            # refresh an expired token, so the field must always be
            # present when a token is available.
            reboot_context = IMPORT_reboot_mcp_context.get_reboot_context(ctx)
            if reboot_context is not None and reboot_context.bearer_token is not None:
                result["bearer_token"] = reboot_context.bearer_token
            return result

        def _get_reboot_url_from_request(
            ctx: IMPORT_mcp_server_fastmcp.Context,
        ) -> str:
            """Extract Reboot URL from request headers."""
            request = ctx.request_context.request
            if request is None:
                raise RuntimeError(
                    "No HTTP request in MCP context"
                )
            host = (
                request.headers.get("x-forwarded-host")
                or request.headers.get("host")
            )
            if not host:
                raise RuntimeError(
                    "No host header in request"
                )
            scheme = (
                request.headers.get("x-forwarded-proto")
                or ("https" if request.url.scheme == "https"
                    else "http")
            )
            return f"{scheme}://{host}"

        @mcp.resource(
            "ui://user/{ui}/{cache_bust}",
            name="user-ui",
            title="User UI",
            description="User UIs.",
            mime_type="text/html;profile=mcp-app",
        )
        def user_ui_resource(
            ctx: IMPORT_mcp_server_fastmcp.Context,
            ui: str,
            cache_bust: str,
        ) -> str:
            # `cache_bust` comes from the URI's trailing
            # segment: the content-hash token that `list_tools`
            # wrote into the tool's `_meta.ui.resourceUri`. We
            # pass it to `ui_html` so the per-process HTML
            # cache keys on it — same token = cache hit,
            # changed token (because the dist file changed) =
            # cache miss = fresh read.
            reboot_url = _get_reboot_url_from_request(ctx)
            if ui not in _ui_app_paths:
                raise ValueError(f"Unknown UI: {ui}")
            return IMPORT_reboot_mcp_ui.ui_html(
                _ui_app_paths[ui],
                reboot_url,
                ui_name=ui,
                artifact_path=_ui_artifact_paths.get(ui),
                cache_bust=cache_bust,
            )

        pass  # End of _add_mcp.

    @staticmethod
    async def _auto_construct(
        context: IMPORT_reboot_aio_external.ExternalContext,
        state_id: str,
    ) -> None:
        """Auto-construct a `User` state."""
        # Some states have `_auto_construct` called many times, e.g. a
        # per-user state may get auto-constructed at the start of every
        # MCP session. We derive a deterministic idempotency key from the
        # state ID so that repeated calls (even from different contexts)
        # are a NOOP. We prefer this over catching the
        # `StateAlreadyExists` error that would otherwise result, since
        # that logs an `ERROR` to user-visible logs.
        idempotency_key = IMPORT_uuid.uuid5(
            IMPORT_uuid.NAMESPACE_URL,
            f"urn:dev.reboot:auto-construct:User:{state_id}",
        )
        await User.idempotently(
            key=idempotency_key,
        ).create(context, state_id)

    def ref(
        self,
        *,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> User.WeakReference[User.WeakReference._WriterSchedule]:
        context = IMPORT_reboot_aio_contexts.Context.get()

        if context is None:
            raise RuntimeError(
                'Missing asyncio context variable `context`; '
                'are you using this class without Reboot?'
            )

        return User.WeakReference(
            # TODO(https://github.com/reboot-dev/mono/issues/3226): add support for calling other applications.
            # For now this always stays within the application that creates the context.
            application_id=None,
            state_id=context._state_ref.id,
            schedule_type=User.WeakReference._WriterSchedule,
            # If the user didn't specify a bearer token we may still end up using the app-internal bearer token,
            # but that's decided at the time of the call.
            bearer_token=bearer_token,
            servicer=self,
        )

    class Effects(IMPORT_reboot_aio_state_managers.Effects):
        def __init__(
            self,
            *,
            state: loopos.v1.loopos_pb2.User,
            response: IMPORT_typing.Optional[IMPORT_google_protobuf_message.Message] = None,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [loopos.v1.loopos_pb2.User])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)






    # For 'loopos.v1.UserMethods.Create'.
    class CreateEffects(Effects):
        def __init__(
            self,
            *,
            state: loopos.v1.loopos_pb2.User,
            response: google.protobuf.empty_pb2.Empty,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [loopos.v1.loopos_pb2.User])
            IMPORT_reboot_aio_types.assert_type(response, [google.protobuf.empty_pb2.Empty])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)



    InlineWriterCallableResult = IMPORT_typing.TypeVar('InlineWriterCallableResult', covariant=True)

    class InlineWriterCallable(IMPORT_typing.Protocol[InlineWriterCallableResult]):
        async def __call__(
            self,
            state: loopos.v1.loopos_pb2.User
        ) -> UserBaseServicer.InlineWriterCallableResult:
            ...

    class WorkflowState:

        def __init__(
            self,
            servicer,
        ):
            self._servicer = servicer

        async def read(
            self, context: IMPORT_reboot_aio_contexts.WorkflowContext
        ) -> User.State:
            """Read the current state within a workflow."""
            return await (
                self.always() if context.within_until()
                else (
                    self.per_iteration() if context.within_loop()
                    else self.per_workflow()
                )
            ).read(context)

        async def write(
            self,
            idempotency_alias: str,
            context: IMPORT_reboot_aio_contexts.WorkflowContext,
            writer: UserBaseServicer.InlineWriterCallable[UserBaseServicer.InlineWriterCallableResult],
            __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
            *,
            type: IMPORT_reboot_aio_workflows.Type | IMPORT_reboot_aio_workflows._Unset = IMPORT_reboot_aio_workflows._UNSET,
        ) -> UserBaseServicer.InlineWriterCallableResult:
            """Perform an "inline write" within a workflow.

            `type=` is optional: when omitted, `writer`'s return
            annotation is used (falling back to `type(None)` if
            there is none).
            """
            type_t, _ = IMPORT_reboot_aio_workflows._resolve_callable_return_type(
                writer, type,
            )
            return await (
                self.per_iteration(idempotency_alias) if context.within_loop()
                else self.per_workflow(idempotency_alias)
            ).write(
                context, writer, __options__, type=type_t
            )

        class _Idempotently:

            def __init__(
                self,
                *,
                servicer: UserBaseServicer,
                alias: IMPORT_typing.Optional[str],
                how: IMPORT_reboot_aio_workflows.How,
            ):
                self._servicer = servicer
                self._alias = alias
                self._how = how

            async def read(
                self, context: IMPORT_reboot_aio_contexts.WorkflowContext
            ) -> User.State:
                """Read the current state within a workflow."""
                return await self._read(
                    self._servicer,
                    context.idempotency(
                        key=IMPORT_reboot_aio_idempotency.make_expiring_idempotency_key(),
                        how=IMPORT_reboot_aio_idempotency.ALWAYS,
                    ) if self._how == IMPORT_reboot_aio_workflows.ALWAYS else context.idempotency(
                        alias=self._alias,
                        how=self._how,
                    ),
                    context,
                )

            @staticmethod
            async def _read(
                servicer: UserBaseServicer,
                idempotency: IMPORT_reboot_aio_idempotency.Idempotency,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
            ) -> User.State:
                """Read the current state within a workflow."""
                IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.WorkflowContext])

                if servicer._middleware is None:
                    raise RuntimeError(
                        'Reboot middleware was not created; '
                        'are you using this class without Reboot?'
                    )

                async def read() -> loopos.v1.loopos_pb2.User:
                    assert servicer._middleware is not None
                    return await servicer._middleware._state_manager.read(
                        context, servicer.__state_type__
                    )

                if idempotency.always:
                    return UserFromProto(await read())

                state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User')

                # Use the idempotency manager to make sure that this
                # reader is being called following the rules.
                with context.idempotently(
                    state_type_name=state_type_name,
                    state_ref=context._state_ref,
                    # Not calling a method so `service_name`,
                    # `method`, `request`, etc are irrelevant.
                    service_name=None,
                    method=None,
                    mutation=False,
                    request=None,
                    metadata=None,
                    idempotency=idempotency,
                    # Only need to pass `aborted_type` for mutations.
                    aborted_type=None,
                ) as idempotency_key:
                    assert idempotency_key is not None
                    protobuf_state = await IMPORT_reboot_aio_workflows.at_least_once(
                        (
                            # TODO: for easier debugging include the
                            # original alias (or generated alias in
                            # the case of `.per_iteration()` w/o an
                            # alias) instead of just
                            # `idempotency_key`.
                            f"inline reader of '{ state_type_name }' ({str(idempotency_key)})",
                            # NOTE: we want this to be `PER_WORKFLOW`
                            # because any per iteration concerns
                            # should have already been taken care of
                            # by caller using `.per_iteration()`.
                            IMPORT_reboot_aio_workflows.PER_WORKFLOW
                        ),
                        context,
                        read,
                        type=loopos.v1.loopos_pb2.User,
                    )

                    return UserFromProto(protobuf_state)

            async def write(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: UserBaseServicer.InlineWriterCallable[UserBaseServicer.InlineWriterCallableResult],
                __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
                *,
                type: IMPORT_reboot_aio_workflows.Type | IMPORT_reboot_aio_workflows._Unset = IMPORT_reboot_aio_workflows._UNSET,
                check_type: bool = True,
            ) -> UserBaseServicer.InlineWriterCallableResult:
                # `type=` is optional: when omitted, `writer`'s
                # return annotation is used (falling back to
                # `type(None)` when there is none).
                type_t, _ = IMPORT_reboot_aio_workflows._resolve_callable_return_type(
                    writer, type,
                )
                return await self._write(
                    context,
                    writer,
                    __options__,
                    type_result=type_t,
                    check_type=check_type,
                )

            async def _write(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: UserBaseServicer.InlineWriterCallable[UserBaseServicer.InlineWriterCallableResult],
                __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
                *,
                type_result: IMPORT_reboot_aio_workflows.TypeT,
                check_type: bool,
            ) -> UserBaseServicer.InlineWriterCallableResult:
                unidempotently = self._how == IMPORT_reboot_aio_workflows.ALWAYS
                idempotency = (
                    context.idempotency(
                        key=IMPORT_reboot_aio_idempotency.make_expiring_idempotency_key(),
                        how=IMPORT_reboot_aio_idempotency.ALWAYS,
                    ) if unidempotently else context.idempotency(
                        alias=self._alias,
                        how=self._how,
                    )
                )

                return await self._write_validating_effects(
                    self._servicer,
                    idempotency,
                    context,
                    writer,
                    __options__,
                    type_result=type_result,
                    check_type=check_type,
                    unidempotently=unidempotently,
                    checkpoint=context.checkpoint(),
                )

            @staticmethod
            @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
            async def _write_validating_effects(
                validating_effects: bool,
                servicer: UserBaseServicer,
                idempotency: IMPORT_reboot_aio_idempotency.Idempotency,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: UserBaseServicer.InlineWriterCallable[UserBaseServicer.InlineWriterCallableResult],
                __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
                *,
                type_result: IMPORT_reboot_aio_workflows.TypeT,
                check_type: bool,
                unidempotently: bool,
                checkpoint: IMPORT_reboot_aio_idempotency.Checkpoint,
            ) -> UserBaseServicer.InlineWriterCallableResult:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.WorkflowContext])

                if servicer._middleware is None:
                    raise RuntimeError(
                        'Reboot middleware was not created; '
                        'are you using this class without Reboot?'
                    )

                metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None

                if __options__ is not None:
                    if __options__.metadata is not None:
                        metadata = __options__.metadata

                if metadata is None:
                    metadata = ()

                headers = IMPORT_reboot_aio_headers.Headers(
                    application_id=context.application_id,
                    state_ref=context._state_ref,
                    caller_id=IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=context.application_id,
                    ),
                    workflow_id=context.workflow_id,
                    # Only set `workflow_iteration` for
                    # `per_iteration` calls so that the
                    # mutation is stored/recovered with
                    # iteration scoping.
                    workflow_iteration=(
                        context.workflow_iteration
                        if idempotency.per_iteration else None
                    ),
                )

                metadata += headers.to_grpc_metadata()

                idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
                with context.idempotently(
                    state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                    state_ref=context._state_ref,
                    service_name=None,  # Indicates an inline writer.
                    method=None,  # Indicates an inline writer.
                    mutation=True,
                    request=None,  # Indicates an inline writer.
                    metadata=metadata,
                    idempotency=idempotency,
                    aborted_type=None,  # Indicates an inline writer.
                ) as idempotency_key:

                    if any(t[0] == IMPORT_reboot_aio_headers.IDEMPOTENCY_KEY_HEADER for t in metadata):
                        raise ValueError(
                            f"Do not set '{IMPORT_reboot_aio_headers.IDEMPOTENCY_KEY_HEADER}' metadata yourself"
                        )

                    if idempotency_key is not None:
                        metadata += (
                            (IMPORT_reboot_aio_headers.IDEMPOTENCY_KEY_HEADER, str(idempotency_key)),
                        )

                    with servicer._middleware.use_context(
                        headers=IMPORT_reboot_aio_headers.Headers.from_grpc_metadata(metadata),
                        state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                        method='inline writer',
                        context_type=IMPORT_reboot_aio_contexts.WriterContext,
                    ) as writer_context:
                        # Check if we already have performed this mutation!
                        #
                        # We do this _before_ calling 'transactionally()' because
                        # if this call is for a transaction method _and_ we've
                        # already performed the transaction then we don't want to
                        # become a transaction participant (again) we just want to
                        # return the transaction's response.
                        idempotent_mutation = (
                            await servicer._middleware._state_manager.check_for_idempotent_mutation(
                                writer_context
                            )
                        )

                        if idempotent_mutation is not None:
                            assert len(idempotent_mutation.response) != 0
                            response = IMPORT_google_protobuf_wrappers_pb2.BytesValue()
                            response.ParseFromString(idempotent_mutation.response)
                            result: UserBaseServicer.InlineWriterCallableResult = IMPORT_pickle.loads(response.value)

                            if check_type and not isinstance(result, IMPORT_reboot_aio_workflows._isinstance_type(type_result)):
                                raise TypeError(
                                    f"Stored result of type '{type(result).__name__}' from 'writer' "
                                    f"is not of expected type '{IMPORT_reboot_aio_workflows._format_type(type_result)}'; have you changed "
                                    "the 'type' that you expect after having stored a result?"
                                )

                            return result

                        async with servicer._middleware._state_manager.transactionally(
                            writer_context,
                            servicer._middleware.tasks_dispatcher,
                            aborted_type=None,
                        ) as transaction:
                            async with servicer._middleware._state_manager.writer(
                                writer_context,
                                servicer.__state_type__,
                                servicer._middleware.tasks_dispatcher,
                                # TODO: Decide if we want to do any kind of authorization for inline
                                # writers otherwise passing `None` here is fine.
                                authorize=None,
                                transaction=transaction,
                            ) as (protobuf_state, state_manager_writer):
                                # Serialize the state so we can see if it changed.
                                serialized_state = protobuf_state.SerializeToString(
                                    deterministic=True,
                                )

                                typed_state = UserFromProto(protobuf_state)

                                result = await writer(state=typed_state)

                                UserToProto(typed_state, protobuf_state)

                                if check_type and not isinstance(result, IMPORT_reboot_aio_workflows._isinstance_type(type_result)):
                                    raise TypeError(
                                        f"Result of type '{type(result).__name__}' from 'writer' is "
                                        f"not of expected type '{IMPORT_reboot_aio_workflows._format_type(type_result)}'; "
                                        "did you specify an incorrect 'type'?"
                                    )

                                task: IMPORT_typing.Optional[IMPORT_reboot_aio_tasks.TaskEffect] = context.task

                                assert task is not None, (
                                    "Should always have a task when running a `workflow`"
                                )

                                method_name = f"User.{task.method_name} inline writer"

                                if idempotency.alias is not None:
                                    method_name += " with idempotency alias '" + idempotency.alias + "'"
                                elif idempotency.key is not None:
                                    method_name += " with idempotency key=" + str(idempotency.key)

                                servicer._middleware.maybe_raise_effect_validation_retry(
                                    logger=logger,
                                    idempotency_manager=context,
                                    method_name=method_name,
                                    validating_effects=validating_effects,
                                    context=context,
                                    checkpoint=checkpoint,
                                )

                                # We don't pass the context to the
                                # writer, so we don't expect there to
                                # be any scheduled tasks!
                                assert len(context._tasks) == 0

                                effects = IMPORT_reboot_aio_state_managers.Effects(
                                    state=(
                                        # Pass `None` if the state hasn't changed!
                                        protobuf_state if serialized_state != protobuf_state.SerializeToString(
                                            deterministic=True,
                                        )
                                        else None
                                    ),
                                    response=IMPORT_google_protobuf_wrappers_pb2.BytesValue(
                                        value=IMPORT_pickle.dumps(result)
                                    ),
                                )

                                await state_manager_writer.complete(effects)

                                return result

        def per_workflow(self, alias: IMPORT_typing.Optional[str] = None):
            return UserBaseServicer.WorkflowState._Idempotently(
                servicer=self._servicer,
                alias=alias,
                how=IMPORT_reboot_aio_workflows.PER_WORKFLOW,
            )

        def per_iteration(self, alias: IMPORT_typing.Optional[str] = None):
            return UserBaseServicer.WorkflowState._Idempotently(
                servicer=self._servicer,
                alias=alias,
                how=IMPORT_reboot_aio_workflows.PER_ITERATION,
            )

        class _Always:
            """Helper class for providing better types for `write` that don't
            require passing `type` or `check_type`."""

            def __init__(
                self,
                *,
                servicer: UserBaseServicer,
            ):
                self._servicer = servicer

            async def read(
                self, context: IMPORT_reboot_aio_contexts.WorkflowContext
            ) -> User.State:
                return await UserBaseServicer.WorkflowState._Idempotently(
                    servicer=self._servicer,
                    alias=None,
                    how=IMPORT_reboot_aio_workflows.ALWAYS,
                ).read(context)

            async def write(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: UserBaseServicer.InlineWriterCallable[UserBaseServicer.InlineWriterCallableResult],
                __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
            ) -> UserBaseServicer.InlineWriterCallableResult:
                return await UserBaseServicer.WorkflowState._Idempotently(
                    servicer=self._servicer,
                    alias=None,
                    how=IMPORT_reboot_aio_workflows.ALWAYS,
                )._write(
                    context,
                    writer,
                    __options__,
                    type_result=type(None),
                    check_type=False,
                )

        def always(self):
            return UserBaseServicer.WorkflowState._Always(
                servicer=self._servicer,
            )

    # For 'loopos.v1.UserMethods.IngestTextMessage'.
    @IMPORT_abc_abstractmethod
    async def _IngestTextMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: User.State,
        request: loopos.v1.loopos_pb2.UserIngestTextMessageRequest,
    ) -> loopos.v1.loopos_pb2.UserIngestTextMessageResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.IngestVoiceNote'.
    @IMPORT_abc_abstractmethod
    async def _IngestVoiceNote(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: User.State,
        request: loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest,
    ) -> loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.ListTickets'.
    @IMPORT_abc_abstractmethod
    async def _ListTickets(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> loopos.v1.loopos_pb2.UserListTicketsResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.QueryBrain'.
    @IMPORT_abc_abstractmethod
    async def _QueryBrain(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: loopos.v1.loopos_pb2.UserQueryBrainRequest,
    ) -> loopos.v1.loopos_pb2.UserQueryBrainResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.LiveState'.
    @IMPORT_abc_abstractmethod
    async def _LiveState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> loopos.v1.loopos_pb2.UserLiveStateResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.Create'.
    # Concrete no-op default for `create`.
    # Override in your Servicer to run custom initialization
    # on new User instances.
    async def _Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: User.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        pass



class UserSingletonServicer(UserBaseServicer):

    @property
    def state(self):
        return UserBaseServicer.WorkflowState(
            servicer=self
        )

    # For 'loopos.v1.UserMethods.IngestTextMessage'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def IngestTextMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: User.State,
        request: User.IngestTextMessageRequest,
    ) -> User.IngestTextMessageResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.ingest_text_message(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def ingest_text_message(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: User.State,
        request: User.IngestTextMessageRequest,
    ) -> User.IngestTextMessageResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.IngestVoiceNote'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def IngestVoiceNote(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: User.State,
        request: User.IngestVoiceNoteRequest,
    ) -> User.IngestVoiceNoteResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.ingest_voice_note(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def ingest_voice_note(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: User.State,
        request: User.IngestVoiceNoteRequest,
    ) -> User.IngestVoiceNoteResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.ListTickets'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def ListTickets(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
    ) -> User.ListTicketsResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.list_tickets(
            context,
            state,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def list_tickets(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
    ) -> User.ListTicketsResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.QueryBrain'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def QueryBrain(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: User.QueryBrainRequest,
    ) -> User.QueryBrainResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.query_brain(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def query_brain(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: User.QueryBrainRequest,
    ) -> User.QueryBrainResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.LiveState'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def LiveState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
    ) -> User.LiveStateResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.live_state(
            context,
            state,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def live_state(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
    ) -> User.LiveStateResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.Create'.
    # Concrete no-op default for `create`.
    # Override in your Servicer to run custom initialization
    # on new User instances.
    async def Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: User.State,
    ) -> None:
        pass

    async def create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: loopos.v1.loopos_pb2.User,
    ) -> None:
        pass


    # For 'loopos.v1.UserMethods.IngestTextMessage'.
    async def _IngestTextMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: User.State,
        request: loopos.v1.loopos_pb2.UserIngestTextMessageRequest,
    ) -> loopos.v1.loopos_pb2.UserIngestTextMessageResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.User('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.IngestTextMessage()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = UserIngestTextMessageRequestFromProto(request)
            return UserIngestTextMessageResponseToProto(
                await self.IngestTextMessage(
                    context,
                    state,
                    typed_request
                )
            )


    # For 'loopos.v1.UserMethods.IngestVoiceNote'.
    async def _IngestVoiceNote(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: User.State,
        request: loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest,
    ) -> loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.User('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.IngestVoiceNote()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = UserIngestVoiceNoteRequestFromProto(request)
            return UserIngestVoiceNoteResponseToProto(
                await self.IngestVoiceNote(
                    context,
                    state,
                    typed_request
                )
            )


    # For 'loopos.v1.UserMethods.ListTickets'.
    async def _ListTickets(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> loopos.v1.loopos_pb2.UserListTicketsResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.User('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.ListTickets()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            response = (
                self.ListTickets(
                    context,
                    state,
                )
            )
            return UserListTicketsResponseToProto(await response)

    # For 'loopos.v1.UserMethods.QueryBrain'.
    async def _QueryBrain(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: loopos.v1.loopos_pb2.UserQueryBrainRequest,
    ) -> loopos.v1.loopos_pb2.UserQueryBrainResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.User('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.QueryBrain()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = UserQueryBrainRequestFromProto(request)
            response = (
                self.QueryBrain(
                    context,
                    state,
                    typed_request,
                )
            )
            return UserQueryBrainResponseToProto(await response)

    # For 'loopos.v1.UserMethods.LiveState'.
    async def _LiveState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> loopos.v1.loopos_pb2.UserLiveStateResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.User('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.LiveState()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            response = (
                self.LiveState(
                    context,
                    state,
                )
            )
            return UserLiveStateResponseToProto(await response)

    # For 'loopos.v1.UserMethods.Create'.
    async def _Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: User.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.User('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.Create()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            return UserCreateResponseToProto(
                await self.Create(
                    context,
                    state,
                )
            )




class UserServicer(UserBaseServicer):

    _state: IMPORT_contextvars.ContextVar[
        IMPORT_typing.Optional[User.State]
    ] = IMPORT_contextvars.ContextVar(
        'Provides access to state for each call, i.e., there may be '
        'multiple readers executing concurrently but each might have '
        'a different `state`',
        default=None,
    )

    # An instance of the derived class for each state.
    _instances: dict[str, UserServicer] = {}

    def _instance(self, state_id: str):
        instances = UserServicer._instances
        instance = instances.get(state_id)
        if instance is None:
            instance = self.__class__()
            instance._middleware = self._middleware
        instances[state_id] = instance
        return instance

    @property
    def state(self) -> User.State:
        state = UserServicer._state.get()
        if state is None:
            raise RuntimeError(
                "`state` property is only relevant within a `Servicer` method"
            )
        return state

    @state.setter
    def state(self, new_state: User.State):
        state = UserServicer._state.get()
        if state is None:
            raise RuntimeError(
                "`state` property is only relevant within a `Servicer` method"
            )
        for field_name, field_value in new_state.model_dump().items():
            setattr(state, field_name, field_value)

    # For 'loopos.v1.UserMethods.IngestTextMessage'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def IngestTextMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        request: User.IngestTextMessageRequest,
    ) -> User.IngestTextMessageResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.ingest_text_message(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def ingest_text_message(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        request: User.IngestTextMessageRequest,
    ) -> User.IngestTextMessageResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.IngestVoiceNote'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def IngestVoiceNote(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        request: User.IngestVoiceNoteRequest,
    ) -> User.IngestVoiceNoteResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.ingest_voice_note(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def ingest_voice_note(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        request: User.IngestVoiceNoteRequest,
    ) -> User.IngestVoiceNoteResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.ListTickets'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def ListTickets(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
    ) -> User.ListTicketsResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.list_tickets(
            context,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def list_tickets(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
    ) -> User.ListTicketsResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.QueryBrain'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def QueryBrain(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: User.QueryBrainRequest,
    ) -> User.QueryBrainResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.query_brain(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def query_brain(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        request: User.QueryBrainRequest,
    ) -> User.QueryBrainResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.LiveState'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def LiveState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
    ) -> User.LiveStateResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.live_state(
            context,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def live_state(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
    ) -> User.LiveStateResponse:
        raise NotImplementedError

    # For 'loopos.v1.UserMethods.Create'.
    # Concrete no-op default for `create`.
    # Override in your Servicer to run custom initialization
    # on new User instances.
    async def Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
    ) -> None:
        return await self.create(
            context,
        )

    async def create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
    ) -> None:
        pass


    # For 'loopos.v1.UserMethods.IngestTextMessage'.
    async def _IngestTextMessage(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: User.State,
        request: loopos.v1.loopos_pb2.UserIngestTextMessageRequest,
    ) -> loopos.v1.loopos_pb2.UserIngestTextMessageResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert UserServicer._state.get() is None
        UserServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"loopos.v1.User('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.IngestTextMessage()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = UserIngestTextMessageRequestFromProto(request)

                return UserIngestTextMessageResponseToProto(
                    await instance.IngestTextMessage(
                        context,
                        typed_request,
                    )
                )
        finally:
            UserServicer._state.set(None)

    # For 'loopos.v1.UserMethods.IngestVoiceNote'.
    async def _IngestVoiceNote(
        self,
        context: IMPORT_reboot_aio_contexts.TransactionContext,
        state: User.State,
        request: loopos.v1.loopos_pb2.UserIngestVoiceNoteRequest,
    ) -> loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert UserServicer._state.get() is None
        UserServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"loopos.v1.User('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.IngestVoiceNote()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = UserIngestVoiceNoteRequestFromProto(request)

                return UserIngestVoiceNoteResponseToProto(
                    await instance.IngestVoiceNote(
                        context,
                        typed_request,
                    )
                )
        finally:
            UserServicer._state.set(None)

    # For 'loopos.v1.UserMethods.ListTickets'.
    async def _ListTickets(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> loopos.v1.loopos_pb2.UserListTicketsResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert UserServicer._state.get() is None
        UserServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"loopos.v1.User('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.ListTickets()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):

                return UserListTicketsResponseToProto(
                    await instance.ListTickets(
                        context,
                    )
                )
        finally:
            UserServicer._state.set(None)

    # For 'loopos.v1.UserMethods.QueryBrain'.
    async def _QueryBrain(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: loopos.v1.loopos_pb2.UserQueryBrainRequest,
    ) -> loopos.v1.loopos_pb2.UserQueryBrainResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert UserServicer._state.get() is None
        UserServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"loopos.v1.User('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.QueryBrain()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = UserQueryBrainRequestFromProto(request)

                return UserQueryBrainResponseToProto(
                    await instance.QueryBrain(
                        context,
                        typed_request,
                    )
                )
        finally:
            UserServicer._state.set(None)

    # For 'loopos.v1.UserMethods.LiveState'.
    async def _LiveState(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: User.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> loopos.v1.loopos_pb2.UserLiveStateResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert UserServicer._state.get() is None
        UserServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"loopos.v1.User('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.LiveState()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):

                return UserLiveStateResponseToProto(
                    await instance.LiveState(
                        context,
                    )
                )
        finally:
            UserServicer._state.set(None)

    # For 'loopos.v1.UserMethods.Create'.
    async def _Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: User.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert UserServicer._state.get() is None
        UserServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"loopos.v1.User('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.Create()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):

                return UserCreateResponseToProto(
                    await instance.Create(
                        context,
                    )
                )
        finally:
            UserServicer._state.set(None)


class OpsTicketBaseServicer(IMPORT_reboot_aio_servicers.Servicer):
    Authorizer: IMPORT_typing.TypeAlias = OpsTicketAuthorizer
    # Node-backed servicers attach a JS bridge object at runtime. We declare it
    # on the base class so mypy accepts accesses when a servicer is typed as
    # `OpsTicketBaseServicer` (e.g. in classmethod workflows).
    _js_servicer_reference: IMPORT_typing.Any

    __servicer__: IMPORT_contextvars.ContextVar[IMPORT_typing.Optional[OpsTicketBaseServicer]] = IMPORT_contextvars.ContextVar(
        'Provides access to a servicer in the current asyncio context. '
        'We need that to be able to do inline writes and reads inside '
        'a workflow',
        default=None,
    )

    __service_names__ = [
        IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
    ]
    __state_type_name__ = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket')
    __state_type__ = loopos.v1.loopos_pb2.OpsTicket
    __file_descriptor__ = loopos.v1.loopos_pb2.DESCRIPTOR

    def __init__(self):
        super().__init__()
        # NOTE: need to hold on to the middleware so we can do inline
        # writes (see 'self.write(...)').
        #
        # Because '_middleware' is not really private this does mean
        # users may do possibly dangerous things, but this is no more
        # likely given they could have already overridden
        # 'create_middleware()'.
        self._middleware: IMPORT_typing.Optional[OpsTicketServicerMiddleware] = None

    def create_middleware(
        self,
        *,
        application_id: IMPORT_reboot_aio_types.ApplicationId,
        server_id: IMPORT_reboot_aio_types.ServerId,
        state_manager: IMPORT_reboot_aio_state_managers.StateManager,
        placement_client: IMPORT_reboot_aio_placement.PlacementClient,
        channel_manager: IMPORT_reboot_aio_internals_channel_manager._ChannelManager,
        tasks_cache: IMPORT_reboot_aio_internals_tasks_cache.TasksCache,
        token_verifier: IMPORT_typing.Optional[IMPORT_reboot_aio_auth_token_verifiers.TokenVerifier],
        effect_validation: IMPORT_reboot_aio_contexts.EffectValidation,
        ready: IMPORT_asyncio.Event,
    ) -> OpsTicketServicerMiddleware:
        self._middleware = OpsTicketServicerMiddleware(
            servicer=self,
            application_id=application_id,
            server_id=server_id,
            state_manager=state_manager,
            placement_client=placement_client,
            channel_manager=channel_manager,
            tasks_cache=tasks_cache,
            token_verifier=token_verifier,
            effect_validation=effect_validation,
            ready=ready,
        )
        return self._middleware

    def authorizer(self) -> IMPORT_typing.Optional[IMPORT_reboot_aio_auth_authorizers.Authorizer | IMPORT_reboot_aio_auth_authorizers.AuthorizerRule]:
        return None

    def token_verifier(self) -> IMPORT_typing.Optional[IMPORT_reboot_aio_auth_token_verifiers.TokenVerifier]:
        return None

    _is_auto_construct = False


    @staticmethod
    def _mcp_tool_names() -> list[str]:
        """Return the MCP tool names this servicer registers."""
        return [
            "ops_ticket_triage",
            "ops_ticket_acknowledge_dispatch",
            "ops_ticket_dispatch_with_escalation",
            "ops_ticket_propose_new_rule",
            "ops_ticket_show_brain_sources",
        ]

    @staticmethod
    def _add_mcp(
        mcp: IMPORT_mcp_server_fastmcp.FastMCP,
        auto_construct_state_type_full_names: list[IMPORT_reboot_aio_types.StateTypeName],
    ) -> None:
        """Register MCP tools and resources for OpsTicket."""

        # Tool for 'loopos.v1.OpsTicketMethods.Triage'.
        @mcp.tool(
            name="ops_ticket_triage",
            title="Triage",
            description="Run triage on this ticket: classify (fast tier), retrieve Brain context, attach SkillArtifact, draft dispatch (strong tier). Severity >= 4 → AWAITING_HUMAN; otherwise auto-authorize.",
        )
        async def ops_ticket_triage_tool(
            ctx: IMPORT_mcp_server_fastmcp.Context,
            ops_ticket_id: str,
        ):
            reboot_context = IMPORT_reboot_mcp_context.get_reboot_context(ctx)
            ref = OpsTicket.ref(ops_ticket_id)
            return await ref.triage(reboot_context)


        # Tool for 'loopos.v1.OpsTicketMethods.AcknowledgeDispatch'.
        @mcp.tool(
            name="ops_ticket_acknowledge_dispatch",
            title="AcknowledgeDispatch",
            description="Operator acknowledgement for a pending dispatch. Marks the dispatch acknowledged; the dispatch_with_escalation workflow polls for this and resolves rather than escalating.",
        )
        async def ops_ticket_acknowledge_dispatch_tool(
            ctx: IMPORT_mcp_server_fastmcp.Context,
            ops_ticket_id: str,
            *,
            request: OpsTicket.AcknowledgeDispatchRequest,
        ):
            reboot_context = IMPORT_reboot_mcp_context.get_reboot_context(ctx)
            ref = OpsTicket.ref(ops_ticket_id)
            return await ref.acknowledge_dispatch(reboot_context, request)


        # Tool for 'loopos.v1.OpsTicketMethods.DispatchWithEscalation'.
        @mcp.tool(
            name="ops_ticket_dispatch_with_escalation",
            title="DispatchWithEscalation",
            description="Durable dispatch flow: mock-WhatsApp the assignee, wait for acknowledge_dispatch, auto-escalate to AWAITING_HUMAN after the configured timeout.",
        )
        async def ops_ticket_dispatch_with_escalation_tool(
            ctx: IMPORT_mcp_server_fastmcp.Context,
            ops_ticket_id: str,
            *,
            request: OpsTicket.DispatchWithEscalationRequest,
        ):
            reboot_context = IMPORT_reboot_mcp_context.get_reboot_context(ctx)
            ref = OpsTicket.ref(ops_ticket_id)
            return await ref.dispatch_with_escalation(reboot_context, request)


        # Tool for 'loopos.v1.OpsTicketMethods.ProposeNewRule'.
        @mcp.tool(
            name="ops_ticket_propose_new_rule",
            title="ProposeNewRule",
            description="Propose a new automation rule based on patterns in this ticket's resolution. Returns a Lightsprint-ready prompt. Does NOT modify automations/rules.py — that's Lightsprint's job.",
        )
        async def ops_ticket_propose_new_rule_tool(
            ctx: IMPORT_mcp_server_fastmcp.Context,
            ops_ticket_id: str,
        ):
            reboot_context = IMPORT_reboot_mcp_context.get_reboot_context(ctx)
            ref = OpsTicket.ref(ops_ticket_id)
            return await ref.propose_new_rule(reboot_context)


        # Tool for 'loopos.v1.OpsTicketMethods.ShowBrainSources'.
        @mcp.tool(
            name="ops_ticket_show_brain_sources",
            title="ShowBrainSources",
            description="Return the Brain sources that triage matched to this ticket: founder voice memo, SOP, historical resolution.",
        )
        async def ops_ticket_show_brain_sources_tool(
            ctx: IMPORT_mcp_server_fastmcp.Context,
            ops_ticket_id: str,
        ):
            reboot_context = IMPORT_reboot_mcp_context.get_reboot_context(ctx)
            ref = OpsTicket.ref(ops_ticket_id)
            return await ref.show_brain_sources(reboot_context)

        pass  # End of _add_mcp.


    def ref(
        self,
        *,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> OpsTicket.WeakReference[OpsTicket.WeakReference._WriterSchedule]:
        context = IMPORT_reboot_aio_contexts.Context.get()

        if context is None:
            raise RuntimeError(
                'Missing asyncio context variable `context`; '
                'are you using this class without Reboot?'
            )

        return OpsTicket.WeakReference(
            # TODO(https://github.com/reboot-dev/mono/issues/3226): add support for calling other applications.
            # For now this always stays within the application that creates the context.
            application_id=None,
            state_id=context._state_ref.id,
            schedule_type=OpsTicket.WeakReference._WriterSchedule,
            # If the user didn't specify a bearer token we may still end up using the app-internal bearer token,
            # but that's decided at the time of the call.
            bearer_token=bearer_token,
            servicer=self,
        )

    class Effects(IMPORT_reboot_aio_state_managers.Effects):
        def __init__(
            self,
            *,
            state: loopos.v1.loopos_pb2.OpsTicket,
            response: IMPORT_typing.Optional[IMPORT_google_protobuf_message.Message] = None,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [loopos.v1.loopos_pb2.OpsTicket])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)

    # For 'loopos.v1.OpsTicketMethods.Create'.
    class CreateEffects(Effects):
        def __init__(
            self,
            *,
            state: loopos.v1.loopos_pb2.OpsTicket,
            response: google.protobuf.empty_pb2.Empty,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [loopos.v1.loopos_pb2.OpsTicket])
            IMPORT_reboot_aio_types.assert_type(response, [google.protobuf.empty_pb2.Empty])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)


    # For 'loopos.v1.OpsTicketMethods.Triage'.
    class TriageEffects(Effects):
        def __init__(
            self,
            *,
            state: loopos.v1.loopos_pb2.OpsTicket,
            response: google.protobuf.empty_pb2.Empty,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [loopos.v1.loopos_pb2.OpsTicket])
            IMPORT_reboot_aio_types.assert_type(response, [google.protobuf.empty_pb2.Empty])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)


    # For 'loopos.v1.OpsTicketMethods.AcknowledgeDispatch'.
    class AcknowledgeDispatchEffects(Effects):
        def __init__(
            self,
            *,
            state: loopos.v1.loopos_pb2.OpsTicket,
            response: google.protobuf.empty_pb2.Empty,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [loopos.v1.loopos_pb2.OpsTicket])
            IMPORT_reboot_aio_types.assert_type(response, [google.protobuf.empty_pb2.Empty])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)



    # For 'loopos.v1.OpsTicketMethods.ProposeNewRule'.
    class ProposeNewRuleEffects(Effects):
        def __init__(
            self,
            *,
            state: loopos.v1.loopos_pb2.OpsTicket,
            response: loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse,
            tasks: IMPORT_typing.Optional[list[IMPORT_reboot_aio_tasks.TaskEffect]] = None,
            _colocated_upserts: IMPORT_typing.Optional[list[tuple[str, IMPORT_typing.Optional[bytes]]]] = None,
        ):
            IMPORT_reboot_aio_types.assert_type(state, [loopos.v1.loopos_pb2.OpsTicket])
            IMPORT_reboot_aio_types.assert_type(response, [loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse])

            super().__init__(state=state, response=response, tasks=tasks, _colocated_upserts=_colocated_upserts)




    InlineWriterCallableResult = IMPORT_typing.TypeVar('InlineWriterCallableResult', covariant=True)

    class InlineWriterCallable(IMPORT_typing.Protocol[InlineWriterCallableResult]):
        async def __call__(
            self,
            state: loopos.v1.loopos_pb2.OpsTicket
        ) -> OpsTicketBaseServicer.InlineWriterCallableResult:
            ...

    class WorkflowState:

        def __init__(
            self,
            servicer,
        ):
            self._servicer = servicer

        async def read(
            self, context: IMPORT_reboot_aio_contexts.WorkflowContext
        ) -> OpsTicket.State:
            """Read the current state within a workflow."""
            return await (
                self.always() if context.within_until()
                else (
                    self.per_iteration() if context.within_loop()
                    else self.per_workflow()
                )
            ).read(context)

        async def write(
            self,
            idempotency_alias: str,
            context: IMPORT_reboot_aio_contexts.WorkflowContext,
            writer: OpsTicketBaseServicer.InlineWriterCallable[OpsTicketBaseServicer.InlineWriterCallableResult],
            __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
            *,
            type: IMPORT_reboot_aio_workflows.Type | IMPORT_reboot_aio_workflows._Unset = IMPORT_reboot_aio_workflows._UNSET,
        ) -> OpsTicketBaseServicer.InlineWriterCallableResult:
            """Perform an "inline write" within a workflow.

            `type=` is optional: when omitted, `writer`'s return
            annotation is used (falling back to `type(None)` if
            there is none).
            """
            type_t, _ = IMPORT_reboot_aio_workflows._resolve_callable_return_type(
                writer, type,
            )
            return await (
                self.per_iteration(idempotency_alias) if context.within_loop()
                else self.per_workflow(idempotency_alias)
            ).write(
                context, writer, __options__, type=type_t
            )

        class _Idempotently:

            def __init__(
                self,
                *,
                servicer: OpsTicketBaseServicer,
                alias: IMPORT_typing.Optional[str],
                how: IMPORT_reboot_aio_workflows.How,
            ):
                self._servicer = servicer
                self._alias = alias
                self._how = how

            async def read(
                self, context: IMPORT_reboot_aio_contexts.WorkflowContext
            ) -> OpsTicket.State:
                """Read the current state within a workflow."""
                return await self._read(
                    self._servicer,
                    context.idempotency(
                        key=IMPORT_reboot_aio_idempotency.make_expiring_idempotency_key(),
                        how=IMPORT_reboot_aio_idempotency.ALWAYS,
                    ) if self._how == IMPORT_reboot_aio_workflows.ALWAYS else context.idempotency(
                        alias=self._alias,
                        how=self._how,
                    ),
                    context,
                )

            @staticmethod
            async def _read(
                servicer: OpsTicketBaseServicer,
                idempotency: IMPORT_reboot_aio_idempotency.Idempotency,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
            ) -> OpsTicket.State:
                """Read the current state within a workflow."""
                IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.WorkflowContext])

                if servicer._middleware is None:
                    raise RuntimeError(
                        'Reboot middleware was not created; '
                        'are you using this class without Reboot?'
                    )

                async def read() -> loopos.v1.loopos_pb2.OpsTicket:
                    assert servicer._middleware is not None
                    return await servicer._middleware._state_manager.read(
                        context, servicer.__state_type__
                    )

                if idempotency.always:
                    return OpsTicketFromProto(await read())

                state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket')

                # Use the idempotency manager to make sure that this
                # reader is being called following the rules.
                with context.idempotently(
                    state_type_name=state_type_name,
                    state_ref=context._state_ref,
                    # Not calling a method so `service_name`,
                    # `method`, `request`, etc are irrelevant.
                    service_name=None,
                    method=None,
                    mutation=False,
                    request=None,
                    metadata=None,
                    idempotency=idempotency,
                    # Only need to pass `aborted_type` for mutations.
                    aborted_type=None,
                ) as idempotency_key:
                    assert idempotency_key is not None
                    protobuf_state = await IMPORT_reboot_aio_workflows.at_least_once(
                        (
                            # TODO: for easier debugging include the
                            # original alias (or generated alias in
                            # the case of `.per_iteration()` w/o an
                            # alias) instead of just
                            # `idempotency_key`.
                            f"inline reader of '{ state_type_name }' ({str(idempotency_key)})",
                            # NOTE: we want this to be `PER_WORKFLOW`
                            # because any per iteration concerns
                            # should have already been taken care of
                            # by caller using `.per_iteration()`.
                            IMPORT_reboot_aio_workflows.PER_WORKFLOW
                        ),
                        context,
                        read,
                        type=loopos.v1.loopos_pb2.OpsTicket,
                    )

                    return OpsTicketFromProto(protobuf_state)

            async def write(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: OpsTicketBaseServicer.InlineWriterCallable[OpsTicketBaseServicer.InlineWriterCallableResult],
                __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
                *,
                type: IMPORT_reboot_aio_workflows.Type | IMPORT_reboot_aio_workflows._Unset = IMPORT_reboot_aio_workflows._UNSET,
                check_type: bool = True,
            ) -> OpsTicketBaseServicer.InlineWriterCallableResult:
                # `type=` is optional: when omitted, `writer`'s
                # return annotation is used (falling back to
                # `type(None)` when there is none).
                type_t, _ = IMPORT_reboot_aio_workflows._resolve_callable_return_type(
                    writer, type,
                )
                return await self._write(
                    context,
                    writer,
                    __options__,
                    type_result=type_t,
                    check_type=check_type,
                )

            async def _write(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: OpsTicketBaseServicer.InlineWriterCallable[OpsTicketBaseServicer.InlineWriterCallableResult],
                __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
                *,
                type_result: IMPORT_reboot_aio_workflows.TypeT,
                check_type: bool,
            ) -> OpsTicketBaseServicer.InlineWriterCallableResult:
                unidempotently = self._how == IMPORT_reboot_aio_workflows.ALWAYS
                idempotency = (
                    context.idempotency(
                        key=IMPORT_reboot_aio_idempotency.make_expiring_idempotency_key(),
                        how=IMPORT_reboot_aio_idempotency.ALWAYS,
                    ) if unidempotently else context.idempotency(
                        alias=self._alias,
                        how=self._how,
                    )
                )

                return await self._write_validating_effects(
                    self._servicer,
                    idempotency,
                    context,
                    writer,
                    __options__,
                    type_result=type_result,
                    check_type=check_type,
                    unidempotently=unidempotently,
                    checkpoint=context.checkpoint(),
                )

            @staticmethod
            @IMPORT_reboot_aio_internals_middleware.maybe_run_function_twice_to_validate_effects
            async def _write_validating_effects(
                validating_effects: bool,
                servicer: OpsTicketBaseServicer,
                idempotency: IMPORT_reboot_aio_idempotency.Idempotency,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: OpsTicketBaseServicer.InlineWriterCallable[OpsTicketBaseServicer.InlineWriterCallableResult],
                __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
                *,
                type_result: IMPORT_reboot_aio_workflows.TypeT,
                check_type: bool,
                unidempotently: bool,
                checkpoint: IMPORT_reboot_aio_idempotency.Checkpoint,
            ) -> OpsTicketBaseServicer.InlineWriterCallableResult:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                IMPORT_reboot_aio_types.assert_type(context, [IMPORT_reboot_aio_contexts.WorkflowContext])

                if servicer._middleware is None:
                    raise RuntimeError(
                        'Reboot middleware was not created; '
                        'are you using this class without Reboot?'
                    )

                metadata: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None

                if __options__ is not None:
                    if __options__.metadata is not None:
                        metadata = __options__.metadata

                if metadata is None:
                    metadata = ()

                headers = IMPORT_reboot_aio_headers.Headers(
                    application_id=context.application_id,
                    state_ref=context._state_ref,
                    caller_id=IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=context.application_id,
                    ),
                    workflow_id=context.workflow_id,
                    # Only set `workflow_iteration` for
                    # `per_iteration` calls so that the
                    # mutation is stored/recovered with
                    # iteration scoping.
                    workflow_iteration=(
                        context.workflow_iteration
                        if idempotency.per_iteration else None
                    ),
                )

                metadata += headers.to_grpc_metadata()

                idempotency_key: IMPORT_typing.Optional[IMPORT_uuid.UUID]
                with context.idempotently(
                    state_type_name=IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                    state_ref=context._state_ref,
                    service_name=None,  # Indicates an inline writer.
                    method=None,  # Indicates an inline writer.
                    mutation=True,
                    request=None,  # Indicates an inline writer.
                    metadata=metadata,
                    idempotency=idempotency,
                    aborted_type=None,  # Indicates an inline writer.
                ) as idempotency_key:

                    if any(t[0] == IMPORT_reboot_aio_headers.IDEMPOTENCY_KEY_HEADER for t in metadata):
                        raise ValueError(
                            f"Do not set '{IMPORT_reboot_aio_headers.IDEMPOTENCY_KEY_HEADER}' metadata yourself"
                        )

                    if idempotency_key is not None:
                        metadata += (
                            (IMPORT_reboot_aio_headers.IDEMPOTENCY_KEY_HEADER, str(idempotency_key)),
                        )

                    with servicer._middleware.use_context(
                        headers=IMPORT_reboot_aio_headers.Headers.from_grpc_metadata(metadata),
                        state_type_name = IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                        method='inline writer',
                        context_type=IMPORT_reboot_aio_contexts.WriterContext,
                    ) as writer_context:
                        # Check if we already have performed this mutation!
                        #
                        # We do this _before_ calling 'transactionally()' because
                        # if this call is for a transaction method _and_ we've
                        # already performed the transaction then we don't want to
                        # become a transaction participant (again) we just want to
                        # return the transaction's response.
                        idempotent_mutation = (
                            await servicer._middleware._state_manager.check_for_idempotent_mutation(
                                writer_context
                            )
                        )

                        if idempotent_mutation is not None:
                            assert len(idempotent_mutation.response) != 0
                            response = IMPORT_google_protobuf_wrappers_pb2.BytesValue()
                            response.ParseFromString(idempotent_mutation.response)
                            result: OpsTicketBaseServicer.InlineWriterCallableResult = IMPORT_pickle.loads(response.value)

                            if check_type and not isinstance(result, IMPORT_reboot_aio_workflows._isinstance_type(type_result)):
                                raise TypeError(
                                    f"Stored result of type '{type(result).__name__}' from 'writer' "
                                    f"is not of expected type '{IMPORT_reboot_aio_workflows._format_type(type_result)}'; have you changed "
                                    "the 'type' that you expect after having stored a result?"
                                )

                            return result

                        async with servicer._middleware._state_manager.transactionally(
                            writer_context,
                            servicer._middleware.tasks_dispatcher,
                            aborted_type=None,
                        ) as transaction:
                            async with servicer._middleware._state_manager.writer(
                                writer_context,
                                servicer.__state_type__,
                                servicer._middleware.tasks_dispatcher,
                                # TODO: Decide if we want to do any kind of authorization for inline
                                # writers otherwise passing `None` here is fine.
                                authorize=None,
                                transaction=transaction,
                            ) as (protobuf_state, state_manager_writer):
                                # Serialize the state so we can see if it changed.
                                serialized_state = protobuf_state.SerializeToString(
                                    deterministic=True,
                                )

                                typed_state = OpsTicketFromProto(protobuf_state)

                                result = await writer(state=typed_state)

                                OpsTicketToProto(typed_state, protobuf_state)

                                if check_type and not isinstance(result, IMPORT_reboot_aio_workflows._isinstance_type(type_result)):
                                    raise TypeError(
                                        f"Result of type '{type(result).__name__}' from 'writer' is "
                                        f"not of expected type '{IMPORT_reboot_aio_workflows._format_type(type_result)}'; "
                                        "did you specify an incorrect 'type'?"
                                    )

                                task: IMPORT_typing.Optional[IMPORT_reboot_aio_tasks.TaskEffect] = context.task

                                assert task is not None, (
                                    "Should always have a task when running a `workflow`"
                                )

                                method_name = f"OpsTicket.{task.method_name} inline writer"

                                if idempotency.alias is not None:
                                    method_name += " with idempotency alias '" + idempotency.alias + "'"
                                elif idempotency.key is not None:
                                    method_name += " with idempotency key=" + str(idempotency.key)

                                servicer._middleware.maybe_raise_effect_validation_retry(
                                    logger=logger,
                                    idempotency_manager=context,
                                    method_name=method_name,
                                    validating_effects=validating_effects,
                                    context=context,
                                    checkpoint=checkpoint,
                                )

                                # We don't pass the context to the
                                # writer, so we don't expect there to
                                # be any scheduled tasks!
                                assert len(context._tasks) == 0

                                effects = IMPORT_reboot_aio_state_managers.Effects(
                                    state=(
                                        # Pass `None` if the state hasn't changed!
                                        protobuf_state if serialized_state != protobuf_state.SerializeToString(
                                            deterministic=True,
                                        )
                                        else None
                                    ),
                                    response=IMPORT_google_protobuf_wrappers_pb2.BytesValue(
                                        value=IMPORT_pickle.dumps(result)
                                    ),
                                )

                                await state_manager_writer.complete(effects)

                                return result

        def per_workflow(self, alias: IMPORT_typing.Optional[str] = None):
            return OpsTicketBaseServicer.WorkflowState._Idempotently(
                servicer=self._servicer,
                alias=alias,
                how=IMPORT_reboot_aio_workflows.PER_WORKFLOW,
            )

        def per_iteration(self, alias: IMPORT_typing.Optional[str] = None):
            return OpsTicketBaseServicer.WorkflowState._Idempotently(
                servicer=self._servicer,
                alias=alias,
                how=IMPORT_reboot_aio_workflows.PER_ITERATION,
            )

        class _Always:
            """Helper class for providing better types for `write` that don't
            require passing `type` or `check_type`."""

            def __init__(
                self,
                *,
                servicer: OpsTicketBaseServicer,
            ):
                self._servicer = servicer

            async def read(
                self, context: IMPORT_reboot_aio_contexts.WorkflowContext
            ) -> OpsTicket.State:
                return await OpsTicketBaseServicer.WorkflowState._Idempotently(
                    servicer=self._servicer,
                    alias=None,
                    how=IMPORT_reboot_aio_workflows.ALWAYS,
                ).read(context)

            async def write(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: OpsTicketBaseServicer.InlineWriterCallable[OpsTicketBaseServicer.InlineWriterCallableResult],
                __options__: IMPORT_reboot_aio_call.Options = IMPORT_reboot_aio_call.Options(),
            ) -> OpsTicketBaseServicer.InlineWriterCallableResult:
                return await OpsTicketBaseServicer.WorkflowState._Idempotently(
                    servicer=self._servicer,
                    alias=None,
                    how=IMPORT_reboot_aio_workflows.ALWAYS,
                )._write(
                    context,
                    writer,
                    __options__,
                    type_result=type(None),
                    check_type=False,
                )

        def always(self):
            return OpsTicketBaseServicer.WorkflowState._Always(
                servicer=self._servicer,
            )

    # For 'loopos.v1.OpsTicketMethods.Create'.
    @IMPORT_abc_abstractmethod
    async def _Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: loopos.v1.loopos_pb2.OpsTicketCreateRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.Triage'.
    @IMPORT_abc_abstractmethod
    async def _Triage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.AcknowledgeDispatch'.
    @IMPORT_abc_abstractmethod
    async def _AcknowledgeDispatch(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.DispatchWithEscalation'.
    @IMPORT_abc_abstractmethod
    async def _DispatchWithEscalation(
        self,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationRequest,
    ) -> loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.ProposeNewRule'.
    @IMPORT_abc_abstractmethod
    async def _ProposeNewRule(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.ShowBrainSources'.
    @IMPORT_abc_abstractmethod
    async def _ShowBrainSources(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: OpsTicket.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse:
        raise NotImplementedError



class OpsTicketSingletonServicer(OpsTicketBaseServicer):

    @property
    def state(self):
        return OpsTicketBaseServicer.WorkflowState(
            servicer=self
        )

    # For 'loopos.v1.OpsTicketMethods.Create'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: OpsTicket.CreateRequest,
    ) -> None:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.create(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: loopos.v1.loopos_pb2.OpsTicket,
        request: OpsTicket.CreateRequest,
    ) -> None:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.Triage'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def Triage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
    ) -> None:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.triage(
            context,
            state,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def triage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: loopos.v1.loopos_pb2.OpsTicket,
    ) -> None:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.AcknowledgeDispatch'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def AcknowledgeDispatch(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: OpsTicket.AcknowledgeDispatchRequest,
    ) -> None:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.acknowledge_dispatch(
            context,
            state,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def acknowledge_dispatch(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: loopos.v1.loopos_pb2.OpsTicket,
        request: OpsTicket.AcknowledgeDispatchRequest,
    ) -> None:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.DispatchWithEscalation'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    @classmethod
    async def DispatchWithEscalation(
        cls,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: OpsTicket.DispatchWithEscalationRequest,
    ) -> OpsTicket.DispatchWithEscalationResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await cls.dispatch_with_escalation(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    @classmethod
    async def dispatch_with_escalation(
        cls,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: OpsTicket.DispatchWithEscalationRequest,
    ) -> OpsTicket.DispatchWithEscalationResponse:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.ProposeNewRule'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def ProposeNewRule(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
    ) -> OpsTicket.ProposeNewRuleResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.propose_new_rule(
            context,
            state,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def propose_new_rule(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: loopos.v1.loopos_pb2.OpsTicket,
    ) -> OpsTicket.ProposeNewRuleResponse:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.ShowBrainSources'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def ShowBrainSources(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: OpsTicket.State,
    ) -> OpsTicket.ShowBrainSourcesResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.show_brain_sources(
            context,
            state,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def show_brain_sources(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: OpsTicket.State,
    ) -> OpsTicket.ShowBrainSourcesResponse:
        raise NotImplementedError


    # For 'loopos.v1.OpsTicketMethods.Create'.
    async def _Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: loopos.v1.loopos_pb2.OpsTicketCreateRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.OpsTicket('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.Create()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = OpsTicketCreateRequestFromProto(request)
            return OpsTicketCreateResponseToProto(
                await self.Create(
                    context,
                    state,
                    typed_request
                )
            )


    # For 'loopos.v1.OpsTicketMethods.Triage'.
    async def _Triage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.OpsTicket('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.Triage()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            return OpsTicketTriageResponseToProto(
                await self.Triage(
                    context,
                    state,
                )
            )


    # For 'loopos.v1.OpsTicketMethods.AcknowledgeDispatch'.
    async def _AcknowledgeDispatch(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.OpsTicket('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.AcknowledgeDispatch()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = OpsTicketAcknowledgeDispatchRequestFromProto(request)
            return OpsTicketAcknowledgeDispatchResponseToProto(
                await self.AcknowledgeDispatch(
                    context,
                    state,
                    typed_request
                )
            )


    # For 'loopos.v1.OpsTicketMethods.DispatchWithEscalation'.
    async def _DispatchWithEscalation(
        self,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationRequest,
    ) -> loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.OpsTicket('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.DispatchWithEscalation()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = OpsTicketDispatchWithEscalationRequestFromProto(request)
            return OpsTicketDispatchWithEscalationResponseToProto(
                await self.DispatchWithEscalation(
                    context,
                    typed_request,
                )
            )


    # For 'loopos.v1.OpsTicketMethods.ProposeNewRule'.
    async def _ProposeNewRule(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.OpsTicket('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.ProposeNewRule()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            return OpsTicketProposeNewRuleResponseToProto(
                await self.ProposeNewRule(
                    context,
                    state,
                )
            )


    # For 'loopos.v1.OpsTicketMethods.ShowBrainSources'.
    async def _ShowBrainSources(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: OpsTicket.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.OpsTicket('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(self)}.ShowBrainSources()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            response = (
                self.ShowBrainSources(
                    context,
                    state,
                )
            )
            return OpsTicketShowBrainSourcesResponseToProto(await response)



class OpsTicketServicer(OpsTicketBaseServicer):

    _state: IMPORT_contextvars.ContextVar[
        IMPORT_typing.Optional[OpsTicket.State]
    ] = IMPORT_contextvars.ContextVar(
        'Provides access to state for each call, i.e., there may be '
        'multiple readers executing concurrently but each might have '
        'a different `state`',
        default=None,
    )

    # An instance of the derived class for each state.
    _instances: dict[str, OpsTicketServicer] = {}

    def _instance(self, state_id: str):
        instances = OpsTicketServicer._instances
        instance = instances.get(state_id)
        if instance is None:
            instance = self.__class__()
            instance._middleware = self._middleware
        instances[state_id] = instance
        return instance

    @property
    def state(self) -> OpsTicket.State:
        state = OpsTicketServicer._state.get()
        if state is None:
            raise RuntimeError(
                "`state` property is only relevant within a `Servicer` method"
            )
        return state

    @state.setter
    def state(self, new_state: OpsTicket.State):
        state = OpsTicketServicer._state.get()
        if state is None:
            raise RuntimeError(
                "`state` property is only relevant within a `Servicer` method"
            )
        for field_name, field_value in new_state.model_dump().items():
            setattr(state, field_name, field_value)

    # For 'loopos.v1.OpsTicketMethods.Create'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: OpsTicket.CreateRequest,
    ) -> None:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.create(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: OpsTicket.CreateRequest,
    ) -> None:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.Triage'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def Triage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
    ) -> None:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.triage(
            context,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def triage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
    ) -> None:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.AcknowledgeDispatch'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def AcknowledgeDispatch(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: OpsTicket.AcknowledgeDispatchRequest,
    ) -> None:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.acknowledge_dispatch(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def acknowledge_dispatch(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        request: OpsTicket.AcknowledgeDispatchRequest,
    ) -> None:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.DispatchWithEscalation'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    @classmethod
    async def DispatchWithEscalation(
        cls,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: OpsTicket.DispatchWithEscalationRequest,
    ) -> OpsTicket.DispatchWithEscalationResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await cls.dispatch_with_escalation(
            context,
            request,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    @classmethod
    async def dispatch_with_escalation(
        cls,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: OpsTicket.DispatchWithEscalationRequest,
    ) -> OpsTicket.DispatchWithEscalationResponse:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.ProposeNewRule'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def ProposeNewRule(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
    ) -> OpsTicket.ProposeNewRuleResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.propose_new_rule(
            context,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def propose_new_rule(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
    ) -> OpsTicket.ProposeNewRuleResponse:
        raise NotImplementedError

    # For 'loopos.v1.OpsTicketMethods.ShowBrainSources'.
    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that new code that
    # doesn't implement it continues to work.
    # TODO: make it abstractmethod when renaming is done.
    async def ShowBrainSources(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
    ) -> OpsTicket.ShowBrainSourcesResponse:
        # During the migration from 'PascalCase' to 'snake_case' method
        # naming in Python servicers, we call the 'snake_case' version
        # by default, so new names will do the correct thing making the
        # code to be backwards compatible for some time and if a servicer
        # overrides the 'PascalCase' version - it will override that
        # method and will just work.
        return await self.show_brain_sources(
            context,
        )

    # To be backwards compatible during the renaming don't make this
    # method to be 'abstractmethod', so that existing code that
    # doesn't implement it continues to work.
    async def show_brain_sources(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
    ) -> OpsTicket.ShowBrainSourcesResponse:
        raise NotImplementedError


    # For 'loopos.v1.OpsTicketMethods.Create'.
    async def _Create(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: loopos.v1.loopos_pb2.OpsTicketCreateRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert OpsTicketServicer._state.get() is None
        OpsTicketServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"loopos.v1.OpsTicket('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.Create()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = OpsTicketCreateRequestFromProto(request)

                return OpsTicketCreateResponseToProto(
                    await instance.Create(
                        context,
                        typed_request,
                    )
                )
        finally:
            OpsTicketServicer._state.set(None)

    # For 'loopos.v1.OpsTicketMethods.Triage'.
    async def _Triage(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> google.protobuf.empty_pb2.Empty:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert OpsTicketServicer._state.get() is None
        OpsTicketServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"loopos.v1.OpsTicket('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.Triage()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):

                return OpsTicketTriageResponseToProto(
                    await instance.Triage(
                        context,
                    )
                )
        finally:
            OpsTicketServicer._state.set(None)

    # For 'loopos.v1.OpsTicketMethods.AcknowledgeDispatch'.
    async def _AcknowledgeDispatch(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: loopos.v1.loopos_pb2.OpsTicketAcknowledgeDispatchRequest,
    ) -> google.protobuf.empty_pb2.Empty:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert OpsTicketServicer._state.get() is None
        OpsTicketServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"loopos.v1.OpsTicket('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.AcknowledgeDispatch()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):
                typed_request = OpsTicketAcknowledgeDispatchRequestFromProto(request)

                return OpsTicketAcknowledgeDispatchResponseToProto(
                    await instance.AcknowledgeDispatch(
                        context,
                        typed_request,
                    )
                )
        finally:
            OpsTicketServicer._state.set(None)

    # For 'loopos.v1.OpsTicketMethods.DispatchWithEscalation'.
    async def _DispatchWithEscalation(
        self,
        context: IMPORT_reboot_aio_contexts.WorkflowContext,
        request: loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationRequest,
    ) -> loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse:
        # Wrap the call to the developer's method in a `span` so that it
        # is traced using its fully-qualified Python name.
        instance = self._instance(context.state_id)
        with IMPORT_reboot_aio_tracing.span(
                state_name=f"loopos.v1.OpsTicket('{context.state_id}')",
                span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.DispatchWithEscalation()",
                level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                python_specific=True,
        ):
            typed_request = OpsTicketDispatchWithEscalationRequestFromProto(request)

            return OpsTicketDispatchWithEscalationResponseToProto(
                await instance.DispatchWithEscalation(
                    context,
                    typed_request,
                )
            )

    # For 'loopos.v1.OpsTicketMethods.ProposeNewRule'.
    async def _ProposeNewRule(
        self,
        context: IMPORT_reboot_aio_contexts.WriterContext,
        state: OpsTicket.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert OpsTicketServicer._state.get() is None
        OpsTicketServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"loopos.v1.OpsTicket('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.ProposeNewRule()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):

                return OpsTicketProposeNewRuleResponseToProto(
                    await instance.ProposeNewRule(
                        context,
                    )
                )
        finally:
            OpsTicketServicer._state.set(None)

    # For 'loopos.v1.OpsTicketMethods.ShowBrainSources'.
    async def _ShowBrainSources(
        self,
        context: IMPORT_reboot_aio_contexts.ReaderContext,
        state: OpsTicket.State,
        request: google.protobuf.empty_pb2.Empty,
    ) -> loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse:
        # We should have an asyncio task and thus context per request,
        # let's confirm this assumption by making sure that
        # `_state is None`.
        assert OpsTicketServicer._state.get() is None
        OpsTicketServicer._state.set(state)
        try:
            # Wrap the call to the developer's method in a `span` so that it
            # is traced using its fully-qualified Python name.
            instance = self._instance(context.state_id)
            with IMPORT_reboot_aio_tracing.span(
                    state_name=f"loopos.v1.OpsTicket('{context.state_id}')",
                    span_name=f"{IMPORT_reboot_aio_tracing.qualified_type_name(instance)}.ShowBrainSources()",
                    level=IMPORT_reboot_aio_tracing.TraceLevel.CUSTOMER,
                    python_specific=True,
            ):

                return OpsTicketShowBrainSourcesResponseToProto(
                    await instance.ShowBrainSources(
                        context,
                    )
                )
        finally:
            OpsTicketServicer._state.set(None)



############################ Clients ############################
# The main developer-facing entrypoints for any Reboot type. Relevant to both
# clients and servicers (who use it to find the right servicer base types, as well
# as often being clients themselves).

# Attach an explicit time time zone to "naive" `datetime` objects. A "naive" `datetime` doesn't have a
# time zone. Such objects are typically interpreted as representing local time, but could be confused
# for objects representing UTC. This helper function disambiguates by explicitly attaching the local
# time zone to `datetime` objects that don't already have an explicit time zone. If the `datetime` object
# is already timezone-aware, we still convert it to our custom `DateTimeWithTimeZone` type.
def ensure_has_timezone(
    *,
    when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
) -> IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone | IMPORT_datetime_timedelta]:
    if isinstance(when, IMPORT_datetime_datetime):
        return IMPORT_reboot_time_DateTimeWithTimeZone.from_datetime(when)
    return when

User_ScheduleTypeVar = IMPORT_typing.TypeVar('User_ScheduleTypeVar', 'User.WeakReference._Schedule', 'User.WeakReference._WriterSchedule')
User_IdempotentlyScheduleTypeVar = IMPORT_typing.TypeVar('User_IdempotentlyScheduleTypeVar', 'User.WeakReference._Schedule', 'User.WeakReference._WriterSchedule')

User_UntilCallableType = IMPORT_typing.TypeVar('User_UntilCallableType')

class UserSingleton:
    Servicer: IMPORT_typing.TypeAlias = UserSingletonServicer


class User:


    Servicer: IMPORT_typing.TypeAlias = UserServicer

    singleton: IMPORT_typing.TypeAlias = UserSingleton

    Effects: IMPORT_typing.TypeAlias = UserBaseServicer.Effects

    Authorizer: IMPORT_typing.TypeAlias = UserAuthorizer

    State: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.User.state)

    IngestTextMessageRequest: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.User.methods['ingest_text_message'].request)
    IngestTextMessageResponse: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.User.methods['ingest_text_message'].response)

    IngestVoiceNoteRequest: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.User.methods['ingest_voice_note'].request)
    IngestVoiceNoteResponse: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.User.methods['ingest_voice_note'].response)


    ListTicketsResponse: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.User.methods['list_tickets'].response)

    QueryBrainRequest: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.User.methods['query_brain'].request)
    QueryBrainResponse: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.User.methods['query_brain'].response)


    LiveStateResponse: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.User.methods['live_state'].response)




    __state_type_name__ = IMPORT_reboot_aio_types.StateTypeName("loopos.v1.User")

    class IngestTextMessageTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, User.IngestTextMessageResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> loopos.v1.loopos_pb2.UserIngestTextMessageResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = loopos.v1.loopos_pb2.UserIngestTextMessageResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'loopos.v1.loopos_pb2.UserIngestTextMessageResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = User.IngestTextMessageAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return UserIngestTextMessageResponseFromProto(response)

            return wait_for_task().__await__()


    class IngestTextMessageAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message | IMPORT_reboot_api.Model]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            if self.is_declared_error(error):
                # If the error is a declared error, we should always
                # store both formats for Pydantic and Protobuf for that
                # error, so users can access the Pydantic version via
                # `aborted.error` and the Protobuf version will be used
                # to send the error over the wire.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Happens when somebody raises an aborted with a Pydantic
                    # error format, usually in the backend method
                    # implementation.

                    self._error = error

                else:
                    # Usually happens on the client side when we are
                    # constructing an aborted from the `AioRpcError`, so
                    # the error will be in Protobuf shape, and we want
                    # to convert it back to Pydantic if possible.
                    self._method_protobuf_error = error

            else:
                # Non declared error should be well known Reboot or gRPC
                # error, which always comes in the Protobuf format, however
                # it might be a case when the server is using new API and
                # client is old, so server might send a declared error from
                # the server perspective, but client doesn't know that it
                # is a declared error now, so we need to gracefully
                # cover that case.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Store both formats so users can access the Pydantic
                    # version via `aborted.error` and the Protobuf version
                    # will be used to send the error over the wire.
                    self._error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()
                    self._method_protobuf_error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()

                    self._message = (
                        "Received a declared error from the server, "
                        "but the client doesn't know about it; "
                        "please update the client to the latest version\n"
                    ) + (
                        f"Original error: {error.model_dump_json()}\n"
                    ) + message if message is not None else ""

                # Store both formats so users can access the error via
                # `aborted.error` and the Protobuf version
                # will be used to send the error over the wire.
                self._error = error
                self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error | MethodProtobufError:
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message | IMPORT_reboot.api.Model) -> bool:
            if isinstance(error, IMPORT_reboot.api.Model):
                return type(error) in IMPORT_api.User.methods['ingest_text_message'].errors
            return False

    class IngestVoiceNoteTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, User.IngestVoiceNoteResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'loopos.v1.loopos_pb2.UserIngestVoiceNoteResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = User.IngestVoiceNoteAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return UserIngestVoiceNoteResponseFromProto(response)

            return wait_for_task().__await__()


    class IngestVoiceNoteAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message | IMPORT_reboot_api.Model]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            if self.is_declared_error(error):
                # If the error is a declared error, we should always
                # store both formats for Pydantic and Protobuf for that
                # error, so users can access the Pydantic version via
                # `aborted.error` and the Protobuf version will be used
                # to send the error over the wire.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Happens when somebody raises an aborted with a Pydantic
                    # error format, usually in the backend method
                    # implementation.

                    self._error = error

                else:
                    # Usually happens on the client side when we are
                    # constructing an aborted from the `AioRpcError`, so
                    # the error will be in Protobuf shape, and we want
                    # to convert it back to Pydantic if possible.
                    self._method_protobuf_error = error

            else:
                # Non declared error should be well known Reboot or gRPC
                # error, which always comes in the Protobuf format, however
                # it might be a case when the server is using new API and
                # client is old, so server might send a declared error from
                # the server perspective, but client doesn't know that it
                # is a declared error now, so we need to gracefully
                # cover that case.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Store both formats so users can access the Pydantic
                    # version via `aborted.error` and the Protobuf version
                    # will be used to send the error over the wire.
                    self._error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()
                    self._method_protobuf_error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()

                    self._message = (
                        "Received a declared error from the server, "
                        "but the client doesn't know about it; "
                        "please update the client to the latest version\n"
                    ) + (
                        f"Original error: {error.model_dump_json()}\n"
                    ) + message if message is not None else ""

                # Store both formats so users can access the error via
                # `aborted.error` and the Protobuf version
                # will be used to send the error over the wire.
                self._error = error
                self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error | MethodProtobufError:
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message | IMPORT_reboot.api.Model) -> bool:
            if isinstance(error, IMPORT_reboot.api.Model):
                return type(error) in IMPORT_api.User.methods['ingest_voice_note'].errors
            return False

    class ListTicketsTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, User.ListTicketsResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> loopos.v1.loopos_pb2.UserListTicketsResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = loopos.v1.loopos_pb2.UserListTicketsResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'loopos.v1.loopos_pb2.UserListTicketsResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = User.ListTicketsAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return UserListTicketsResponseFromProto(response)

            return wait_for_task().__await__()


    class ListTicketsAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message | IMPORT_reboot_api.Model]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            if self.is_declared_error(error):
                # If the error is a declared error, we should always
                # store both formats for Pydantic and Protobuf for that
                # error, so users can access the Pydantic version via
                # `aborted.error` and the Protobuf version will be used
                # to send the error over the wire.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Happens when somebody raises an aborted with a Pydantic
                    # error format, usually in the backend method
                    # implementation.

                    self._error = error

                else:
                    # Usually happens on the client side when we are
                    # constructing an aborted from the `AioRpcError`, so
                    # the error will be in Protobuf shape, and we want
                    # to convert it back to Pydantic if possible.
                    self._method_protobuf_error = error

            else:
                # Non declared error should be well known Reboot or gRPC
                # error, which always comes in the Protobuf format, however
                # it might be a case when the server is using new API and
                # client is old, so server might send a declared error from
                # the server perspective, but client doesn't know that it
                # is a declared error now, so we need to gracefully
                # cover that case.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Store both formats so users can access the Pydantic
                    # version via `aborted.error` and the Protobuf version
                    # will be used to send the error over the wire.
                    self._error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()
                    self._method_protobuf_error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()

                    self._message = (
                        "Received a declared error from the server, "
                        "but the client doesn't know about it; "
                        "please update the client to the latest version\n"
                    ) + (
                        f"Original error: {error.model_dump_json()}\n"
                    ) + message if message is not None else ""

                # Store both formats so users can access the error via
                # `aborted.error` and the Protobuf version
                # will be used to send the error over the wire.
                self._error = error
                self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error | MethodProtobufError:
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message | IMPORT_reboot.api.Model) -> bool:
            if isinstance(error, IMPORT_reboot.api.Model):
                return type(error) in IMPORT_api.User.methods['list_tickets'].errors
            return False

    class QueryBrainTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, User.QueryBrainResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> loopos.v1.loopos_pb2.UserQueryBrainResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = loopos.v1.loopos_pb2.UserQueryBrainResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'loopos.v1.loopos_pb2.UserQueryBrainResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = User.QueryBrainAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return UserQueryBrainResponseFromProto(response)

            return wait_for_task().__await__()


    class QueryBrainAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message | IMPORT_reboot_api.Model]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            if self.is_declared_error(error):
                # If the error is a declared error, we should always
                # store both formats for Pydantic and Protobuf for that
                # error, so users can access the Pydantic version via
                # `aborted.error` and the Protobuf version will be used
                # to send the error over the wire.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Happens when somebody raises an aborted with a Pydantic
                    # error format, usually in the backend method
                    # implementation.

                    self._error = error

                else:
                    # Usually happens on the client side when we are
                    # constructing an aborted from the `AioRpcError`, so
                    # the error will be in Protobuf shape, and we want
                    # to convert it back to Pydantic if possible.
                    self._method_protobuf_error = error

            else:
                # Non declared error should be well known Reboot or gRPC
                # error, which always comes in the Protobuf format, however
                # it might be a case when the server is using new API and
                # client is old, so server might send a declared error from
                # the server perspective, but client doesn't know that it
                # is a declared error now, so we need to gracefully
                # cover that case.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Store both formats so users can access the Pydantic
                    # version via `aborted.error` and the Protobuf version
                    # will be used to send the error over the wire.
                    self._error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()
                    self._method_protobuf_error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()

                    self._message = (
                        "Received a declared error from the server, "
                        "but the client doesn't know about it; "
                        "please update the client to the latest version\n"
                    ) + (
                        f"Original error: {error.model_dump_json()}\n"
                    ) + message if message is not None else ""

                # Store both formats so users can access the error via
                # `aborted.error` and the Protobuf version
                # will be used to send the error over the wire.
                self._error = error
                self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error | MethodProtobufError:
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message | IMPORT_reboot.api.Model) -> bool:
            if isinstance(error, IMPORT_reboot.api.Model):
                return type(error) in IMPORT_api.User.methods['query_brain'].errors
            return False

    class LiveStateTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, User.LiveStateResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> loopos.v1.loopos_pb2.UserLiveStateResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = loopos.v1.loopos_pb2.UserLiveStateResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'loopos.v1.loopos_pb2.UserLiveStateResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = User.LiveStateAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return UserLiveStateResponseFromProto(response)

            return wait_for_task().__await__()


    class LiveStateAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message | IMPORT_reboot_api.Model]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            if self.is_declared_error(error):
                # If the error is a declared error, we should always
                # store both formats for Pydantic and Protobuf for that
                # error, so users can access the Pydantic version via
                # `aborted.error` and the Protobuf version will be used
                # to send the error over the wire.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Happens when somebody raises an aborted with a Pydantic
                    # error format, usually in the backend method
                    # implementation.

                    self._error = error

                else:
                    # Usually happens on the client side when we are
                    # constructing an aborted from the `AioRpcError`, so
                    # the error will be in Protobuf shape, and we want
                    # to convert it back to Pydantic if possible.
                    self._method_protobuf_error = error

            else:
                # Non declared error should be well known Reboot or gRPC
                # error, which always comes in the Protobuf format, however
                # it might be a case when the server is using new API and
                # client is old, so server might send a declared error from
                # the server perspective, but client doesn't know that it
                # is a declared error now, so we need to gracefully
                # cover that case.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Store both formats so users can access the Pydantic
                    # version via `aborted.error` and the Protobuf version
                    # will be used to send the error over the wire.
                    self._error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()
                    self._method_protobuf_error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()

                    self._message = (
                        "Received a declared error from the server, "
                        "but the client doesn't know about it; "
                        "please update the client to the latest version\n"
                    ) + (
                        f"Original error: {error.model_dump_json()}\n"
                    ) + message if message is not None else ""

                # Store both formats so users can access the error via
                # `aborted.error` and the Protobuf version
                # will be used to send the error over the wire.
                self._error = error
                self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error | MethodProtobufError:
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message | IMPORT_reboot.api.Model) -> bool:
            if isinstance(error, IMPORT_reboot.api.Model):
                return type(error) in IMPORT_api.User.methods['live_state'].errors
            return False

    class CreateTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, None]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> google.protobuf.empty_pb2.Empty:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = google.protobuf.empty_pb2.Empty()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'google.protobuf.empty_pb2.Empty'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = User.CreateAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return UserCreateResponseFromProto(response)

            return wait_for_task().__await__()

    CreateEffects: IMPORT_typing.TypeAlias = UserBaseServicer.CreateEffects

    class CreateAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message | IMPORT_reboot_api.Model]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            if self.is_declared_error(error):
                # If the error is a declared error, we should always
                # store both formats for Pydantic and Protobuf for that
                # error, so users can access the Pydantic version via
                # `aborted.error` and the Protobuf version will be used
                # to send the error over the wire.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Happens when somebody raises an aborted with a Pydantic
                    # error format, usually in the backend method
                    # implementation.

                    self._error = error

                else:
                    # Usually happens on the client side when we are
                    # constructing an aborted from the `AioRpcError`, so
                    # the error will be in Protobuf shape, and we want
                    # to convert it back to Pydantic if possible.
                    self._method_protobuf_error = error

            else:
                # Non declared error should be well known Reboot or gRPC
                # error, which always comes in the Protobuf format, however
                # it might be a case when the server is using new API and
                # client is old, so server might send a declared error from
                # the server perspective, but client doesn't know that it
                # is a declared error now, so we need to gracefully
                # cover that case.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Store both formats so users can access the Pydantic
                    # version via `aborted.error` and the Protobuf version
                    # will be used to send the error over the wire.
                    self._error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()
                    self._method_protobuf_error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()

                    self._message = (
                        "Received a declared error from the server, "
                        "but the client doesn't know about it; "
                        "please update the client to the latest version\n"
                    ) + (
                        f"Original error: {error.model_dump_json()}\n"
                    ) + message if message is not None else ""

                # Store both formats so users can access the error via
                # `aborted.error` and the Protobuf version
                # will be used to send the error over the wire.
                self._error = error
                self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error | MethodProtobufError:
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message | IMPORT_reboot.api.Model) -> bool:
            if isinstance(error, IMPORT_reboot.api.Model):
                return type(error) in IMPORT_api.User.methods['create'].errors
            return False


    class WeakReference(IMPORT_typing.Generic[User_ScheduleTypeVar]):

        _schedule_type: type[User_ScheduleTypeVar]

        def __init__(
            self,
            # When application ID is None, refers to a state within the application given by the context.
            application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
            state_id: IMPORT_reboot_aio_types.StateId,
            *,
            schedule_type: type[User_ScheduleTypeVar],
            bearer_token: IMPORT_typing.Optional[str] = None,
            servicer: IMPORT_typing.Optional[UserBaseServicer] = None,
        ):
            self._application_id = application_id
            self._state_ref = IMPORT_reboot_aio_types.StateRef.from_id(
              User.__state_type_name__,
              state_id,
            )
            self._schedule_type = schedule_type
            self._idempotency_manager: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.IdempotencyManager] = None
            self._reader_stub: IMPORT_typing.Optional[UserReaderStub] = None
            self._writer_stub: IMPORT_typing.Optional[UserWriterStub] = None
            self._workflow_stub: IMPORT_typing.Optional[UserWorkflowStub] = None
            self._tasks_stub: IMPORT_typing.Optional[UserTasksStub] = None
            self._bearer_token = bearer_token
            self._servicer = servicer

        @property
        def state_id(self) -> IMPORT_reboot_aio_types.StateId:
            return self._state_ref.id

        def _reader(
            self,
            context: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        ) -> UserReaderStub:
            if self._reader_stub is None:
                self._reader_stub = UserReaderStub(
                    context=context,
                    state_ref=self._state_ref,
                    bearer_token=self._bearer_token,
                )
            assert self._reader_stub is not None
            if self._idempotency_manager is None:
                self._idempotency_manager = context
            elif self._idempotency_manager != context:
                raise IMPORT_reboot_aio_call.MixedContextsError(
                    "This `WeakReference` for `User` with ID "
                    f"'{self.state_id}' has previously been used by a "
                    "different `Context`. That is not allowed. "
                    "Instead create a new `WeakReference` for every `Context` by calling "
                    f"`User.ref('{self.state_id}')`."
                )
            return self._reader_stub

        def _writer(
            self,
            context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        ) -> UserWriterStub:
            if self._writer_stub is None:
                self._writer_stub = UserWriterStub(
                    context=context,
                    state_ref=self._state_ref,
                    bearer_token=self._bearer_token,
                )
            assert self._writer_stub is not None
            if self._idempotency_manager is None:
                self._idempotency_manager = context
            elif self._idempotency_manager != context:
                raise IMPORT_reboot_aio_call.MixedContextsError(
                    "This `WeakReference` for `User` with ID "
                    f"'{self.state_id}' has previously been used by a "
                    "different `Context`. That is not allowed. "
                    "Instead create a new `WeakReference` for every `Context` by calling "
                    f"`User.ref('{self.state_id}')`."
                )
            return self._writer_stub

        def _workflow(
            self,
            context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        ) -> UserWorkflowStub:
            if self._workflow_stub is None:
                self._workflow_stub = UserWorkflowStub(
                    context=context,
                    state_ref=self._state_ref,
                    bearer_token=self._bearer_token,
                )
            assert self._workflow_stub is not None
            if self._idempotency_manager is None:
                self._idempotency_manager = context
            elif self._idempotency_manager != context:
                raise IMPORT_reboot_aio_call.MixedContextsError(
                    "This `WeakReference` for `User` with ID "
                    f"'{self.state_id}' has previously been used by a "
                    "different `Context`. That is not allowed. "
                    "Instead create a new `WeakReference` for every `Context` by calling "
                    f"`User.ref('{self.state_id}')`."
                )
            return self._workflow_stub

        def _tasks(
            self,
            context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        ) -> UserTasksStub:
            if self._tasks_stub is None:
                self._tasks_stub = UserTasksStub(
                    context=context,
                    state_ref=self._state_ref,
                    bearer_token=self._bearer_token,
                )
            assert self._tasks_stub is not None
            if self._idempotency_manager is None:
                self._idempotency_manager = context
            elif self._idempotency_manager != context:
                raise IMPORT_reboot_aio_call.MixedContextsError(
                    "This `WeakReference` for `User` with ID "
                    f"'{self.state_id}' has previously been used by a "
                    "different `Context`. That is not allowed. "
                    "Instead create a new `WeakReference` for every `Context` by calling "
                    f"`User.ref('{self.state_id}')`."
                )
            return self._tasks_stub

        class _Reactively:

            def __init__(
                self,
                *,
                application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
                state_ref: IMPORT_reboot_aio_types.StateRef,
                bearer_token: IMPORT_typing.Optional[str] = None,
            ):
                self._application_id = application_id
                self._state_ref = state_ref
                self._bearer_token = bearer_token


            async def ListTickets( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                # Explicitly-reactive calls only make sense in the context of either...
                # (A) an external client, or...
                # (B) methods that may reasonably run for a long time, which in Reboot means: readers or workflows.
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[User.ListTicketsResponse]:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WorkflowContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __bearer_token__: IMPORT_typing.Optional[str] = None
                __caller_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                if __bearer_token__ is None:
                    __bearer_token__ = __this__._bearer_token
                if __bearer_token__ is None and isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    # Within a Reboot context we do not pass on the caller's bearer token, as that might
                    # have security implications - we cannot simply trust any service we are calling with
                    # the user's credentials. Instead, the developer can rely on the default app-internal
                    # auth, or override that and set an explicit bearer token.
                    #
                    # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
                    # by the developer for the purpose of making these calls. Note that only
                    # `ExternalContext` even has a `bearer_token` field.
                    __bearer_token__ = __context__.bearer_token

                if __metadata__ is None:
                    __metadata__ = ()

                if isinstance(__context__, IMPORT_reboot_aio_contexts.Context):
                    __caller_id__ = IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=__context__.application_id,
                    )
                    if __this__._application_id is None:
                        # Given our context type (inside a Reboot application) we can default to
                        # making the application send traffic to itself.
                        __this__._application_id = __context__.application_id
                elif isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    __caller_id__ = __context__.caller_id

                __headers__ = IMPORT_reboot_aio_headers.Headers(
                    bearer_token=__bearer_token__,
                    state_ref=__this__._state_ref,
                    application_id=__this__._application_id,
                    caller_id=__caller_id__,
                )

                __metadata__ += __headers__.to_grpc_metadata()

                __query_backoff__ = IMPORT_reboot_aio_backoff.Backoff()
                while True:
                    __call__ = None
                    try:
                        async with __context__.channel_manager.get_channel_to_state(
                            IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                            __this__._state_ref,
                        ) as __channel__:

                            __call__ = IMPORT_rbt_v1alpha1.react_pb2_grpc.ReactStub(
                                __channel__
                            ).Query(
                                IMPORT_rbt_v1alpha1.react_pb2.QueryRequest(
                                    method='ListTickets',
                                    request=UserListTicketsRequestToProto(
                                    ).SerializeToString(),
                                ),
                                metadata=__metadata__,
                            )

                            async for __query_response__ in __call__:
                                # Clear the backoff so we don't wait
                                # as long the next time we get
                                # disconnected.
                                __query_backoff__.clear()

                                # The backend may have sent us this query
                                # response only to let us know that a new
                                # idempotency key has been recorded; there may
                                # not be a new response. Python callers don't
                                # (currently) care about such an event, so we
                                # simply ignore it.
                                if not __query_response__.HasField("response"):
                                    continue

                                __response__ = loopos.v1.loopos_pb2.UserListTicketsResponse()
                                __response__.ParseFromString(__query_response__.response)
                                yield UserListTicketsResponseFromProto(__response__)

                    except IMPORT_grpc.aio.AioRpcError as error:
                        # We expect to get disconnected from the server
                        # from time to time, e.g., when it is being
                        # updated, but we don't want that error to
                        # propagate, we just want to retry.
                        if IMPORT_reboot.aio.aborted.is_grpc_retryable_exception(error):
                            logger.debug(
                                "Reactive read to 'loopos.v1.UserMethods.ListTickets' "
                                f"failed with a retryable error: '{error}'; "
                                "will retry..."
                            )
                            await __query_backoff__()
                            continue
                        if error.code() == IMPORT_grpc.StatusCode.ABORTED:
                            # Reconstitute the error that the server threw, if it was a declared error.
                            status = await IMPORT_rpc_status_async.from_call(__call__)
                            if status is not None:
                                raise User.ListTicketsAborted.from_status(
                                    status
                                ) from None
                            raise User.ListTicketsAborted.from_grpc_aio_rpc_error(
                                error
                            ) from None

                        raise

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            list_tickets = ListTickets
            @IMPORT_typing.overload
            async def QueryBrain(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: User.QueryBrainRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[User.QueryBrainResponse]:
                ...

            @IMPORT_typing.overload
            async def QueryBrain(
                __this__,
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                query: str | Unset = UNSET,
                property_id: str | Unset = UNSET,
            ) -> IMPORT_typing.AsyncIterator[User.QueryBrainResponse]:
                ...

            async def QueryBrain( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                # Explicitly-reactive calls only make sense in the context of either...
                # (A) an external client, or...
                # (B) methods that may reasonably run for a long time, which in Reboot means: readers or workflows.
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[User.QueryBrainRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                query: str | Unset = UNSET,
                property_id: str | Unset = UNSET,
            ) -> IMPORT_typing.AsyncIterator[User.QueryBrainResponse]:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WorkflowContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.QueryBrainRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.QueryBrainRequest)

                __request__: IMPORT_typing.Optional[User.QueryBrainRequest] = None

                if isinstance(__request_or_options__, User.QueryBrainRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __request__ = __request_or_options__

                    assert query is None
                    assert property_id is None
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = UserQueryBrainRequestFromInputFields(
                        query=query,
                        property_id=property_id,
                    )

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __bearer_token__: IMPORT_typing.Optional[str] = None
                __caller_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                if __bearer_token__ is None:
                    __bearer_token__ = __this__._bearer_token
                if __bearer_token__ is None and isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    # Within a Reboot context we do not pass on the caller's bearer token, as that might
                    # have security implications - we cannot simply trust any service we are calling with
                    # the user's credentials. Instead, the developer can rely on the default app-internal
                    # auth, or override that and set an explicit bearer token.
                    #
                    # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
                    # by the developer for the purpose of making these calls. Note that only
                    # `ExternalContext` even has a `bearer_token` field.
                    __bearer_token__ = __context__.bearer_token

                if __metadata__ is None:
                    __metadata__ = ()

                if isinstance(__context__, IMPORT_reboot_aio_contexts.Context):
                    __caller_id__ = IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=__context__.application_id,
                    )
                    if __this__._application_id is None:
                        # Given our context type (inside a Reboot application) we can default to
                        # making the application send traffic to itself.
                        __this__._application_id = __context__.application_id
                elif isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    __caller_id__ = __context__.caller_id

                __headers__ = IMPORT_reboot_aio_headers.Headers(
                    bearer_token=__bearer_token__,
                    state_ref=__this__._state_ref,
                    application_id=__this__._application_id,
                    caller_id=__caller_id__,
                )

                __metadata__ += __headers__.to_grpc_metadata()

                __query_backoff__ = IMPORT_reboot_aio_backoff.Backoff()
                while True:
                    __call__ = None
                    try:
                        async with __context__.channel_manager.get_channel_to_state(
                            IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                            __this__._state_ref,
                        ) as __channel__:

                            __call__ = IMPORT_rbt_v1alpha1.react_pb2_grpc.ReactStub(
                                __channel__
                            ).Query(
                                IMPORT_rbt_v1alpha1.react_pb2.QueryRequest(
                                    method='QueryBrain',
                                    request=UserQueryBrainRequestToProto(
                                        __request__
                                    ).SerializeToString(),
                                ),
                                metadata=__metadata__,
                            )

                            async for __query_response__ in __call__:
                                # Clear the backoff so we don't wait
                                # as long the next time we get
                                # disconnected.
                                __query_backoff__.clear()

                                # The backend may have sent us this query
                                # response only to let us know that a new
                                # idempotency key has been recorded; there may
                                # not be a new response. Python callers don't
                                # (currently) care about such an event, so we
                                # simply ignore it.
                                if not __query_response__.HasField("response"):
                                    continue

                                __response__ = loopos.v1.loopos_pb2.UserQueryBrainResponse()
                                __response__.ParseFromString(__query_response__.response)
                                yield UserQueryBrainResponseFromProto(__response__)

                    except IMPORT_grpc.aio.AioRpcError as error:
                        # We expect to get disconnected from the server
                        # from time to time, e.g., when it is being
                        # updated, but we don't want that error to
                        # propagate, we just want to retry.
                        if IMPORT_reboot.aio.aborted.is_grpc_retryable_exception(error):
                            logger.debug(
                                "Reactive read to 'loopos.v1.UserMethods.QueryBrain' "
                                f"failed with a retryable error: '{error}'; "
                                "will retry..."
                            )
                            await __query_backoff__()
                            continue
                        if error.code() == IMPORT_grpc.StatusCode.ABORTED:
                            # Reconstitute the error that the server threw, if it was a declared error.
                            status = await IMPORT_rpc_status_async.from_call(__call__)
                            if status is not None:
                                raise User.QueryBrainAborted.from_status(
                                    status
                                ) from None
                            raise User.QueryBrainAborted.from_grpc_aio_rpc_error(
                                error
                            ) from None

                        raise

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            query_brain = QueryBrain

            async def LiveState( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                # Explicitly-reactive calls only make sense in the context of either...
                # (A) an external client, or...
                # (B) methods that may reasonably run for a long time, which in Reboot means: readers or workflows.
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[User.LiveStateResponse]:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WorkflowContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __bearer_token__: IMPORT_typing.Optional[str] = None
                __caller_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                if __bearer_token__ is None:
                    __bearer_token__ = __this__._bearer_token
                if __bearer_token__ is None and isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    # Within a Reboot context we do not pass on the caller's bearer token, as that might
                    # have security implications - we cannot simply trust any service we are calling with
                    # the user's credentials. Instead, the developer can rely on the default app-internal
                    # auth, or override that and set an explicit bearer token.
                    #
                    # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
                    # by the developer for the purpose of making these calls. Note that only
                    # `ExternalContext` even has a `bearer_token` field.
                    __bearer_token__ = __context__.bearer_token

                if __metadata__ is None:
                    __metadata__ = ()

                if isinstance(__context__, IMPORT_reboot_aio_contexts.Context):
                    __caller_id__ = IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=__context__.application_id,
                    )
                    if __this__._application_id is None:
                        # Given our context type (inside a Reboot application) we can default to
                        # making the application send traffic to itself.
                        __this__._application_id = __context__.application_id
                elif isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    __caller_id__ = __context__.caller_id

                __headers__ = IMPORT_reboot_aio_headers.Headers(
                    bearer_token=__bearer_token__,
                    state_ref=__this__._state_ref,
                    application_id=__this__._application_id,
                    caller_id=__caller_id__,
                )

                __metadata__ += __headers__.to_grpc_metadata()

                __query_backoff__ = IMPORT_reboot_aio_backoff.Backoff()
                while True:
                    __call__ = None
                    try:
                        async with __context__.channel_manager.get_channel_to_state(
                            IMPORT_reboot_aio_types.StateTypeName('loopos.v1.User'),
                            __this__._state_ref,
                        ) as __channel__:

                            __call__ = IMPORT_rbt_v1alpha1.react_pb2_grpc.ReactStub(
                                __channel__
                            ).Query(
                                IMPORT_rbt_v1alpha1.react_pb2.QueryRequest(
                                    method='LiveState',
                                    request=UserLiveStateRequestToProto(
                                    ).SerializeToString(),
                                ),
                                metadata=__metadata__,
                            )

                            async for __query_response__ in __call__:
                                # Clear the backoff so we don't wait
                                # as long the next time we get
                                # disconnected.
                                __query_backoff__.clear()

                                # The backend may have sent us this query
                                # response only to let us know that a new
                                # idempotency key has been recorded; there may
                                # not be a new response. Python callers don't
                                # (currently) care about such an event, so we
                                # simply ignore it.
                                if not __query_response__.HasField("response"):
                                    continue

                                __response__ = loopos.v1.loopos_pb2.UserLiveStateResponse()
                                __response__.ParseFromString(__query_response__.response)
                                yield UserLiveStateResponseFromProto(__response__)

                    except IMPORT_grpc.aio.AioRpcError as error:
                        # We expect to get disconnected from the server
                        # from time to time, e.g., when it is being
                        # updated, but we don't want that error to
                        # propagate, we just want to retry.
                        if IMPORT_reboot.aio.aborted.is_grpc_retryable_exception(error):
                            logger.debug(
                                "Reactive read to 'loopos.v1.UserMethods.LiveState' "
                                f"failed with a retryable error: '{error}'; "
                                "will retry..."
                            )
                            await __query_backoff__()
                            continue
                        if error.code() == IMPORT_grpc.StatusCode.ABORTED:
                            # Reconstitute the error that the server threw, if it was a declared error.
                            status = await IMPORT_rpc_status_async.from_call(__call__)
                            if status is not None:
                                raise User.LiveStateAborted.from_status(
                                    status
                                ) from None
                            raise User.LiveStateAborted.from_grpc_aio_rpc_error(
                                error
                            ) from None

                        raise

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            live_state = LiveState

        def reactively(self):
            return User.WeakReference._Reactively(
                application_id=self._application_id,
                state_ref=self._state_ref,
                bearer_token=self._bearer_token,
            )

        class _Idempotently(IMPORT_typing.Generic[User_IdempotentlyScheduleTypeVar]):

            _weak_reference: User.WeakReference[User_IdempotentlyScheduleTypeVar]

            def __init__(
                self,
                *,
                weak_reference: User.WeakReference[User_IdempotentlyScheduleTypeVar],
                idempotency: IMPORT_reboot_aio_idempotency.Idempotency,
            ):
                self._weak_reference = weak_reference
                self._idempotency = idempotency

            def schedule(
                self,
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
            ) -> User_IdempotentlyScheduleTypeVar:
                return self._weak_reference._schedule_type(
                    self._weak_reference._application_id,
                    self._weak_reference._tasks,
                    when=when,
                    idempotency=self._idempotency,
                )

            def spawn(
                self,
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
            ) -> User.WeakReference._Spawn:
                return User.WeakReference._Spawn(
                    self._weak_reference._application_id,
                    self._weak_reference._tasks,
                    when=when,
                    idempotency=self._idempotency,
                )

            async def read(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
            ) -> User.State:
                if self._weak_reference._servicer is None:
                    raise RuntimeError(
                        "`read()` is currently only supported within workflows; "
                        "Please reach out and let us know your use case if this "
                        "is important for you!"
                    )

                return await UserBaseServicer.WorkflowState._Idempotently._read(
                    self._weak_reference._servicer,
                    self._idempotency,
                    context,
                )

            async def write(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: UserBaseServicer.InlineWriterCallable[UserBaseServicer.InlineWriterCallableResult],
                *,
                type: IMPORT_reboot_aio_workflows.Type | IMPORT_reboot_aio_workflows._Unset = IMPORT_reboot_aio_workflows._UNSET,
            ) -> UserBaseServicer.InlineWriterCallableResult:
                if self._weak_reference._servicer is None:
                    raise RuntimeError(
                        "`write()` is currently only supported within workflows; "
                        "Please reach out and let us know your use case if this "
                        "is important for you!"
                    )

                # `type=` is optional: when omitted, `writer`'s
                # return annotation is used.
                type_t, _ = IMPORT_reboot_aio_workflows._resolve_callable_return_type(
                    writer, type,
                )

                return await UserBaseServicer.WorkflowState._Idempotently._write_validating_effects(
                    self._weak_reference._servicer,
                    self._idempotency,
                    context,
                    writer,
                    type_result=type_t,
                    check_type=not self._idempotency.always,
                    unidempotently=self._idempotency.always,
                    checkpoint=context.checkpoint(),
                )

            @IMPORT_typing.overload
            async def IngestTextMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: User.IngestTextMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.IngestTextMessageResponse:
                ...

            @IMPORT_typing.overload
            async def IngestTextMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                text: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> User.IngestTextMessageResponse:
                ...

            async def IngestTextMessage( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[User.IngestTextMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                text: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> User.IngestTextMessageResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.IngestTextMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.IngestTextMessageRequest)

                __request__: IMPORT_typing.Optional[User.IngestTextMessageRequest] = None
                if isinstance(__request_or_options__, User.IngestTextMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert property_id is UNSET
                    assert text is UNSET
                    assert sender_user_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = UserIngestTextMessageRequestFromInputFields(
                        property_id=property_id,
                        text=text,
                        sender_user_id=sender_user_id,
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.IngestTextMessage(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            ingest_text_message = IngestTextMessage
            @IMPORT_typing.overload
            async def IngestVoiceNote(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: User.IngestVoiceNoteRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.IngestVoiceNoteResponse:
                ...

            @IMPORT_typing.overload
            async def IngestVoiceNote(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                audio_url: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> User.IngestVoiceNoteResponse:
                ...

            async def IngestVoiceNote( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[User.IngestVoiceNoteRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                audio_url: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> User.IngestVoiceNoteResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.IngestVoiceNoteRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.IngestVoiceNoteRequest)

                __request__: IMPORT_typing.Optional[User.IngestVoiceNoteRequest] = None
                if isinstance(__request_or_options__, User.IngestVoiceNoteRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert property_id is UNSET
                    assert audio_url is UNSET
                    assert sender_user_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = UserIngestVoiceNoteRequestFromInputFields(
                        property_id=property_id,
                        audio_url=audio_url,
                        sender_user_id=sender_user_id,
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.IngestVoiceNote(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            ingest_voice_note = IngestVoiceNote

            async def ListTickets( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.ListTicketsResponse:
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.ListTickets(
                    __context__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            list_tickets = ListTickets
            @IMPORT_typing.overload
            async def QueryBrain(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: User.QueryBrainRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.QueryBrainResponse:
                ...

            @IMPORT_typing.overload
            async def QueryBrain(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                query: str | Unset = UNSET,
                property_id: str | Unset = UNSET,
            ) -> User.QueryBrainResponse:
                ...

            async def QueryBrain( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[User.QueryBrainRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                query: str | Unset = UNSET,
                property_id: str | Unset = UNSET,
            ) -> User.QueryBrainResponse:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.QueryBrainRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.QueryBrainRequest)

                __request__: IMPORT_typing.Optional[User.QueryBrainRequest] = None
                if isinstance(__request_or_options__, User.QueryBrainRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert query is UNSET
                    assert property_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = UserQueryBrainRequestFromInputFields(
                        query=query,
                        property_id=property_id,
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.QueryBrain(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            query_brain = QueryBrain

            async def LiveState( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.LiveStateResponse:
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.LiveState(
                    __context__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            live_state = LiveState

        @IMPORT_typing.overload
        def idempotently(self, alias: IMPORT_typing.Optional[str] = None, *, how: IMPORT_reboot_aio_idempotency.How = IMPORT_reboot_aio_idempotency.PER_WORKFLOW) -> User.WeakReference._Idempotently[User_ScheduleTypeVar]:
            ...

        @IMPORT_typing.overload
        def idempotently(self, *, key: IMPORT_uuid.UUID, how: IMPORT_reboot_aio_idempotency.How = IMPORT_reboot_aio_idempotency.PER_WORKFLOW) -> User.WeakReference._Idempotently[User_ScheduleTypeVar]:
            ...

        def idempotently(
            self,
            alias: IMPORT_typing.Optional[str] = None,
            *,
            key: IMPORT_typing.Optional[IMPORT_uuid.UUID] = None,
            how: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.How] = None,
        ) -> User.WeakReference._Idempotently[User_ScheduleTypeVar]:
            return User.WeakReference._Idempotently(
                weak_reference=self,
                idempotency=IMPORT_reboot_aio_contexts.Context.idempotency(
                    alias=alias,
                    key=key,
                    how=how,
                )
            )

        def per_workflow(self, alias: IMPORT_typing.Optional[str] = None):
            return self.idempotently(alias)

        def per_iteration(self, alias: IMPORT_typing.Optional[str] = None):
            return self.idempotently(
                alias,
                how=IMPORT_reboot_aio_idempotency.PER_ITERATION,
            )

        def always(self):
            return self.idempotently(
                key=IMPORT_reboot_aio_idempotency.make_expiring_idempotency_key(),
                how=IMPORT_reboot_aio_idempotency.ALWAYS,
            )

        class _UntilChangesSatisfies(IMPORT_typing.Generic[User_UntilCallableType]):

            _idempotency_alias: str
            _context: IMPORT_reboot_aio_contexts.WorkflowContext
            _callable: IMPORT_typing.Callable[[], IMPORT_typing.Awaitable[User_UntilCallableType]]
            _type: type[User_UntilCallableType]

            def __init__(
                self,
                *,
                idempotency_alias: str,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                callable: IMPORT_typing.Callable[[], IMPORT_typing.Awaitable[User_UntilCallableType]],
                type: type[User_UntilCallableType],
            ):
                self._idempotency_alias = idempotency_alias
                self._context = context
                self._callable = callable
                self._type = type

            async def changes(self):
                return await IMPORT_reboot_aio_workflows.until_changes(
                    self._idempotency_alias,
                    self._context,
                    self._callable,
                    type=self._type,
                )

            async def satisfies(
                self,
                condition: IMPORT_typing.Callable[[User_UntilCallableType], bool],
            ):

                async def converge():
                    response = await self._callable()
                    if condition(response):
                        return response
                    return False

                return await IMPORT_reboot_aio_workflows.until(
                    self._idempotency_alias,
                    self._context,
                    converge,
                    type=self._type,
                )

        class _Until:

            _weak_reference: User.WeakReference
            _idempotency_alias: str

            def __init__(
                self,
                *,
                weak_reference: User.WeakReference,
                idempotency_alias: str,
            ):
                self._weak_reference = weak_reference
                self._idempotency_alias = idempotency_alias

            def read(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
            ) -> User.WeakReference._UntilChangesSatisfies[User.State]:
                IMPORT_reboot_aio_types.assert_type(
                    context,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                async def callable():
                    return await self._weak_reference.read(context)

                return User.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=self._idempotency_alias,
                    context=context,
                    callable=callable,
                    type=User.State,
                )


            def ListTickets( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.WeakReference._UntilChangesSatisfies[User.ListTicketsResponse]:
                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                async def callable():
                    return await __this__._weak_reference.ListTickets(
                        __context__,
                        __options__,
                    )

                return User.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=__this__._idempotency_alias,
                    context=__context__,
                    callable=callable,
                    type=User.ListTicketsResponse,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            list_tickets = ListTickets
            @IMPORT_typing.overload
            async def QueryBrain(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: User.QueryBrainRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.WeakReference._UntilChangesSatisfies[User.QueryBrainResponse]:
                ...

            @IMPORT_typing.overload
            async def QueryBrain(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                query: str | Unset = UNSET,
                property_id: str | Unset = UNSET,
            ) -> User.WeakReference._UntilChangesSatisfies[User.QueryBrainResponse]:
                ...

            def QueryBrain( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __request_or_options__: IMPORT_typing.Optional[User.QueryBrainRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                query: str | Unset = UNSET,
                property_id: str | Unset = UNSET,
            ) -> User.WeakReference._UntilChangesSatisfies[User.QueryBrainResponse]:
                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                __request__: IMPORT_typing.Optional[User.QueryBrainRequest] = None
                if isinstance(__request_or_options__, User.QueryBrainRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert query is UNSET
                    assert property_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = UserQueryBrainRequestFromInputFields(
                        query=query,
                        property_id=property_id,
                    )

                async def callable():
                    return await __this__._weak_reference.QueryBrain(
                        __context__,
                        __request__,
                        __options__,
                    )

                return User.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=__this__._idempotency_alias,
                    context=__context__,
                    callable=callable,
                    type=User.QueryBrainResponse,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            query_brain = QueryBrain

            def LiveState( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.WeakReference._UntilChangesSatisfies[User.LiveStateResponse]:
                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                async def callable():
                    return await __this__._weak_reference.LiveState(
                        __context__,
                        __options__,
                    )

                return User.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=__this__._idempotency_alias,
                    context=__context__,
                    callable=callable,
                    type=User.LiveStateResponse,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            live_state = LiveState

        def until(self, alias: str):
            return User.WeakReference._Until(
                weak_reference=self,
                idempotency_alias=alias,
            )

        def schedule(
            self,
            *,
            when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
        ) -> User_ScheduleTypeVar:
            return self._schedule_type(self._application_id, self._tasks, when=when)

        class _Schedule:

            def __init__(
                self,
                application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
                tasks: IMPORT_typing.Callable[[IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext], UserTasksStub],
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
                idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            ) -> None:
                self._application_id = application_id
                self._tasks = tasks
                self._when = ensure_has_timezone(when=when)
                self._idempotency = idempotency

            # User callable tasks:
            @IMPORT_typing.overload
            async def IngestTextMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: User.IngestTextMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def IngestTextMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                text: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def IngestTextMessage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[User.IngestTextMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                text: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.IngestTextMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.IngestTextMessageRequest)

                __request__: IMPORT_typing.Optional[User.IngestTextMessageRequest] = None
                if isinstance(__request_or_options__, User.IngestTextMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert property_id is UNSET
                    assert text is UNSET
                    assert sender_user_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = UserIngestTextMessageRequestFromInputFields(
                        property_id=property_id,
                        text=text,
                        sender_user_id=sender_user_id,
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).IngestTextMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            ingest_text_message = IngestTextMessage
            @IMPORT_typing.overload
            async def IngestVoiceNote(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: User.IngestVoiceNoteRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def IngestVoiceNote(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                audio_url: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def IngestVoiceNote( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[User.IngestVoiceNoteRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                audio_url: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.IngestVoiceNoteRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.IngestVoiceNoteRequest)

                __request__: IMPORT_typing.Optional[User.IngestVoiceNoteRequest] = None
                if isinstance(__request_or_options__, User.IngestVoiceNoteRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert property_id is UNSET
                    assert audio_url is UNSET
                    assert sender_user_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = UserIngestVoiceNoteRequestFromInputFields(
                        property_id=property_id,
                        audio_url=audio_url,
                        sender_user_id=sender_user_id,
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).IngestVoiceNote(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            ingest_voice_note = IngestVoiceNote

            async def ListTickets( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).ListTickets(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            list_tickets = ListTickets
            @IMPORT_typing.overload
            async def QueryBrain(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: User.QueryBrainRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def QueryBrain(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                query: str | Unset = UNSET,
                property_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def QueryBrain( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[User.QueryBrainRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                query: str | Unset = UNSET,
                property_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.QueryBrainRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.QueryBrainRequest)

                __request__: IMPORT_typing.Optional[User.QueryBrainRequest] = None
                if isinstance(__request_or_options__, User.QueryBrainRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert query is UNSET
                    assert property_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = UserQueryBrainRequestFromInputFields(
                        query=query,
                        property_id=property_id,
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).QueryBrain(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            query_brain = QueryBrain

            async def LiveState( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).LiveState(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            live_state = LiveState

        # A `WriterContext` can not call any methods in `_Schedule` to
        # prevent a writer from doing a `Foo.ref()` and trying to
        # schedule. However, we want to allow a writer to schedule
        # when we are constructing a `WeakReference` from
        # `self.ref()` so instead we return a `_WriterSchedule` to
        # provide type safety that allows a `WriterContext` to
        # schedule (for itself).
        class _WriterSchedule:

            def __init__(
                self,
                application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
                tasks: IMPORT_typing.Callable[[IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext], UserTasksStub],
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
                idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            ) -> None:
                self._tasks = tasks
                self._when = ensure_has_timezone(when=when)
                self._idempotency = idempotency

            # User callable tasks:
            @IMPORT_typing.overload
            async def IngestTextMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: User.IngestTextMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def IngestTextMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                text: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def IngestTextMessage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[User.IngestTextMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                text: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.IngestTextMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.IngestTextMessageRequest)

                __request__: IMPORT_typing.Optional[User.IngestTextMessageRequest] = None
                if isinstance(__request_or_options__, User.IngestTextMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert property_id is UNSET
                    assert text is UNSET
                    assert sender_user_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = UserIngestTextMessageRequestFromInputFields(
                        property_id=property_id,
                        text=text,
                        sender_user_id=sender_user_id,
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await UserServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).IngestTextMessage(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).IngestTextMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            ingest_text_message = IngestTextMessage
            @IMPORT_typing.overload
            async def IngestVoiceNote(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: User.IngestVoiceNoteRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def IngestVoiceNote(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                audio_url: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def IngestVoiceNote( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[User.IngestVoiceNoteRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                audio_url: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.IngestVoiceNoteRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.IngestVoiceNoteRequest)

                __request__: IMPORT_typing.Optional[User.IngestVoiceNoteRequest] = None
                if isinstance(__request_or_options__, User.IngestVoiceNoteRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert property_id is UNSET
                    assert audio_url is UNSET
                    assert sender_user_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = UserIngestVoiceNoteRequestFromInputFields(
                        property_id=property_id,
                        audio_url=audio_url,
                        sender_user_id=sender_user_id,
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await UserServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).IngestVoiceNote(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).IngestVoiceNote(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            ingest_voice_note = IngestVoiceNote

            async def ListTickets( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await UserServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).ListTickets(
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).ListTickets(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            list_tickets = ListTickets
            @IMPORT_typing.overload
            async def QueryBrain(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: User.QueryBrainRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def QueryBrain(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                query: str | Unset = UNSET,
                property_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def QueryBrain( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[User.QueryBrainRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                query: str | Unset = UNSET,
                property_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.QueryBrainRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.QueryBrainRequest)

                __request__: IMPORT_typing.Optional[User.QueryBrainRequest] = None
                if isinstance(__request_or_options__, User.QueryBrainRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert query is UNSET
                    assert property_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = UserQueryBrainRequestFromInputFields(
                        query=query,
                        property_id=property_id,
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await UserServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).QueryBrain(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).QueryBrain(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            query_brain = QueryBrain

            async def LiveState( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await UserServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).LiveState(
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).LiveState(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            live_state = LiveState

        def spawn(
            self,
            *,
            when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
        ) -> User.WeakReference._Spawn:
            # Within a `workflow`, all "bare" `spawn()` calls are
            # syntactic sugar for `per_workflow()`, unless we're
            # within a control loop, in which case they are syntactic
            # sugar for `per_iteration()`.
            context = IMPORT_reboot_aio_contexts.Context.get()
            if context is not None:
                if isinstance(context, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return (
                        self.per_iteration() if context.within_loop()
                        else self.per_workflow()
                    ).spawn(when=when)
                elif isinstance(context, IMPORT_reboot_aio_external.InitializeContext):
                    return self.idempotently().spawn(when=when)

            return User.WeakReference._Spawn(
                self._application_id, self._tasks, when=when
            )

        class _Spawn:

            def __init__(
                self,
                application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
                tasks: IMPORT_typing.Callable[[IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext], UserTasksStub],
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
                idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            ) -> None:
                self._application_id = application_id
                self._tasks = tasks
                self._when = ensure_has_timezone(when=when)
                self._idempotency = idempotency

            # User callable tasks:
            @IMPORT_typing.overload
            async def IngestTextMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: User.IngestTextMessageRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.IngestTextMessageTask:
                ...

            @IMPORT_typing.overload
            async def IngestTextMessage(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                text: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> User.IngestTextMessageTask:
                ...

            async def IngestTextMessage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[User.IngestTextMessageRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                text: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> User.IngestTextMessageTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.IngestTextMessageRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.IngestTextMessageRequest)


                __request__: IMPORT_typing.Optional[User.IngestTextMessageRequest] = None
                if isinstance(__request_or_options__, User.IngestTextMessageRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert property_id is UNSET
                    assert text is UNSET
                    assert sender_user_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = UserIngestTextMessageRequestFromInputFields(
                        property_id=property_id,
                        text=text,
                        sender_user_id=sender_user_id,
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).IngestTextMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return User.IngestTextMessageTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            ingest_text_message = IngestTextMessage
            @IMPORT_typing.overload
            async def IngestVoiceNote(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: User.IngestVoiceNoteRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.IngestVoiceNoteTask:
                ...

            @IMPORT_typing.overload
            async def IngestVoiceNote(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                audio_url: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> User.IngestVoiceNoteTask:
                ...

            async def IngestVoiceNote( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[User.IngestVoiceNoteRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                property_id: str | Unset = UNSET,
                audio_url: str | Unset = UNSET,
                sender_user_id: str | Unset = UNSET,
            ) -> User.IngestVoiceNoteTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.IngestVoiceNoteRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.IngestVoiceNoteRequest)


                __request__: IMPORT_typing.Optional[User.IngestVoiceNoteRequest] = None
                if isinstance(__request_or_options__, User.IngestVoiceNoteRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert property_id is UNSET
                    assert audio_url is UNSET
                    assert sender_user_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = UserIngestVoiceNoteRequestFromInputFields(
                        property_id=property_id,
                        audio_url=audio_url,
                        sender_user_id=sender_user_id,
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).IngestVoiceNote(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return User.IngestVoiceNoteTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            ingest_voice_note = IngestVoiceNote

            async def ListTickets( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.ListTicketsTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).ListTickets(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return User.ListTicketsTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            list_tickets = ListTickets
            @IMPORT_typing.overload
            async def QueryBrain(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: User.QueryBrainRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.QueryBrainTask:
                ...

            @IMPORT_typing.overload
            async def QueryBrain(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                query: str | Unset = UNSET,
                property_id: str | Unset = UNSET,
            ) -> User.QueryBrainTask:
                ...

            async def QueryBrain( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[User.QueryBrainRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                query: str | Unset = UNSET,
                property_id: str | Unset = UNSET,
            ) -> User.QueryBrainTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.QueryBrainRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.QueryBrainRequest)


                __request__: IMPORT_typing.Optional[User.QueryBrainRequest] = None
                if isinstance(__request_or_options__, User.QueryBrainRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert query is UNSET
                    assert property_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = UserQueryBrainRequestFromInputFields(
                        query=query,
                        property_id=property_id,
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).QueryBrain(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return User.QueryBrainTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            query_brain = QueryBrain

            async def LiveState( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> User.LiveStateTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).LiveState(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return User.LiveStateTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            live_state = LiveState

        async def read(
            self,
            context: IMPORT_reboot_aio_contexts.WorkflowContext,
        ) -> User.State:
            return await (
                self.always() if context.within_until()
                else (
                    self.per_iteration() if context.within_loop()
                    else self.per_workflow()
                )
            ).read(context)

        async def write(
            self,
            context: IMPORT_reboot_aio_contexts.WorkflowContext,
            writer: UserBaseServicer.InlineWriterCallable[UserBaseServicer.InlineWriterCallableResult],
            *,
            type: IMPORT_reboot_aio_workflows.Type | IMPORT_reboot_aio_workflows._Unset = IMPORT_reboot_aio_workflows._UNSET,
        ) -> UserBaseServicer.InlineWriterCallableResult:
            """Perform an "inline write" within a workflow.

            `type=` is optional: when omitted, `writer`'s return
            annotation is used.
            """
            # We forward `type=` along to the inner `write` (which
            # also infers if not passed). Pass through whichever
            # form the user supplied -- explicit class or the
            # `_UNSET` sentinel to trigger inference downstream.
            return await (
                self.always() if context.within_until()
                else (
                    self.per_iteration() if context.within_loop()
                    else self.per_workflow()
                )
            ).write(
                context, writer, type=type
            )

        # User specific methods:
        @IMPORT_typing.overload
        async def IngestTextMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: User.IngestTextMessageRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> User.IngestTextMessageResponse:
            ...

        @IMPORT_typing.overload
        async def IngestTextMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            property_id: str | Unset = UNSET,
            text: str | Unset = UNSET,
            sender_user_id: str | Unset = UNSET,
        ) -> User.IngestTextMessageResponse:
            ...

        async def IngestTextMessage( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[User.IngestTextMessageRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            property_id: str | Unset = UNSET,
            text: str | Unset = UNSET,
            sender_user_id: str | Unset = UNSET,
        ) -> User.IngestTextMessageResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.IngestTextMessageRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.IngestTextMessageRequest)

            __request__: IMPORT_typing.Optional[User.IngestTextMessageRequest] = None
            if isinstance(__request_or_options__, User.IngestTextMessageRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                assert property_id is UNSET
                assert text is UNSET
                assert sender_user_id is UNSET

                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = UserIngestTextMessageRequestFromInputFields(
                    property_id=property_id,
                    text=text,
                    sender_user_id=sender_user_id,
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.per_iteration() if __context__.within_loop()
                        else __this__.per_workflow()
                    ).IngestTextMessage(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().IngestTextMessage(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token

            return UserIngestTextMessageResponseFromProto(
                await __this__._workflow(__context__).IngestTextMessage(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        ingest_text_message = IngestTextMessage
        @IMPORT_typing.overload
        async def IngestVoiceNote(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: User.IngestVoiceNoteRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> User.IngestVoiceNoteResponse:
            ...

        @IMPORT_typing.overload
        async def IngestVoiceNote(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            property_id: str | Unset = UNSET,
            audio_url: str | Unset = UNSET,
            sender_user_id: str | Unset = UNSET,
        ) -> User.IngestVoiceNoteResponse:
            ...

        async def IngestVoiceNote( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[User.IngestVoiceNoteRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            property_id: str | Unset = UNSET,
            audio_url: str | Unset = UNSET,
            sender_user_id: str | Unset = UNSET,
        ) -> User.IngestVoiceNoteResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.IngestVoiceNoteRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.IngestVoiceNoteRequest)

            __request__: IMPORT_typing.Optional[User.IngestVoiceNoteRequest] = None
            if isinstance(__request_or_options__, User.IngestVoiceNoteRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                assert property_id is UNSET
                assert audio_url is UNSET
                assert sender_user_id is UNSET

                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = UserIngestVoiceNoteRequestFromInputFields(
                    property_id=property_id,
                    audio_url=audio_url,
                    sender_user_id=sender_user_id,
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.per_iteration() if __context__.within_loop()
                        else __this__.per_workflow()
                    ).IngestVoiceNote(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().IngestVoiceNote(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token

            return UserIngestVoiceNoteResponseFromProto(
                await __this__._workflow(__context__).IngestVoiceNote(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        ingest_voice_note = IngestVoiceNote

        async def ListTickets( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> User.ListTicketsResponse:
            assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            #
            # Unless we are "within until" in which case all "bare"
            # calls are `.always().
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.always() if __context__.within_until()
                        else (
                            __this__.per_iteration() if __context__.within_loop()
                            else __this__.per_workflow()
                        )
                    ).ListTickets(
                        __context__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().ListTickets(
                        __context__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token
            return UserListTicketsResponseFromProto(
                await __this__._reader(__context__).ListTickets(
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                    idempotency=__idempotency__,
                )
            )
        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        list_tickets = ListTickets
        @IMPORT_typing.overload
        async def QueryBrain(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: User.QueryBrainRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> User.QueryBrainResponse:
            ...

        @IMPORT_typing.overload
        async def QueryBrain(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            query: str | Unset = UNSET,
            property_id: str | Unset = UNSET,
        ) -> User.QueryBrainResponse:
            ...

        async def QueryBrain( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[User.QueryBrainRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            query: str | Unset = UNSET,
            property_id: str | Unset = UNSET,
        ) -> User.QueryBrainResponse:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=User.QueryBrainRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=User.QueryBrainRequest)

            __request__: IMPORT_typing.Optional[User.QueryBrainRequest] = None

            if isinstance(__request_or_options__, User.QueryBrainRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                assert query is UNSET
                assert property_id is UNSET

                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = UserQueryBrainRequestFromInputFields(
                    query=query,
                    property_id=property_id,
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            #
            # Unless we are "within until" in which case all "bare"
            # calls are `.always().
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.always() if __context__.within_until()
                        else (
                            __this__.per_iteration() if __context__.within_loop()
                            else __this__.per_workflow()
                        )
                    ).QueryBrain(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().QueryBrain(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token
            return UserQueryBrainResponseFromProto(
                await __this__._reader(__context__).QueryBrain(
                    __request__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                    idempotency=__idempotency__,
                )
            )
        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        query_brain = QueryBrain

        async def LiveState( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> User.LiveStateResponse:
            assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            #
            # Unless we are "within until" in which case all "bare"
            # calls are `.always().
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.always() if __context__.within_until()
                        else (
                            __this__.per_iteration() if __context__.within_loop()
                            else __this__.per_workflow()
                        )
                    ).LiveState(
                        __context__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().LiveState(
                        __context__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token
            return UserLiveStateResponseFromProto(
                await __this__._reader(__context__).LiveState(
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                    idempotency=__idempotency__,
                )
            )
        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        live_state = LiveState

    class _Forall:

        _ids: IMPORT_typing.Iterable[str]

        def __init__(self, ids: IMPORT_typing.Iterable[str]):
            self._ids = ids

        @IMPORT_typing.overload
        async def IngestTextMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: User.IngestTextMessageRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[User.IngestTextMessageResponse]:
            ...

        @IMPORT_typing.overload
        async def IngestTextMessage(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            property_id: str | Unset = UNSET,
            text: str | Unset = UNSET,
            sender_user_id: str | Unset = UNSET,
        ) -> list[User.IngestTextMessageResponse]:
            ...

        async def IngestTextMessage( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[User.IngestTextMessageRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            property_id: str | Unset = UNSET,
            text: str | Unset = UNSET,
            sender_user_id: str | Unset = UNSET,
        ) -> list[User.IngestTextMessageResponse]:
            if isinstance(__request_or_options__, User.IngestTextMessageRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                assert property_id is UNSET
                assert text is UNSET
                assert sender_user_id is UNSET

                return await IMPORT_asyncio.gather(
                    *[
                        User.ref(
                            id
                        ).IngestTextMessage(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        User.ref(
                            id
                        ).IngestTextMessage(
                            __context__,
                            UserIngestTextMessageRequestFromInputFields(
                                property_id=property_id,
                                text=text,
                                sender_user_id=sender_user_id,
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        ingest_text_message = IngestTextMessage
        @IMPORT_typing.overload
        async def IngestVoiceNote(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: User.IngestVoiceNoteRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[User.IngestVoiceNoteResponse]:
            ...

        @IMPORT_typing.overload
        async def IngestVoiceNote(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            property_id: str | Unset = UNSET,
            audio_url: str | Unset = UNSET,
            sender_user_id: str | Unset = UNSET,
        ) -> list[User.IngestVoiceNoteResponse]:
            ...

        async def IngestVoiceNote( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[User.IngestVoiceNoteRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            property_id: str | Unset = UNSET,
            audio_url: str | Unset = UNSET,
            sender_user_id: str | Unset = UNSET,
        ) -> list[User.IngestVoiceNoteResponse]:
            if isinstance(__request_or_options__, User.IngestVoiceNoteRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                assert property_id is UNSET
                assert audio_url is UNSET
                assert sender_user_id is UNSET

                return await IMPORT_asyncio.gather(
                    *[
                        User.ref(
                            id
                        ).IngestVoiceNote(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        User.ref(
                            id
                        ).IngestVoiceNote(
                            __context__,
                            UserIngestVoiceNoteRequestFromInputFields(
                                property_id=property_id,
                                audio_url=audio_url,
                                sender_user_id=sender_user_id,
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        ingest_voice_note = IngestVoiceNote

        async def ListTickets( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[User.ListTicketsResponse]:
            __options__ = __options__ or IMPORT_reboot_aio_call.Options()
            return await IMPORT_asyncio.gather(
                *[
                    User.ref(
                        id
                    ).ListTickets(
                        __context__,
                        __options__,
                    ) for id in __this__._ids
                ]
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        list_tickets = ListTickets
        @IMPORT_typing.overload
        async def QueryBrain(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: User.QueryBrainRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[User.QueryBrainResponse]:
            ...

        @IMPORT_typing.overload
        async def QueryBrain(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            query: str | Unset = UNSET,
            property_id: str | Unset = UNSET,
        ) -> list[User.QueryBrainResponse]:
            ...

        async def QueryBrain( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[User.QueryBrainRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            query: str | Unset = UNSET,
            property_id: str | Unset = UNSET,
        ) -> list[User.QueryBrainResponse]:
            if isinstance(__request_or_options__, User.QueryBrainRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                assert query is UNSET
                assert property_id is UNSET

                return await IMPORT_asyncio.gather(
                    *[
                        User.ref(
                            id
                        ).QueryBrain(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        User.ref(
                            id
                        ).QueryBrain(
                            __context__,
                            UserQueryBrainRequestFromInputFields(
                                query=query,
                                property_id=property_id,
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        query_brain = QueryBrain

        async def LiveState( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[User.LiveStateResponse]:
            __options__ = __options__ or IMPORT_reboot_aio_call.Options()
            return await IMPORT_asyncio.gather(
                *[
                    User.ref(
                        id
                    ).LiveState(
                        __context__,
                        __options__,
                    ) for id in __this__._ids
                ]
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        live_state = LiveState

    @classmethod
    def forall(cls, ids: IMPORT_typing.Iterable[str]) -> User._Forall:
        return User._Forall(ids)

    @classmethod
    def ref(
        cls,
        state_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId] = None,
        *,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> User.WeakReference[User.WeakReference._Schedule] | User.WeakReference[User.WeakReference._WriterSchedule]:
        # We support calling `User.ref()` with
        # no `state_id` __only__ inside a workflow to be able to call an
        # inline writer, inline reader or other method call, since
        # workflow is a `classmethod` and therefor we can't get a
        # reference to outselves as `self.ref()`.
        if state_id is None:
            context = IMPORT_reboot_aio_contexts.Context.get()

            if context is None:
                raise RuntimeError(
                    'Missing asyncio context variable `context`; '
                    'are you using this class without Reboot?'
                )

            if not isinstance(context, IMPORT_reboot_aio_contexts.WorkflowContext):
                raise RuntimeError(
                    '`ref()` called without a `state_id` can only be used within a Workflow.'
                )

            servicer = UserBaseServicer.__servicer__.get()

            if servicer is None:
                raise RuntimeError(
                    'Missing asyncio context variable `servicer`; '
                    'are you using this class without Reboot?'
                )

            return User.WeakReference(
                # TODO(https://github.com/reboot-dev/mono/issues/3226): add support for calling other applications.
                # For now this always stays within the application that creates the context.
                application_id=None,
                state_id=context._state_ref.id,
                schedule_type=User.WeakReference._WriterSchedule,
                # If the user didn't specify a bearer token we may still end up using the app-internal bearer token,
                # but that's decided at the time of the call.
                bearer_token=bearer_token,
                servicer=servicer,
            )

        return User.WeakReference(
            # TODO(https://github.com/reboot-dev/mono/issues/3226): add support for calling other applications.
            # For now this always stays within the application that creates the context.
            application_id=None,
            state_id=state_id,
            schedule_type=User.WeakReference._Schedule,
            bearer_token=bearer_token,
        )


    @classmethod
    async def Create( # type: ignore[misc]
        __cls__,
        __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        __state_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId] = None,
        __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> tuple[User.WeakReference, None]:
        # Within a `workflow`, all "bare" calls are
        # `per_workflow()` calls, unless we're within a control
        # loop, in which case they are syntactic sugar for
        # `per_iteration()`.
        #
        # Unless we are "within until" in which case all "bare"
        # calls are `.always()`.

        assert __state_id__ is None or isinstance(__state_id__, IMPORT_reboot_aio_types.StateId)
        assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
        assert __idempotency__ is None or isinstance(__idempotency__, IMPORT_reboot_aio_idempotency.Idempotency)

        if __idempotency__ is None:
            __args__: tuple[IMPORT_typing.Any, ...] = (
                __context__,
            )
            if __state_id__ is not None:
                # Don't include `state_id` if it is `None`, so that
                # we won't break the positional argument deduction.
                __args__ += (__state_id__,)
            __args__ += (
                __options__,
            )
            if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                return await (
                    __cls__.always() if __context__.within_until()
                    else (
                        __cls__.per_iteration() if __context__.within_loop()
                        else __cls__.per_workflow()
                    )
                ).Create(
                    *__args__
                )
            elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                return await __cls__.idempotently().Create(
                    *__args__
                )

        __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
        __bearer_token__: IMPORT_typing.Optional[str] = None

        if __options__ is not None:
            IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
            if __options__.metadata is not None:
                __metadata__ = __options__.metadata
            if __options__.bearer_token is not None:
                __bearer_token__ = __options__.bearer_token

        if __state_id__ is None:
            if __idempotency__ is None:
                __state_id__ = str(IMPORT_uuid.uuid4())
            else:
                __state_id__ = __context__.generate_idempotent_state_id(
                    state_type_name=__cls__.__state_type_name__,
                    service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.UserMethods'),
                    method='Create',
                    idempotency=__idempotency__,
                )

        __reference__ = User.ref(
            __state_id__, bearer_token=__bearer_token__
        )
        __stub__ = __reference__._writer(__context__)
        return (
            __reference__,
            UserCreateResponseFromProto(
                await __stub__.Create(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            ),
        )

    # Keep the original functions on the client, so old code will
    # continue to work, but use the new 'snake_case' method in
    # the new code.
    create = Create

    @IMPORT_typing.overload
    @classmethod
    def idempotently(cls, alias: IMPORT_typing.Optional[str] = None, *, how: IMPORT_reboot_aio_idempotency.How = IMPORT_reboot_aio_idempotency.PER_WORKFLOW) -> User._ConstructIdempotently:
        ...

    @IMPORT_typing.overload
    @classmethod
    def idempotently(cls, *, key: IMPORT_uuid.UUID, how: IMPORT_reboot_aio_idempotency.How = IMPORT_reboot_aio_idempotency.PER_WORKFLOW) -> User._ConstructIdempotently:
        ...

    @classmethod
    def idempotently(
        cls,
        alias: IMPORT_typing.Optional[str] = None,
        *,
        key: IMPORT_typing.Optional[IMPORT_uuid.UUID] = None,
        how: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.How] = None,
    ) -> User._ConstructIdempotently:
        return User._ConstructIdempotently(
            _idempotency=IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=alias,
                key=key,
                how=how,
            ),
        )

    @classmethod
    def per_workflow(
        cls,
        alias: IMPORT_typing.Optional[str] = None,
    ):
        return cls.idempotently(alias)

    @classmethod
    def per_iteration(
        cls,
        alias: IMPORT_typing.Optional[str] = None,
    ):
        return cls.idempotently(
            alias,
            how=IMPORT_reboot_aio_idempotency.PER_ITERATION,
        )

    @classmethod
    def always(cls):
        return cls.idempotently(
            key=IMPORT_reboot_aio_idempotency.make_expiring_idempotency_key(),
            how=IMPORT_reboot_aio_idempotency.ALWAYS,
        )

    @IMPORT_dataclasses.dataclass(frozen=True)
    class _ConstructIdempotently:

        _idempotency: IMPORT_reboot_aio_idempotency.Idempotency


        async def Create( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __state_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> tuple[User.WeakReference, None]:
            if __state_id__ is not None and not isinstance(__state_id__, IMPORT_reboot_aio_types.StateId):
                raise TypeError(f"Expecting second positional argument to be of type 'str', got '{type(__state_id__).__name__}'")
            if __options__ is not None and not isinstance(__options__, IMPORT_reboot_aio_call.Options):
                raise TypeError(f"Expecting third positional argument to be of type 'reboot.aio.call.Options', got '{type(__options__).__name__}'")
            __args__: tuple[IMPORT_typing.Any, ...]

            if __state_id__ is not None:
                __args__ = (
                    __context__,
                    __state_id__,
                    __options__,
                    __this__._idempotency,
                )
            else:
                __args__ = (
                    __context__,
                    __options__,
                    __this__._idempotency,
                )

            return await User.Create(
                *__args__,
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        create = Create

OpsTicket_ScheduleTypeVar = IMPORT_typing.TypeVar('OpsTicket_ScheduleTypeVar', 'OpsTicket.WeakReference._Schedule', 'OpsTicket.WeakReference._WriterSchedule')
OpsTicket_IdempotentlyScheduleTypeVar = IMPORT_typing.TypeVar('OpsTicket_IdempotentlyScheduleTypeVar', 'OpsTicket.WeakReference._Schedule', 'OpsTicket.WeakReference._WriterSchedule')

OpsTicket_UntilCallableType = IMPORT_typing.TypeVar('OpsTicket_UntilCallableType')

class OpsTicketSingleton:
    Servicer: IMPORT_typing.TypeAlias = OpsTicketSingletonServicer


class OpsTicket:


    Servicer: IMPORT_typing.TypeAlias = OpsTicketServicer

    singleton: IMPORT_typing.TypeAlias = OpsTicketSingleton

    Effects: IMPORT_typing.TypeAlias = OpsTicketBaseServicer.Effects

    Authorizer: IMPORT_typing.TypeAlias = OpsTicketAuthorizer

    State: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.OpsTicket.state)

    CreateRequest: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.OpsTicket.methods['create'].request)





    AcknowledgeDispatchRequest: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.OpsTicket.methods['acknowledge_dispatch'].request)


    DispatchWithEscalationRequest: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.OpsTicket.methods['dispatch_with_escalation'].request)
    DispatchWithEscalationResponse: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.OpsTicket.methods['dispatch_with_escalation'].response)


    ProposeNewRuleResponse: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.OpsTicket.methods['propose_new_rule'].response)


    ShowBrainSourcesResponse: IMPORT_typing.TypeAlias = IMPORT_typing.cast(type, IMPORT_api.OpsTicket.methods['show_brain_sources'].response)

    __state_type_name__ = IMPORT_reboot_aio_types.StateTypeName("loopos.v1.OpsTicket")

    class CreateTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, None]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> google.protobuf.empty_pb2.Empty:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = google.protobuf.empty_pb2.Empty()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'google.protobuf.empty_pb2.Empty'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = OpsTicket.CreateAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return OpsTicketCreateResponseFromProto(response)

            return wait_for_task().__await__()

    CreateEffects: IMPORT_typing.TypeAlias = OpsTicketBaseServicer.CreateEffects

    class CreateAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message | IMPORT_reboot_api.Model]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            if self.is_declared_error(error):
                # If the error is a declared error, we should always
                # store both formats for Pydantic and Protobuf for that
                # error, so users can access the Pydantic version via
                # `aborted.error` and the Protobuf version will be used
                # to send the error over the wire.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Happens when somebody raises an aborted with a Pydantic
                    # error format, usually in the backend method
                    # implementation.

                    self._error = error

                else:
                    # Usually happens on the client side when we are
                    # constructing an aborted from the `AioRpcError`, so
                    # the error will be in Protobuf shape, and we want
                    # to convert it back to Pydantic if possible.
                    self._method_protobuf_error = error

            else:
                # Non declared error should be well known Reboot or gRPC
                # error, which always comes in the Protobuf format, however
                # it might be a case when the server is using new API and
                # client is old, so server might send a declared error from
                # the server perspective, but client doesn't know that it
                # is a declared error now, so we need to gracefully
                # cover that case.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Store both formats so users can access the Pydantic
                    # version via `aborted.error` and the Protobuf version
                    # will be used to send the error over the wire.
                    self._error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()
                    self._method_protobuf_error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()

                    self._message = (
                        "Received a declared error from the server, "
                        "but the client doesn't know about it; "
                        "please update the client to the latest version\n"
                    ) + (
                        f"Original error: {error.model_dump_json()}\n"
                    ) + message if message is not None else ""

                # Store both formats so users can access the error via
                # `aborted.error` and the Protobuf version
                # will be used to send the error over the wire.
                self._error = error
                self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error | MethodProtobufError:
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message | IMPORT_reboot.api.Model) -> bool:
            if isinstance(error, IMPORT_reboot.api.Model):
                return type(error) in IMPORT_api.OpsTicket.methods['create'].errors
            return False

    class TriageTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, None]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> google.protobuf.empty_pb2.Empty:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = google.protobuf.empty_pb2.Empty()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'google.protobuf.empty_pb2.Empty'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = OpsTicket.TriageAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return OpsTicketTriageResponseFromProto(response)

            return wait_for_task().__await__()

    TriageEffects: IMPORT_typing.TypeAlias = OpsTicketBaseServicer.TriageEffects

    class TriageAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message | IMPORT_reboot_api.Model]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            if self.is_declared_error(error):
                # If the error is a declared error, we should always
                # store both formats for Pydantic and Protobuf for that
                # error, so users can access the Pydantic version via
                # `aborted.error` and the Protobuf version will be used
                # to send the error over the wire.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Happens when somebody raises an aborted with a Pydantic
                    # error format, usually in the backend method
                    # implementation.

                    self._error = error

                else:
                    # Usually happens on the client side when we are
                    # constructing an aborted from the `AioRpcError`, so
                    # the error will be in Protobuf shape, and we want
                    # to convert it back to Pydantic if possible.
                    self._method_protobuf_error = error

            else:
                # Non declared error should be well known Reboot or gRPC
                # error, which always comes in the Protobuf format, however
                # it might be a case when the server is using new API and
                # client is old, so server might send a declared error from
                # the server perspective, but client doesn't know that it
                # is a declared error now, so we need to gracefully
                # cover that case.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Store both formats so users can access the Pydantic
                    # version via `aborted.error` and the Protobuf version
                    # will be used to send the error over the wire.
                    self._error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()
                    self._method_protobuf_error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()

                    self._message = (
                        "Received a declared error from the server, "
                        "but the client doesn't know about it; "
                        "please update the client to the latest version\n"
                    ) + (
                        f"Original error: {error.model_dump_json()}\n"
                    ) + message if message is not None else ""

                # Store both formats so users can access the error via
                # `aborted.error` and the Protobuf version
                # will be used to send the error over the wire.
                self._error = error
                self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error | MethodProtobufError:
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message | IMPORT_reboot.api.Model) -> bool:
            if isinstance(error, IMPORT_reboot.api.Model):
                return type(error) in IMPORT_api.OpsTicket.methods['triage'].errors
            return False

    class AcknowledgeDispatchTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, None]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> google.protobuf.empty_pb2.Empty:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = google.protobuf.empty_pb2.Empty()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'google.protobuf.empty_pb2.Empty'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = OpsTicket.AcknowledgeDispatchAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return OpsTicketAcknowledgeDispatchResponseFromProto(response)

            return wait_for_task().__await__()

    AcknowledgeDispatchEffects: IMPORT_typing.TypeAlias = OpsTicketBaseServicer.AcknowledgeDispatchEffects

    class AcknowledgeDispatchAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message | IMPORT_reboot_api.Model]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            if self.is_declared_error(error):
                # If the error is a declared error, we should always
                # store both formats for Pydantic and Protobuf for that
                # error, so users can access the Pydantic version via
                # `aborted.error` and the Protobuf version will be used
                # to send the error over the wire.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Happens when somebody raises an aborted with a Pydantic
                    # error format, usually in the backend method
                    # implementation.

                    self._error = error

                else:
                    # Usually happens on the client side when we are
                    # constructing an aborted from the `AioRpcError`, so
                    # the error will be in Protobuf shape, and we want
                    # to convert it back to Pydantic if possible.
                    self._method_protobuf_error = error

            else:
                # Non declared error should be well known Reboot or gRPC
                # error, which always comes in the Protobuf format, however
                # it might be a case when the server is using new API and
                # client is old, so server might send a declared error from
                # the server perspective, but client doesn't know that it
                # is a declared error now, so we need to gracefully
                # cover that case.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Store both formats so users can access the Pydantic
                    # version via `aborted.error` and the Protobuf version
                    # will be used to send the error over the wire.
                    self._error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()
                    self._method_protobuf_error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()

                    self._message = (
                        "Received a declared error from the server, "
                        "but the client doesn't know about it; "
                        "please update the client to the latest version\n"
                    ) + (
                        f"Original error: {error.model_dump_json()}\n"
                    ) + message if message is not None else ""

                # Store both formats so users can access the error via
                # `aborted.error` and the Protobuf version
                # will be used to send the error over the wire.
                self._error = error
                self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error | MethodProtobufError:
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message | IMPORT_reboot.api.Model) -> bool:
            if isinstance(error, IMPORT_reboot.api.Model):
                return type(error) in IMPORT_api.OpsTicket.methods['acknowledge_dispatch'].errors
            return False

    class DispatchWithEscalationTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, OpsTicket.DispatchWithEscalationResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'loopos.v1.loopos_pb2.OpsTicketDispatchWithEscalationResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = OpsTicket.DispatchWithEscalationAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return OpsTicketDispatchWithEscalationResponseFromProto(response)

            return wait_for_task().__await__()


    class DispatchWithEscalationAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message | IMPORT_reboot_api.Model]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            if self.is_declared_error(error):
                # If the error is a declared error, we should always
                # store both formats for Pydantic and Protobuf for that
                # error, so users can access the Pydantic version via
                # `aborted.error` and the Protobuf version will be used
                # to send the error over the wire.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Happens when somebody raises an aborted with a Pydantic
                    # error format, usually in the backend method
                    # implementation.

                    self._error = error

                else:
                    # Usually happens on the client side when we are
                    # constructing an aborted from the `AioRpcError`, so
                    # the error will be in Protobuf shape, and we want
                    # to convert it back to Pydantic if possible.
                    self._method_protobuf_error = error

            else:
                # Non declared error should be well known Reboot or gRPC
                # error, which always comes in the Protobuf format, however
                # it might be a case when the server is using new API and
                # client is old, so server might send a declared error from
                # the server perspective, but client doesn't know that it
                # is a declared error now, so we need to gracefully
                # cover that case.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Store both formats so users can access the Pydantic
                    # version via `aborted.error` and the Protobuf version
                    # will be used to send the error over the wire.
                    self._error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()
                    self._method_protobuf_error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()

                    self._message = (
                        "Received a declared error from the server, "
                        "but the client doesn't know about it; "
                        "please update the client to the latest version\n"
                    ) + (
                        f"Original error: {error.model_dump_json()}\n"
                    ) + message if message is not None else ""

                # Store both formats so users can access the error via
                # `aborted.error` and the Protobuf version
                # will be used to send the error over the wire.
                self._error = error
                self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error | MethodProtobufError:
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message | IMPORT_reboot.api.Model) -> bool:
            if isinstance(error, IMPORT_reboot.api.Model):
                return type(error) in IMPORT_api.OpsTicket.methods['dispatch_with_escalation'].errors
            return False

    class ProposeNewRuleTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, OpsTicket.ProposeNewRuleResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'loopos.v1.loopos_pb2.OpsTicketProposeNewRuleResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = OpsTicket.ProposeNewRuleAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return OpsTicketProposeNewRuleResponseFromProto(response)

            return wait_for_task().__await__()

    ProposeNewRuleEffects: IMPORT_typing.TypeAlias = OpsTicketBaseServicer.ProposeNewRuleEffects

    class ProposeNewRuleAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message | IMPORT_reboot_api.Model]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            if self.is_declared_error(error):
                # If the error is a declared error, we should always
                # store both formats for Pydantic and Protobuf for that
                # error, so users can access the Pydantic version via
                # `aborted.error` and the Protobuf version will be used
                # to send the error over the wire.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Happens when somebody raises an aborted with a Pydantic
                    # error format, usually in the backend method
                    # implementation.

                    self._error = error

                else:
                    # Usually happens on the client side when we are
                    # constructing an aborted from the `AioRpcError`, so
                    # the error will be in Protobuf shape, and we want
                    # to convert it back to Pydantic if possible.
                    self._method_protobuf_error = error

            else:
                # Non declared error should be well known Reboot or gRPC
                # error, which always comes in the Protobuf format, however
                # it might be a case when the server is using new API and
                # client is old, so server might send a declared error from
                # the server perspective, but client doesn't know that it
                # is a declared error now, so we need to gracefully
                # cover that case.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Store both formats so users can access the Pydantic
                    # version via `aborted.error` and the Protobuf version
                    # will be used to send the error over the wire.
                    self._error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()
                    self._method_protobuf_error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()

                    self._message = (
                        "Received a declared error from the server, "
                        "but the client doesn't know about it; "
                        "please update the client to the latest version\n"
                    ) + (
                        f"Original error: {error.model_dump_json()}\n"
                    ) + message if message is not None else ""

                # Store both formats so users can access the error via
                # `aborted.error` and the Protobuf version
                # will be used to send the error over the wire.
                self._error = error
                self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error | MethodProtobufError:
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message | IMPORT_reboot.api.Model) -> bool:
            if isinstance(error, IMPORT_reboot.api.Model):
                return type(error) in IMPORT_api.OpsTicket.methods['propose_new_rule'].errors
            return False

    class ShowBrainSourcesTask:
        """Represents a scheduled task running for the
        state. Note that this is not a coroutine because we are trying
        to convey the semantics that the task is already running (or
        will soon be).
        """

        @classmethod
        def retrieve(
            cls,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ):
            return cls(context, task_id=task_id)

        def __init__(
            self,
            context: IMPORT_reboot_aio_contexts.Context | IMPORT_reboot_aio_external.ExternalContext,
            *,
            task_id: IMPORT_rbt_v1alpha1.tasks_pb2.TaskId,
        ) -> None:
            # Depending on the context type (inside or outside a Reboot application)
            # we may or may not know the application ID. If we don't know it, then
            # the `ExternalContext.gateway` will determine it.
            #
            # TODO: in the future we expect to support cross-application calls, in
            #       which case the developer may explicitly pass in an application ID
            #       here.
            self._application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId] = None
            if isinstance(context, IMPORT_reboot_aio_contexts.Context):
                self._application_id = context.application_id
            self._channel_manager = context.channel_manager
            self._task_id = task_id

        @property
        def task_id(self) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
            return self._task_id

        def __await__(self) -> IMPORT_typing.Generator[None, None, OpsTicket.ShowBrainSourcesResponse]:
            """Awaits for task to finish and returns its response."""
            async def wait_for_task() -> loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse:
                channel = self._channel_manager.get_channel_to_state(
                    IMPORT_reboot_aio_types.StateTypeName(self._task_id.state_type),
                    IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                )

                stub = IMPORT_rbt_v1alpha1.tasks_pb2_grpc.TasksStub(channel)

                try:
                    call = IMPORT_reboot_aio_stubs.UnaryRetriedCall(
                        call=None,  # `RetriedCall` can create the call itself.
                        stub_method=stub.Wait,
                        method_name="Wait",
                        request=IMPORT_rbt_v1alpha1.tasks_pb2.WaitRequest(task_id=self._task_id),
                        metadata=IMPORT_reboot_aio_headers.Headers(
                            state_ref=IMPORT_reboot_aio_types.StateRef(self._task_id.state_ref),
                            application_id=self._application_id,
                        ).to_grpc_metadata(),
                        aborted_type=IMPORT_reboot.aio.aborted.SystemAborted,
                    )

                    wait_for_task_response = await call
                except IMPORT_reboot.aio.aborted.SystemAborted as error:
                    if error.code == IMPORT_grpc.StatusCode.NOT_FOUND:
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.UnknownTask()
                        ) from None

                    raise
                else:
                    response_or_error: IMPORT_typing.Optional[IMPORT_google_protobuf_any_pb2.Any] = None
                    is_error = False

                    if wait_for_task_response.response_or_error.WhichOneof("response_or_error") == "response":
                        response_or_error = wait_for_task_response.response_or_error.response
                    else:
                        is_error = True
                        response_or_error = wait_for_task_response.response_or_error.error

                    assert response_or_error is not None
                    assert response_or_error.TypeName() != ""

                    response = loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse()

                    if (
                        not is_error and response_or_error.TypeName() != response.DESCRIPTOR.full_name
                    ):
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.InvalidArgument(),
                            message=
                            f"task with UUID {IMPORT_uuid.UUID(bytes=self._task_id.task_uuid)} "
                            f"has a response of type '{response_or_error.TypeName()}' "
                            "but expecting type 'loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse'; "
                            "are you waiting on a task of the correct method?",
                        ) from None

                    if is_error:
                        aborted_type = OpsTicket.ShowBrainSourcesAborted

                        # In Reboot >= 0.40.2 we expect the error to be a `google.rpc.Status`.
                        if response_or_error.Is(IMPORT_google_rpc_status_pb2.Status.DESCRIPTOR):
                            status = IMPORT_google_rpc_status_pb2.Status()
                            response_or_error.Unpack(status)
                            raise aborted_type.from_status(status)

                        # In Reboot < 0.40.2 workflows throwing declared errors behaved poorly;
                        # we don't aim to emulate its behavior. Indicate that we don't know the
                        # reason for the abort.
                        raise IMPORT_reboot.aio.aborted.SystemAborted(
                            IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                        )

                    else:
                        response_or_error.Unpack(response)
                        return OpsTicketShowBrainSourcesResponseFromProto(response)

            return wait_for_task().__await__()


    class ShowBrainSourcesAborted(IMPORT_reboot.aio.aborted.Aborted):


        Error = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]

        METHOD_PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message]] = [
        ]

        PROTOBUF_ERROR_TYPES: list[type[IMPORT_google_protobuf_message.Message | IMPORT_reboot_api.Model]] = (
            METHOD_PROTOBUF_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES +
            IMPORT_reboot.aio.aborted.REBOOT_ERROR_TYPES
        )

        _error: Error

        MethodProtobufError = IMPORT_typing.Union[
            IMPORT_reboot.aio.aborted.GrpcError,
            IMPORT_reboot.aio.aborted.RebootError,
        ]
        _method_protobuf_error: MethodProtobufError

        _code: IMPORT_grpc.StatusCode
        _message: IMPORT_typing.Optional[str]

        def __init__(
            self,
            error:  IMPORT_reboot.aio.aborted.GrpcError,
            *,
            message: IMPORT_typing.Optional[str] = None,
            # Do not set this value when constructing in order to
            # raise. This is only used internally when constructing
            # from aborted calls.
            error_types: IMPORT_typing.Sequence[type[MethodProtobufError]] = (
                METHOD_PROTOBUF_ERROR_TYPES + IMPORT_reboot.aio.aborted.GRPC_ERROR_TYPES
            ),
        ):
            super().__init__()

            self._message = None

            if self.is_declared_error(error):
                # If the error is a declared error, we should always
                # store both formats for Pydantic and Protobuf for that
                # error, so users can access the Pydantic version via
                # `aborted.error` and the Protobuf version will be used
                # to send the error over the wire.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Happens when somebody raises an aborted with a Pydantic
                    # error format, usually in the backend method
                    # implementation.

                    self._error = error

                else:
                    # Usually happens on the client side when we are
                    # constructing an aborted from the `AioRpcError`, so
                    # the error will be in Protobuf shape, and we want
                    # to convert it back to Pydantic if possible.
                    self._method_protobuf_error = error

            else:
                # Non declared error should be well known Reboot or gRPC
                # error, which always comes in the Protobuf format, however
                # it might be a case when the server is using new API and
                # client is old, so server might send a declared error from
                # the server perspective, but client doesn't know that it
                # is a declared error now, so we need to gracefully
                # cover that case.
                if isinstance(error, IMPORT_reboot.api.Model):
                    # Store both formats so users can access the Pydantic
                    # version via `aborted.error` and the Protobuf version
                    # will be used to send the error over the wire.
                    self._error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()
                    self._method_protobuf_error = IMPORT_rbt_v1alpha1.errors_pb2.Unknown()

                    self._message = (
                        "Received a declared error from the server, "
                        "but the client doesn't know about it; "
                        "please update the client to the latest version\n"
                    ) + (
                        f"Original error: {error.model_dump_json()}\n"
                    ) + message if message is not None else ""

                # Store both formats so users can access the error via
                # `aborted.error` and the Protobuf version
                # will be used to send the error over the wire.
                self._error = error
                self._method_protobuf_error = error

            IMPORT_reboot_aio_types.assert_type(self._protobuf_error, error_types)

            code = self.grpc_status_code_from_error(self._protobuf_error)

            if code is None:
                # Must be a Reboot specific or declared method error.
                code = IMPORT_grpc.StatusCode.ABORTED

            self._code = code

            self._message = self._message if self._message is not None else message

        @property
        def error(self) -> Error | MethodProtobufError:
            return self._error

        @property
        def _protobuf_error(self) -> MethodProtobufError:
            return self._method_protobuf_error

        @property
        def code(self) -> IMPORT_grpc.StatusCode:
            return self._code

        @property
        def message(self) -> IMPORT_typing.Optional[str]:
            return self._message

        @classmethod
        def from_status(cls, status: IMPORT_google_rpc_status_pb2.Status):
            error = cls.error_from_google_rpc_status_details(
                status,
                cls.PROTOBUF_ERROR_TYPES,
            )

            message = status.message if len(status.message) > 0 else None

            if error is not None:
                return cls(error, message=message, error_types=cls.PROTOBUF_ERROR_TYPES)

            error = cls.error_from_google_rpc_status_code(status)

            assert error is not None

            # TODO(benh): also consider getting the type names from
            # `status.details` and including that in `message` to make
            # debugging easier.

            return cls(error, message=message)

        @classmethod
        def from_grpc_aio_rpc_error(cls, aio_rpc_error: IMPORT_grpc.aio.AioRpcError):
            return cls(
                cls.error_from_grpc_aio_rpc_error(aio_rpc_error),
                message=aio_rpc_error.details(),
            )

        @classmethod
        def is_declared_error(cls, error: IMPORT_google_protobuf_message.Message | IMPORT_reboot.api.Model) -> bool:
            if isinstance(error, IMPORT_reboot.api.Model):
                return type(error) in IMPORT_api.OpsTicket.methods['show_brain_sources'].errors
            return False


    class WeakReference(IMPORT_typing.Generic[OpsTicket_ScheduleTypeVar]):

        _schedule_type: type[OpsTicket_ScheduleTypeVar]

        def __init__(
            self,
            # When application ID is None, refers to a state within the application given by the context.
            application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
            state_id: IMPORT_reboot_aio_types.StateId,
            *,
            schedule_type: type[OpsTicket_ScheduleTypeVar],
            bearer_token: IMPORT_typing.Optional[str] = None,
            servicer: IMPORT_typing.Optional[OpsTicketBaseServicer] = None,
        ):
            self._application_id = application_id
            self._state_ref = IMPORT_reboot_aio_types.StateRef.from_id(
              OpsTicket.__state_type_name__,
              state_id,
            )
            self._schedule_type = schedule_type
            self._idempotency_manager: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.IdempotencyManager] = None
            self._reader_stub: IMPORT_typing.Optional[OpsTicketReaderStub] = None
            self._writer_stub: IMPORT_typing.Optional[OpsTicketWriterStub] = None
            self._workflow_stub: IMPORT_typing.Optional[OpsTicketWorkflowStub] = None
            self._tasks_stub: IMPORT_typing.Optional[OpsTicketTasksStub] = None
            self._bearer_token = bearer_token
            self._servicer = servicer

        @property
        def state_id(self) -> IMPORT_reboot_aio_types.StateId:
            return self._state_ref.id

        def _reader(
            self,
            context: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        ) -> OpsTicketReaderStub:
            if self._reader_stub is None:
                self._reader_stub = OpsTicketReaderStub(
                    context=context,
                    state_ref=self._state_ref,
                    bearer_token=self._bearer_token,
                )
            assert self._reader_stub is not None
            if self._idempotency_manager is None:
                self._idempotency_manager = context
            elif self._idempotency_manager != context:
                raise IMPORT_reboot_aio_call.MixedContextsError(
                    "This `WeakReference` for `OpsTicket` with ID "
                    f"'{self.state_id}' has previously been used by a "
                    "different `Context`. That is not allowed. "
                    "Instead create a new `WeakReference` for every `Context` by calling "
                    f"`OpsTicket.ref('{self.state_id}')`."
                )
            return self._reader_stub

        def _writer(
            self,
            context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        ) -> OpsTicketWriterStub:
            if self._writer_stub is None:
                self._writer_stub = OpsTicketWriterStub(
                    context=context,
                    state_ref=self._state_ref,
                    bearer_token=self._bearer_token,
                )
            assert self._writer_stub is not None
            if self._idempotency_manager is None:
                self._idempotency_manager = context
            elif self._idempotency_manager != context:
                raise IMPORT_reboot_aio_call.MixedContextsError(
                    "This `WeakReference` for `OpsTicket` with ID "
                    f"'{self.state_id}' has previously been used by a "
                    "different `Context`. That is not allowed. "
                    "Instead create a new `WeakReference` for every `Context` by calling "
                    f"`OpsTicket.ref('{self.state_id}')`."
                )
            return self._writer_stub

        def _workflow(
            self,
            context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        ) -> OpsTicketWorkflowStub:
            if self._workflow_stub is None:
                self._workflow_stub = OpsTicketWorkflowStub(
                    context=context,
                    state_ref=self._state_ref,
                    bearer_token=self._bearer_token,
                )
            assert self._workflow_stub is not None
            if self._idempotency_manager is None:
                self._idempotency_manager = context
            elif self._idempotency_manager != context:
                raise IMPORT_reboot_aio_call.MixedContextsError(
                    "This `WeakReference` for `OpsTicket` with ID "
                    f"'{self.state_id}' has previously been used by a "
                    "different `Context`. That is not allowed. "
                    "Instead create a new `WeakReference` for every `Context` by calling "
                    f"`OpsTicket.ref('{self.state_id}')`."
                )
            return self._workflow_stub

        def _tasks(
            self,
            context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        ) -> OpsTicketTasksStub:
            if self._tasks_stub is None:
                self._tasks_stub = OpsTicketTasksStub(
                    context=context,
                    state_ref=self._state_ref,
                    bearer_token=self._bearer_token,
                )
            assert self._tasks_stub is not None
            if self._idempotency_manager is None:
                self._idempotency_manager = context
            elif self._idempotency_manager != context:
                raise IMPORT_reboot_aio_call.MixedContextsError(
                    "This `WeakReference` for `OpsTicket` with ID "
                    f"'{self.state_id}' has previously been used by a "
                    "different `Context`. That is not allowed. "
                    "Instead create a new `WeakReference` for every `Context` by calling "
                    f"`OpsTicket.ref('{self.state_id}')`."
                )
            return self._tasks_stub

        class _Reactively:

            def __init__(
                self,
                *,
                application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
                state_ref: IMPORT_reboot_aio_types.StateRef,
                bearer_token: IMPORT_typing.Optional[str] = None,
            ):
                self._application_id = application_id
                self._state_ref = state_ref
                self._bearer_token = bearer_token


            async def ShowBrainSources( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                # Explicitly-reactive calls only make sense in the context of either...
                # (A) an external client, or...
                # (B) methods that may reasonably run for a long time, which in Reboot means: readers or workflows.
                __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WorkflowContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_typing.AsyncIterator[OpsTicket.ShowBrainSourcesResponse]:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.ReaderContext, IMPORT_reboot_aio_contexts.WorkflowContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __bearer_token__: IMPORT_typing.Optional[str] = None
                __caller_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_caller_id.CallerID] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                if __bearer_token__ is None:
                    __bearer_token__ = __this__._bearer_token
                if __bearer_token__ is None and isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    # Within a Reboot context we do not pass on the caller's bearer token, as that might
                    # have security implications - we cannot simply trust any service we are calling with
                    # the user's credentials. Instead, the developer can rely on the default app-internal
                    # auth, or override that and set an explicit bearer token.
                    #
                    # In the case of `ExternalContext`, however, its `bearer_token` was set specifically
                    # by the developer for the purpose of making these calls. Note that only
                    # `ExternalContext` even has a `bearer_token` field.
                    __bearer_token__ = __context__.bearer_token

                if __metadata__ is None:
                    __metadata__ = ()

                if isinstance(__context__, IMPORT_reboot_aio_contexts.Context):
                    __caller_id__ = IMPORT_reboot_aio_caller_id.CallerID(
                        application_id=__context__.application_id,
                    )
                    if __this__._application_id is None:
                        # Given our context type (inside a Reboot application) we can default to
                        # making the application send traffic to itself.
                        __this__._application_id = __context__.application_id
                elif isinstance(__context__, IMPORT_reboot_aio_external.ExternalContext):
                    __caller_id__ = __context__.caller_id

                __headers__ = IMPORT_reboot_aio_headers.Headers(
                    bearer_token=__bearer_token__,
                    state_ref=__this__._state_ref,
                    application_id=__this__._application_id,
                    caller_id=__caller_id__,
                )

                __metadata__ += __headers__.to_grpc_metadata()

                __query_backoff__ = IMPORT_reboot_aio_backoff.Backoff()
                while True:
                    __call__ = None
                    try:
                        async with __context__.channel_manager.get_channel_to_state(
                            IMPORT_reboot_aio_types.StateTypeName('loopos.v1.OpsTicket'),
                            __this__._state_ref,
                        ) as __channel__:

                            __call__ = IMPORT_rbt_v1alpha1.react_pb2_grpc.ReactStub(
                                __channel__
                            ).Query(
                                IMPORT_rbt_v1alpha1.react_pb2.QueryRequest(
                                    method='ShowBrainSources',
                                    request=OpsTicketShowBrainSourcesRequestToProto(
                                    ).SerializeToString(),
                                ),
                                metadata=__metadata__,
                            )

                            async for __query_response__ in __call__:
                                # Clear the backoff so we don't wait
                                # as long the next time we get
                                # disconnected.
                                __query_backoff__.clear()

                                # The backend may have sent us this query
                                # response only to let us know that a new
                                # idempotency key has been recorded; there may
                                # not be a new response. Python callers don't
                                # (currently) care about such an event, so we
                                # simply ignore it.
                                if not __query_response__.HasField("response"):
                                    continue

                                __response__ = loopos.v1.loopos_pb2.OpsTicketShowBrainSourcesResponse()
                                __response__.ParseFromString(__query_response__.response)
                                yield OpsTicketShowBrainSourcesResponseFromProto(__response__)

                    except IMPORT_grpc.aio.AioRpcError as error:
                        # We expect to get disconnected from the server
                        # from time to time, e.g., when it is being
                        # updated, but we don't want that error to
                        # propagate, we just want to retry.
                        if IMPORT_reboot.aio.aborted.is_grpc_retryable_exception(error):
                            logger.debug(
                                "Reactive read to 'loopos.v1.OpsTicketMethods.ShowBrainSources' "
                                f"failed with a retryable error: '{error}'; "
                                "will retry..."
                            )
                            await __query_backoff__()
                            continue
                        if error.code() == IMPORT_grpc.StatusCode.ABORTED:
                            # Reconstitute the error that the server threw, if it was a declared error.
                            status = await IMPORT_rpc_status_async.from_call(__call__)
                            if status is not None:
                                raise OpsTicket.ShowBrainSourcesAborted.from_status(
                                    status
                                ) from None
                            raise OpsTicket.ShowBrainSourcesAborted.from_grpc_aio_rpc_error(
                                error
                            ) from None

                        raise

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            show_brain_sources = ShowBrainSources

        def reactively(self):
            return OpsTicket.WeakReference._Reactively(
                application_id=self._application_id,
                state_ref=self._state_ref,
                bearer_token=self._bearer_token,
            )

        class _Idempotently(IMPORT_typing.Generic[OpsTicket_IdempotentlyScheduleTypeVar]):

            _weak_reference: OpsTicket.WeakReference[OpsTicket_IdempotentlyScheduleTypeVar]

            def __init__(
                self,
                *,
                weak_reference: OpsTicket.WeakReference[OpsTicket_IdempotentlyScheduleTypeVar],
                idempotency: IMPORT_reboot_aio_idempotency.Idempotency,
            ):
                self._weak_reference = weak_reference
                self._idempotency = idempotency

            def schedule(
                self,
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
            ) -> OpsTicket_IdempotentlyScheduleTypeVar:
                return self._weak_reference._schedule_type(
                    self._weak_reference._application_id,
                    self._weak_reference._tasks,
                    when=when,
                    idempotency=self._idempotency,
                )

            def spawn(
                self,
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
            ) -> OpsTicket.WeakReference._Spawn:
                return OpsTicket.WeakReference._Spawn(
                    self._weak_reference._application_id,
                    self._weak_reference._tasks,
                    when=when,
                    idempotency=self._idempotency,
                )

            async def read(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
            ) -> OpsTicket.State:
                if self._weak_reference._servicer is None:
                    raise RuntimeError(
                        "`read()` is currently only supported within workflows; "
                        "Please reach out and let us know your use case if this "
                        "is important for you!"
                    )

                return await OpsTicketBaseServicer.WorkflowState._Idempotently._read(
                    self._weak_reference._servicer,
                    self._idempotency,
                    context,
                )

            async def write(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                writer: OpsTicketBaseServicer.InlineWriterCallable[OpsTicketBaseServicer.InlineWriterCallableResult],
                *,
                type: IMPORT_reboot_aio_workflows.Type | IMPORT_reboot_aio_workflows._Unset = IMPORT_reboot_aio_workflows._UNSET,
            ) -> OpsTicketBaseServicer.InlineWriterCallableResult:
                if self._weak_reference._servicer is None:
                    raise RuntimeError(
                        "`write()` is currently only supported within workflows; "
                        "Please reach out and let us know your use case if this "
                        "is important for you!"
                    )

                # `type=` is optional: when omitted, `writer`'s
                # return annotation is used.
                type_t, _ = IMPORT_reboot_aio_workflows._resolve_callable_return_type(
                    writer, type,
                )

                return await OpsTicketBaseServicer.WorkflowState._Idempotently._write_validating_effects(
                    self._weak_reference._servicer,
                    self._idempotency,
                    context,
                    writer,
                    type_result=type_t,
                    check_type=not self._idempotency.always,
                    unidempotently=self._idempotency.always,
                    checkpoint=context.checkpoint(),
                )


            async def Triage( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> None:
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.Triage(
                    __context__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            triage = Triage
            @IMPORT_typing.overload
            async def AcknowledgeDispatch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: OpsTicket.AcknowledgeDispatchRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> None:
                ...

            @IMPORT_typing.overload
            async def AcknowledgeDispatch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                dispatch_id: str | Unset = UNSET,
            ) -> None:
                ...

            async def AcknowledgeDispatch( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[OpsTicket.AcknowledgeDispatchRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                dispatch_id: str | Unset = UNSET,
            ) -> None:
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=OpsTicket.AcknowledgeDispatchRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=OpsTicket.AcknowledgeDispatchRequest)

                __request__: IMPORT_typing.Optional[OpsTicket.AcknowledgeDispatchRequest] = None
                if isinstance(__request_or_options__, OpsTicket.AcknowledgeDispatchRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert dispatch_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    __request__ = OpsTicketAcknowledgeDispatchRequestFromInputFields(
                        dispatch_id=dispatch_id,
                    )
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.AcknowledgeDispatch(
                    __context__,
                    __request__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            acknowledge_dispatch = AcknowledgeDispatch
            @IMPORT_typing.overload
            async def DispatchWithEscalation(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: OpsTicket.DispatchWithEscalationRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> OpsTicket.DispatchWithEscalationResponse:
                ...

            @IMPORT_typing.overload
            async def DispatchWithEscalation(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                vendor_id: str | Unset = UNSET,
                cost_estimate_usd: float | Unset = UNSET,
            ) -> OpsTicket.DispatchWithEscalationResponse:
                ...

            async def DispatchWithEscalation( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[OpsTicket.DispatchWithEscalationRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                vendor_id: str | Unset = UNSET,
                cost_estimate_usd: float | Unset = UNSET,
            ) -> OpsTicket.DispatchWithEscalationResponse:
                def error_message_supplement():
                    if any([isinstance(__context__, t) for t in [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext]]):
                        return f"'DispatchWithEscalation' is a workflow and must be scheduled from a '{type(__context__).__name__}' via `await [...].schedule([...]).DispatchWithEscalation(context, [...])`"
                    else:
                        return f"'DispatchWithEscalation' is a workflow and can not be called from '{type(__context__).__name__}'"

                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [
                        IMPORT_reboot_aio_contexts.WorkflowContext,
                        IMPORT_reboot_aio_external.ExternalContext,
                    ],
                    error_message_supplement=error_message_supplement(),
                )
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=OpsTicket.DispatchWithEscalationRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=OpsTicket.DispatchWithEscalationRequest)

                if isinstance(__request_or_options__, OpsTicket.DispatchWithEscalationRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                    assert vendor_id is UNSET
                    assert cost_estimate_usd is UNSET

                    return await (
                        await __this__.spawn().DispatchWithEscalation(
                            __context__,
                            __request_or_options__,
                            __options__,
                        )
                    )
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                    return await (
                        await __this__.spawn().DispatchWithEscalation(
                            __context__,
                            OpsTicketDispatchWithEscalationRequestFromInputFields(
                                vendor_id=vendor_id,
                                cost_estimate_usd=cost_estimate_usd,
                            ),
                            __options__,
                        )
                    )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            dispatch_with_escalation = DispatchWithEscalation

            async def ProposeNewRule( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> OpsTicket.ProposeNewRuleResponse:
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.ProposeNewRule(
                    __context__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            propose_new_rule = ProposeNewRule

            async def ShowBrainSources( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> OpsTicket.ShowBrainSourcesResponse:
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])

                return await __this__._weak_reference.ShowBrainSources(
                    __context__,
                    __options__,
                    __idempotency__=__this__._idempotency,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            show_brain_sources = ShowBrainSources

        @IMPORT_typing.overload
        def idempotently(self, alias: IMPORT_typing.Optional[str] = None, *, how: IMPORT_reboot_aio_idempotency.How = IMPORT_reboot_aio_idempotency.PER_WORKFLOW) -> OpsTicket.WeakReference._Idempotently[OpsTicket_ScheduleTypeVar]:
            ...

        @IMPORT_typing.overload
        def idempotently(self, *, key: IMPORT_uuid.UUID, how: IMPORT_reboot_aio_idempotency.How = IMPORT_reboot_aio_idempotency.PER_WORKFLOW) -> OpsTicket.WeakReference._Idempotently[OpsTicket_ScheduleTypeVar]:
            ...

        def idempotently(
            self,
            alias: IMPORT_typing.Optional[str] = None,
            *,
            key: IMPORT_typing.Optional[IMPORT_uuid.UUID] = None,
            how: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.How] = None,
        ) -> OpsTicket.WeakReference._Idempotently[OpsTicket_ScheduleTypeVar]:
            return OpsTicket.WeakReference._Idempotently(
                weak_reference=self,
                idempotency=IMPORT_reboot_aio_contexts.Context.idempotency(
                    alias=alias,
                    key=key,
                    how=how,
                )
            )

        def per_workflow(self, alias: IMPORT_typing.Optional[str] = None):
            return self.idempotently(alias)

        def per_iteration(self, alias: IMPORT_typing.Optional[str] = None):
            return self.idempotently(
                alias,
                how=IMPORT_reboot_aio_idempotency.PER_ITERATION,
            )

        def always(self):
            return self.idempotently(
                key=IMPORT_reboot_aio_idempotency.make_expiring_idempotency_key(),
                how=IMPORT_reboot_aio_idempotency.ALWAYS,
            )

        class _UntilChangesSatisfies(IMPORT_typing.Generic[OpsTicket_UntilCallableType]):

            _idempotency_alias: str
            _context: IMPORT_reboot_aio_contexts.WorkflowContext
            _callable: IMPORT_typing.Callable[[], IMPORT_typing.Awaitable[OpsTicket_UntilCallableType]]
            _type: type[OpsTicket_UntilCallableType]

            def __init__(
                self,
                *,
                idempotency_alias: str,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
                callable: IMPORT_typing.Callable[[], IMPORT_typing.Awaitable[OpsTicket_UntilCallableType]],
                type: type[OpsTicket_UntilCallableType],
            ):
                self._idempotency_alias = idempotency_alias
                self._context = context
                self._callable = callable
                self._type = type

            async def changes(self):
                return await IMPORT_reboot_aio_workflows.until_changes(
                    self._idempotency_alias,
                    self._context,
                    self._callable,
                    type=self._type,
                )

            async def satisfies(
                self,
                condition: IMPORT_typing.Callable[[OpsTicket_UntilCallableType], bool],
            ):

                async def converge():
                    response = await self._callable()
                    if condition(response):
                        return response
                    return False

                return await IMPORT_reboot_aio_workflows.until(
                    self._idempotency_alias,
                    self._context,
                    converge,
                    type=self._type,
                )

        class _Until:

            _weak_reference: OpsTicket.WeakReference
            _idempotency_alias: str

            def __init__(
                self,
                *,
                weak_reference: OpsTicket.WeakReference,
                idempotency_alias: str,
            ):
                self._weak_reference = weak_reference
                self._idempotency_alias = idempotency_alias

            def read(
                self,
                context: IMPORT_reboot_aio_contexts.WorkflowContext,
            ) -> OpsTicket.WeakReference._UntilChangesSatisfies[OpsTicket.State]:
                IMPORT_reboot_aio_types.assert_type(
                    context,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                async def callable():
                    return await self._weak_reference.read(context)

                return OpsTicket.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=self._idempotency_alias,
                    context=context,
                    callable=callable,
                    type=OpsTicket.State,
                )


            def ShowBrainSources( # type: ignore[misc]
                # In methods which are dealing with user input, (i.e.,
                # proto message field names), we should use '__double_underscored__'
                # variables to avoid any potential name conflicts with the method's
                # parameters.
                # The '__self__' parameter is a convention in Python to
                # indicate that this method is a bound method, so we use
                # '__this__' instead.
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> OpsTicket.WeakReference._UntilChangesSatisfies[OpsTicket.ShowBrainSourcesResponse]:
                IMPORT_reboot_aio_types.assert_type(
                    __context__,
                    [IMPORT_reboot_aio_contexts.WorkflowContext],
                )

                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                async def callable():
                    return await __this__._weak_reference.ShowBrainSources(
                        __context__,
                        __options__,
                    )

                return OpsTicket.WeakReference._UntilChangesSatisfies(
                    idempotency_alias=__this__._idempotency_alias,
                    context=__context__,
                    callable=callable,
                    type=OpsTicket.ShowBrainSourcesResponse,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            show_brain_sources = ShowBrainSources

        def until(self, alias: str):
            return OpsTicket.WeakReference._Until(
                weak_reference=self,
                idempotency_alias=alias,
            )

        def schedule(
            self,
            *,
            when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
        ) -> OpsTicket_ScheduleTypeVar:
            return self._schedule_type(self._application_id, self._tasks, when=when)

        class _Schedule:

            def __init__(
                self,
                application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
                tasks: IMPORT_typing.Callable[[IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext], OpsTicketTasksStub],
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
                idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            ) -> None:
                self._application_id = application_id
                self._tasks = tasks
                self._when = ensure_has_timezone(when=when)
                self._idempotency = idempotency

            # OpsTicket callable tasks:

            async def Triage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).Triage(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            triage = Triage
            @IMPORT_typing.overload
            async def AcknowledgeDispatch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: OpsTicket.AcknowledgeDispatchRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def AcknowledgeDispatch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                dispatch_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def AcknowledgeDispatch( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[OpsTicket.AcknowledgeDispatchRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                dispatch_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=OpsTicket.AcknowledgeDispatchRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=OpsTicket.AcknowledgeDispatchRequest)

                __request__: IMPORT_typing.Optional[OpsTicket.AcknowledgeDispatchRequest] = None
                if isinstance(__request_or_options__, OpsTicket.AcknowledgeDispatchRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert dispatch_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = OpsTicketAcknowledgeDispatchRequestFromInputFields(
                        dispatch_id=dispatch_id,
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).AcknowledgeDispatch(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            acknowledge_dispatch = AcknowledgeDispatch
            @IMPORT_typing.overload
            async def DispatchWithEscalation(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: OpsTicket.DispatchWithEscalationRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def DispatchWithEscalation(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                vendor_id: str | Unset = UNSET,
                cost_estimate_usd: float | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def DispatchWithEscalation( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[OpsTicket.DispatchWithEscalationRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                vendor_id: str | Unset = UNSET,
                cost_estimate_usd: float | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=OpsTicket.DispatchWithEscalationRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=OpsTicket.DispatchWithEscalationRequest)

                __request__: IMPORT_typing.Optional[OpsTicket.DispatchWithEscalationRequest] = None
                if isinstance(__request_or_options__, OpsTicket.DispatchWithEscalationRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert vendor_id is UNSET
                    assert cost_estimate_usd is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = OpsTicketDispatchWithEscalationRequestFromInputFields(
                        vendor_id=vendor_id,
                        cost_estimate_usd=cost_estimate_usd,
                    )

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).DispatchWithEscalation(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            dispatch_with_escalation = DispatchWithEscalation

            async def ProposeNewRule( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).ProposeNewRule(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            propose_new_rule = ProposeNewRule

            async def ShowBrainSources( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.TransactionContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.TransactionContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).ShowBrainSources(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return __task_id__

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            show_brain_sources = ShowBrainSources

        # A `WriterContext` can not call any methods in `_Schedule` to
        # prevent a writer from doing a `Foo.ref()` and trying to
        # schedule. However, we want to allow a writer to schedule
        # when we are constructing a `WeakReference` from
        # `self.ref()` so instead we return a `_WriterSchedule` to
        # provide type safety that allows a `WriterContext` to
        # schedule (for itself).
        class _WriterSchedule:

            def __init__(
                self,
                application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
                tasks: IMPORT_typing.Callable[[IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext], OpsTicketTasksStub],
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
                idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            ) -> None:
                self._tasks = tasks
                self._when = ensure_has_timezone(when=when)
                self._idempotency = idempotency

            # OpsTicket callable tasks:

            async def Triage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await OpsTicketServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).Triage(
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).Triage(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            triage = Triage
            @IMPORT_typing.overload
            async def AcknowledgeDispatch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: OpsTicket.AcknowledgeDispatchRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def AcknowledgeDispatch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                dispatch_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def AcknowledgeDispatch( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[OpsTicket.AcknowledgeDispatchRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                dispatch_id: str | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=OpsTicket.AcknowledgeDispatchRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=OpsTicket.AcknowledgeDispatchRequest)

                __request__: IMPORT_typing.Optional[OpsTicket.AcknowledgeDispatchRequest] = None
                if isinstance(__request_or_options__, OpsTicket.AcknowledgeDispatchRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert dispatch_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = OpsTicketAcknowledgeDispatchRequestFromInputFields(
                        dispatch_id=dispatch_id,
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await OpsTicketServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).AcknowledgeDispatch(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).AcknowledgeDispatch(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            acknowledge_dispatch = AcknowledgeDispatch
            @IMPORT_typing.overload
            async def DispatchWithEscalation(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: OpsTicket.DispatchWithEscalationRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            @IMPORT_typing.overload
            async def DispatchWithEscalation(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                vendor_id: str | Unset = UNSET,
                cost_estimate_usd: float | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                ...

            async def DispatchWithEscalation( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __request_or_options__: IMPORT_typing.Optional[OpsTicket.DispatchWithEscalationRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                vendor_id: str | Unset = UNSET,
                cost_estimate_usd: float | Unset = UNSET,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=OpsTicket.DispatchWithEscalationRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=OpsTicket.DispatchWithEscalationRequest)

                __request__: IMPORT_typing.Optional[OpsTicket.DispatchWithEscalationRequest] = None
                if isinstance(__request_or_options__, OpsTicket.DispatchWithEscalationRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert vendor_id is UNSET
                    assert cost_estimate_usd is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = OpsTicketDispatchWithEscalationRequestFromInputFields(
                        vendor_id=vendor_id,
                        cost_estimate_usd=cost_estimate_usd,
                    )

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await OpsTicketServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).DispatchWithEscalation(
                        __request__,
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).DispatchWithEscalation(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            dispatch_with_escalation = DispatchWithEscalation

            async def ProposeNewRule( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await OpsTicketServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).ProposeNewRule(
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).ProposeNewRule(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            propose_new_rule = ProposeNewRule

            async def ShowBrainSources( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> IMPORT_rbt_v1alpha1.tasks_pb2.TaskId:
                # Only `writer`s and `transaction`s should ``schedule()`, a
                # `workflow` should `spawn()`.
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext])

                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                if isinstance(__context__, IMPORT_reboot_aio_contexts.WriterContext):
                    return (await OpsTicketServicerTasks(
                        context=__context__,
                        state_ref=__context__._state_ref,
                    ).ShowBrainSources(
                        schedule=__this__._when,
                    )).task_id

                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                return await __this__._tasks(
                    __context__
                ).ShowBrainSources(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            show_brain_sources = ShowBrainSources

        def spawn(
            self,
            *,
            when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
        ) -> OpsTicket.WeakReference._Spawn:
            # Within a `workflow`, all "bare" `spawn()` calls are
            # syntactic sugar for `per_workflow()`, unless we're
            # within a control loop, in which case they are syntactic
            # sugar for `per_iteration()`.
            context = IMPORT_reboot_aio_contexts.Context.get()
            if context is not None:
                if isinstance(context, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return (
                        self.per_iteration() if context.within_loop()
                        else self.per_workflow()
                    ).spawn(when=when)
                elif isinstance(context, IMPORT_reboot_aio_external.InitializeContext):
                    return self.idempotently().spawn(when=when)

            return OpsTicket.WeakReference._Spawn(
                self._application_id, self._tasks, when=when
            )

        class _Spawn:

            def __init__(
                self,
                application_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.ApplicationId],
                tasks: IMPORT_typing.Callable[[IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext], OpsTicketTasksStub],
                *,
                when: IMPORT_typing.Optional[IMPORT_datetime_datetime | IMPORT_datetime_timedelta] = None,
                idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            ) -> None:
                self._application_id = application_id
                self._tasks = tasks
                self._when = ensure_has_timezone(when=when)
                self._idempotency = idempotency

            # OpsTicket callable tasks:

            async def Triage( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> OpsTicket.TriageTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).Triage(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return OpsTicket.TriageTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            triage = Triage
            @IMPORT_typing.overload
            async def AcknowledgeDispatch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: OpsTicket.AcknowledgeDispatchRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> OpsTicket.AcknowledgeDispatchTask:
                ...

            @IMPORT_typing.overload
            async def AcknowledgeDispatch(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                dispatch_id: str | Unset = UNSET,
            ) -> OpsTicket.AcknowledgeDispatchTask:
                ...

            async def AcknowledgeDispatch( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[OpsTicket.AcknowledgeDispatchRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                dispatch_id: str | Unset = UNSET,
            ) -> OpsTicket.AcknowledgeDispatchTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=OpsTicket.AcknowledgeDispatchRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=OpsTicket.AcknowledgeDispatchRequest)


                __request__: IMPORT_typing.Optional[OpsTicket.AcknowledgeDispatchRequest] = None
                if isinstance(__request_or_options__, OpsTicket.AcknowledgeDispatchRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert dispatch_id is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = OpsTicketAcknowledgeDispatchRequestFromInputFields(
                        dispatch_id=dispatch_id,
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).AcknowledgeDispatch(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return OpsTicket.AcknowledgeDispatchTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            acknowledge_dispatch = AcknowledgeDispatch
            @IMPORT_typing.overload
            async def DispatchWithEscalation(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: OpsTicket.DispatchWithEscalationRequest,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> OpsTicket.DispatchWithEscalationTask:
                ...

            @IMPORT_typing.overload
            async def DispatchWithEscalation(
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                vendor_id: str | Unset = UNSET,
                cost_estimate_usd: float | Unset = UNSET,
            ) -> OpsTicket.DispatchWithEscalationTask:
                ...

            async def DispatchWithEscalation( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __request_or_options__: IMPORT_typing.Optional[OpsTicket.DispatchWithEscalationRequest | IMPORT_reboot_aio_call.Options] = None,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
                *,
                vendor_id: str | Unset = UNSET,
                cost_estimate_usd: float | Unset = UNSET,
            ) -> OpsTicket.DispatchWithEscalationTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                # UX improvement: check that neither positional argument was accidentally
                # given a gRPC request type.
                IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=OpsTicket.DispatchWithEscalationRequest)
                IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=OpsTicket.DispatchWithEscalationRequest)


                __request__: IMPORT_typing.Optional[OpsTicket.DispatchWithEscalationRequest] = None
                if isinstance(__request_or_options__, OpsTicket.DispatchWithEscalationRequest):
                    assert __request_or_options__ is not None
                    assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                    assert vendor_id is UNSET
                    assert cost_estimate_usd is UNSET

                    __request__ = __request_or_options__
                else:
                    assert __options__ is None
                    assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                    __options__ = __request_or_options__

                    __request__ = OpsTicketDispatchWithEscalationRequestFromInputFields(
                        vendor_id=vendor_id,
                        cost_estimate_usd=cost_estimate_usd,
                    )
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).DispatchWithEscalation(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return OpsTicket.DispatchWithEscalationTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            dispatch_with_escalation = DispatchWithEscalation

            async def ProposeNewRule( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> OpsTicket.ProposeNewRuleTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).ProposeNewRule(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return OpsTicket.ProposeNewRuleTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            propose_new_rule = ProposeNewRule

            async def ShowBrainSources( # type: ignore[misc]
                __this__,
                __context__: IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
                __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            ) -> OpsTicket.ShowBrainSourcesTask:
                IMPORT_reboot_aio_types.assert_type(__context__, [IMPORT_reboot_aio_contexts.WorkflowContext, IMPORT_reboot_aio_external.ExternalContext])
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __schedule__: IMPORT_typing.Optional[IMPORT_reboot_time_DateTimeWithTimeZone] = (IMPORT_reboot_time_DateTimeWithTimeZone.now() + __this__._when) if isinstance(
                    __this__._when, IMPORT_datetime_timedelta
                ) else __this__._when

                __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
                __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = __this__._idempotency
                __bearer_token__: IMPORT_typing.Optional[str] = None

                if __options__ is not None:
                    IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                    if __options__.metadata is not None:
                        __metadata__ = __options__.metadata
                    if __options__.bearer_token is not None:
                        __bearer_token__ = __options__.bearer_token

                # Add scheduling information to the metadata.
                __metadata__ = (
                    (IMPORT_reboot_aio_headers.TASK_SCHEDULE,
                    __schedule__.isoformat() if __schedule__ else ''),
                ) + (__metadata__ or tuple())

                __task_id__ = await __this__._tasks(
                    __context__
                ).ShowBrainSources(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )

                return OpsTicket.ShowBrainSourcesTask(
                    __context__,
                    task_id=__task_id__,
                )

            # Keep the original functions on the client, so old code will
            # continue to work, but use the new 'snake_case' method in
            # the new code.
            show_brain_sources = ShowBrainSources

        async def read(
            self,
            context: IMPORT_reboot_aio_contexts.WorkflowContext,
        ) -> OpsTicket.State:
            return await (
                self.always() if context.within_until()
                else (
                    self.per_iteration() if context.within_loop()
                    else self.per_workflow()
                )
            ).read(context)

        async def write(
            self,
            context: IMPORT_reboot_aio_contexts.WorkflowContext,
            writer: OpsTicketBaseServicer.InlineWriterCallable[OpsTicketBaseServicer.InlineWriterCallableResult],
            *,
            type: IMPORT_reboot_aio_workflows.Type | IMPORT_reboot_aio_workflows._Unset = IMPORT_reboot_aio_workflows._UNSET,
        ) -> OpsTicketBaseServicer.InlineWriterCallableResult:
            """Perform an "inline write" within a workflow.

            `type=` is optional: when omitted, `writer`'s return
            annotation is used.
            """
            # We forward `type=` along to the inner `write` (which
            # also infers if not passed). Pass through whichever
            # form the user supplied -- explicit class or the
            # `_UNSET` sentinel to trigger inference downstream.
            return await (
                self.always() if context.within_until()
                else (
                    self.per_iteration() if context.within_loop()
                    else self.per_workflow()
                )
            ).write(
                context, writer, type=type
            )

        # OpsTicket specific methods:

        async def Triage( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> None:
            assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.per_iteration() if __context__.within_loop()
                        else __this__.per_workflow()
                    ).Triage(
                        __context__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().Triage(
                        __context__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token

            return OpsTicketTriageResponseFromProto(
                await __this__._writer(__context__).Triage(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        triage = Triage
        @IMPORT_typing.overload
        async def AcknowledgeDispatch(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: OpsTicket.AcknowledgeDispatchRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> None:
            ...

        @IMPORT_typing.overload
        async def AcknowledgeDispatch(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            dispatch_id: str | Unset = UNSET,
        ) -> None:
            ...

        async def AcknowledgeDispatch( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[OpsTicket.AcknowledgeDispatchRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
            dispatch_id: str | Unset = UNSET,
        ) -> None:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=OpsTicket.AcknowledgeDispatchRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=OpsTicket.AcknowledgeDispatchRequest)

            __request__: IMPORT_typing.Optional[OpsTicket.AcknowledgeDispatchRequest] = None
            if isinstance(__request_or_options__, OpsTicket.AcknowledgeDispatchRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

                assert dispatch_id is UNSET

                __request__ = __request_or_options__
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__

                __request__ = OpsTicketAcknowledgeDispatchRequestFromInputFields(
                    dispatch_id=dispatch_id,
                )

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.per_iteration() if __context__.within_loop()
                        else __this__.per_workflow()
                    ).AcknowledgeDispatch(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().AcknowledgeDispatch(
                        __context__,
                        __request__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token

            return OpsTicketAcknowledgeDispatchResponseFromProto(
                await __this__._writer(__context__).AcknowledgeDispatch(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        acknowledge_dispatch = AcknowledgeDispatch
        @IMPORT_typing.overload
        async def DispatchWithEscalation(
            __this__,
            __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.WorkflowContext,
            __request_or_options__: OpsTicket.DispatchWithEscalationRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> OpsTicket.DispatchWithEscalationResponse:
            ...

        @IMPORT_typing.overload
        async def DispatchWithEscalation(
            __this__,
            __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.WorkflowContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            vendor_id: str | Unset = UNSET,
            cost_estimate_usd: float | Unset = UNSET,
        ) -> OpsTicket.DispatchWithEscalationResponse:
            ...

        async def DispatchWithEscalation( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_external.ExternalContext | IMPORT_reboot_aio_contexts.WorkflowContext,
            __request_or_options__: IMPORT_typing.Optional[OpsTicket.DispatchWithEscalationRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            vendor_id: str | Unset = UNSET,
            cost_estimate_usd: float | Unset = UNSET,
        ) -> OpsTicket.DispatchWithEscalationResponse:
            def error_message_supplement():
                if any([isinstance(__context__, t) for t in [IMPORT_reboot_aio_contexts.WriterContext, IMPORT_reboot_aio_contexts.TransactionContext]]):
                    return f"'DispatchWithEscalation' is a workflow and must be scheduled from a '{type(__context__).__name__}' via `await [...].schedule([...]).DispatchWithEscalation(context, [...])`"
                else:
                    return f"'DispatchWithEscalation' is a workflow and can not be called from a '{type(__context__).__name__}'"

            IMPORT_reboot_aio_types.assert_type(
                __context__,
                [IMPORT_reboot_aio_external.ExternalContext, IMPORT_reboot_aio_contexts.WorkflowContext],
                error_message_supplement=error_message_supplement(),
            )

            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=OpsTicket.DispatchWithEscalationRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=OpsTicket.DispatchWithEscalationRequest)

            if isinstance(__request_or_options__, OpsTicket.DispatchWithEscalationRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                assert vendor_id is UNSET
                assert cost_estimate_usd is UNSET

                return await (
                    await __this__.spawn().DispatchWithEscalation(
                        __context__,
                        __request_or_options__,
                        __options__,
                    )
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await (
                    await __this__.spawn().DispatchWithEscalation(
                        __context__,
                        OpsTicketDispatchWithEscalationRequestFromInputFields(
                            vendor_id=vendor_id,
                            cost_estimate_usd=cost_estimate_usd,
                        ),
                        __options__,
                    )
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        dispatch_with_escalation = DispatchWithEscalation

        async def ProposeNewRule( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> OpsTicket.ProposeNewRuleResponse:
            assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.per_iteration() if __context__.within_loop()
                        else __this__.per_workflow()
                    ).ProposeNewRule(
                        __context__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().ProposeNewRule(
                        __context__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token

            return OpsTicketProposeNewRuleResponseFromProto(
                await __this__._writer(__context__).ProposeNewRule(
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        propose_new_rule = ProposeNewRule

        async def ShowBrainSources( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        ) -> OpsTicket.ShowBrainSourcesResponse:
            assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)

            # Within a `workflow`, all "bare" calls are
            # `per_workflow()` calls, unless we're within a control
            # loop, in which case they are syntactic sugar for
            # `per_iteration()`.
            #
            # Unless we are "within until" in which case all "bare"
            # calls are `.always().
            if __idempotency__ is None:
                if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                    return await (
                        __this__.always() if __context__.within_until()
                        else (
                            __this__.per_iteration() if __context__.within_loop()
                            else __this__.per_workflow()
                        )
                    ).ShowBrainSources(
                        __context__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )
                elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                    return await __this__.idempotently().ShowBrainSources(
                        __context__,
                        __options__ or IMPORT_reboot_aio_call.Options(),
                    )

            __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
            __bearer_token__: IMPORT_typing.Optional[str] = None
            if __options__ is not None:
                IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
                if __options__.metadata is not None:
                    __metadata__ = __options__.metadata
                if __options__.bearer_token is not None:
                    __bearer_token__ = __options__.bearer_token
            return OpsTicketShowBrainSourcesResponseFromProto(
                await __this__._reader(__context__).ShowBrainSources(
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                    idempotency=__idempotency__,
                )
            )
        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        show_brain_sources = ShowBrainSources

    class _Forall:

        _ids: IMPORT_typing.Iterable[str]

        def __init__(self, ids: IMPORT_typing.Iterable[str]):
            self._ids = ids


        async def Triage( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[None]:
            __options__ = __options__ or IMPORT_reboot_aio_call.Options()
            return await IMPORT_asyncio.gather(
                *[
                    OpsTicket.ref(
                        id
                    ).Triage(
                        __context__,
                        __options__,
                    ) for id in __this__._ids
                ]
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        triage = Triage
        @IMPORT_typing.overload
        async def AcknowledgeDispatch(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: OpsTicket.AcknowledgeDispatchRequest,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[None]:
            ...

        @IMPORT_typing.overload
        async def AcknowledgeDispatch(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            dispatch_id: str | Unset = UNSET,
        ) -> list[None]:
            ...

        async def AcknowledgeDispatch( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __request_or_options__: IMPORT_typing.Optional[OpsTicket.AcknowledgeDispatchRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            dispatch_id: str | Unset = UNSET,
        ) -> list[None]:
            if isinstance(__request_or_options__, OpsTicket.AcknowledgeDispatchRequest):
                assert __request_or_options__ is not None
                assert __options__ is None or isinstance(__options__, IMPORT_reboot_aio_call.Options)
                __options__ = __options__ or IMPORT_reboot_aio_call.Options()

                assert dispatch_id is UNSET

                return await IMPORT_asyncio.gather(
                    *[
                        OpsTicket.ref(
                            id
                        ).AcknowledgeDispatch(
                            __context__,
                            __request_or_options__,
                            __options__,
                        ) for id in __this__._ids
                    ]
                )
            else:
                assert __options__ is None
                assert __request_or_options__ is None or isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options__ or IMPORT_reboot_aio_call.Options()

                return await IMPORT_asyncio.gather(
                    *[
                        OpsTicket.ref(
                            id
                        ).AcknowledgeDispatch(
                            __context__,
                            OpsTicketAcknowledgeDispatchRequestFromInputFields(
                                dispatch_id=dispatch_id,
                            ),
                            __options__,
                        ) for id in __this__._ids
                    ]
                )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        acknowledge_dispatch = AcknowledgeDispatch

        async def ProposeNewRule( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[OpsTicket.ProposeNewRuleResponse]:
            __options__ = __options__ or IMPORT_reboot_aio_call.Options()
            return await IMPORT_asyncio.gather(
                *[
                    OpsTicket.ref(
                        id
                    ).ProposeNewRule(
                        __context__,
                        __options__,
                    ) for id in __this__._ids
                ]
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        propose_new_rule = ProposeNewRule

        async def ShowBrainSources( # type: ignore[misc]
            # In methods which are dealing with user input, (i.e.,
            # proto message field names), we should use '__double_underscored__'
            # variables to avoid any potential name conflicts with the method's
            # parameters.
            # The '__self__' parameter is a convention in Python to
            # indicate that this method is a bound method, so we use
            # '__this__' instead.
            __this__,
            __context__: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> list[OpsTicket.ShowBrainSourcesResponse]:
            __options__ = __options__ or IMPORT_reboot_aio_call.Options()
            return await IMPORT_asyncio.gather(
                *[
                    OpsTicket.ref(
                        id
                    ).ShowBrainSources(
                        __context__,
                        __options__,
                    ) for id in __this__._ids
                ]
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        show_brain_sources = ShowBrainSources

    @classmethod
    def forall(cls, ids: IMPORT_typing.Iterable[str]) -> OpsTicket._Forall:
        return OpsTicket._Forall(ids)

    @classmethod
    def ref(
        cls,
        state_id: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId] = None,
        *,
        bearer_token: IMPORT_typing.Optional[str] = None,
    ) -> OpsTicket.WeakReference[OpsTicket.WeakReference._Schedule] | OpsTicket.WeakReference[OpsTicket.WeakReference._WriterSchedule]:
        # We support calling `OpsTicket.ref()` with
        # no `state_id` __only__ inside a workflow to be able to call an
        # inline writer, inline reader or other method call, since
        # workflow is a `classmethod` and therefor we can't get a
        # reference to outselves as `self.ref()`.
        if state_id is None:
            context = IMPORT_reboot_aio_contexts.Context.get()

            if context is None:
                raise RuntimeError(
                    'Missing asyncio context variable `context`; '
                    'are you using this class without Reboot?'
                )

            if not isinstance(context, IMPORT_reboot_aio_contexts.WorkflowContext):
                raise RuntimeError(
                    '`ref()` called without a `state_id` can only be used within a Workflow.'
                )

            servicer = OpsTicketBaseServicer.__servicer__.get()

            if servicer is None:
                raise RuntimeError(
                    'Missing asyncio context variable `servicer`; '
                    'are you using this class without Reboot?'
                )

            return OpsTicket.WeakReference(
                # TODO(https://github.com/reboot-dev/mono/issues/3226): add support for calling other applications.
                # For now this always stays within the application that creates the context.
                application_id=None,
                state_id=context._state_ref.id,
                schedule_type=OpsTicket.WeakReference._WriterSchedule,
                # If the user didn't specify a bearer token we may still end up using the app-internal bearer token,
                # but that's decided at the time of the call.
                bearer_token=bearer_token,
                servicer=servicer,
            )

        return OpsTicket.WeakReference(
            # TODO(https://github.com/reboot-dev/mono/issues/3226): add support for calling other applications.
            # For now this always stays within the application that creates the context.
            application_id=None,
            state_id=state_id,
            schedule_type=OpsTicket.WeakReference._Schedule,
            bearer_token=bearer_token,
        )

    @IMPORT_typing.overload
    @classmethod
    async def Create(
        __cls__,
        __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        __state_id_or_request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId | OpsTicket.CreateRequest] = None,
        __request_or_options_or_idempotency__: IMPORT_typing.Optional[OpsTicket.CreateRequest | IMPORT_reboot_aio_call.Options] = None,
        __options_or_idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options | IMPORT_reboot_aio_idempotency.Idempotency] = None,
        __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
    ) -> tuple[OpsTicket.WeakReference, None]:
        ...

    @IMPORT_typing.overload
    @classmethod
    async def Create(
        __cls__,
        __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        __state_id_or_request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId | IMPORT_reboot_aio_call.Options] = None,
        __request_or_options_or_idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options | IMPORT_reboot_aio_idempotency.Idempotency] = None,
        __options_or_idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        property_id: str | Unset = UNSET,
        source: str | Unset = UNSET,
        raw_input: str | Unset = UNSET,
        transcript_native: str | Unset = UNSET,
        transcript_en: str | Unset = UNSET,
        detected_language: str | Unset = UNSET,
        sender_user_id: str | Unset = UNSET,
        created_at: str | Unset = UNSET,
    ) -> tuple[OpsTicket.WeakReference, None]:
        ...

    @classmethod
    async def Create( # type: ignore[misc]
        __cls__,
        __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        __state_id_or_request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId | OpsTicket.CreateRequest | IMPORT_reboot_aio_call.Options] = None,
        __request_or_options_or_idempotency__: IMPORT_typing.Optional[OpsTicket.CreateRequest | IMPORT_reboot_aio_call.Options | IMPORT_reboot_aio_idempotency.Idempotency] = None,
        __options_or_idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options | IMPORT_reboot_aio_idempotency.Idempotency] = None,
        __idempotency__: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None,
        *,
        property_id: str | Unset = UNSET,
        source: str | Unset = UNSET,
        raw_input: str | Unset = UNSET,
        transcript_native: str | Unset = UNSET,
        transcript_en: str | Unset = UNSET,
        detected_language: str | Unset = UNSET,
        sender_user_id: str | Unset = UNSET,
        created_at: str | Unset = UNSET,
    ) -> tuple[OpsTicket.WeakReference, None]:
        # Within a `workflow`, all "bare" calls are
        # `per_workflow()` calls, unless we're within a control
        # loop, in which case they are syntactic sugar for
        # `per_iteration()`.
        #
        # Unless we are "within until" in which case all "bare"
        # calls are `.always()`.

        __request__: IMPORT_typing.Optional[OpsTicket.CreateRequest] = None
        __state_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId] = None
        __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None

        if isinstance(__state_id_or_request_or_options__, IMPORT_reboot_aio_types.StateId):
            __state_id__ = __state_id_or_request_or_options__
            if isinstance(__request_or_options_or_idempotency__, OpsTicket.CreateRequest):
                __request__ = __request_or_options_or_idempotency__
                assert __options_or_idempotency__ is None or isinstance(__options_or_idempotency__, IMPORT_reboot_aio_call.Options)
                __options__ = __options_or_idempotency__
                assert __idempotency__ is None or isinstance(__idempotency__, IMPORT_reboot_aio_idempotency.Idempotency)

                assert property_id is UNSET
                assert source is UNSET
                assert raw_input is UNSET
                assert transcript_native is UNSET
                assert transcript_en is UNSET
                assert detected_language is UNSET
                assert sender_user_id is UNSET
                assert created_at is UNSET
            else:
                assert __request_or_options_or_idempotency__ is None or isinstance(__request_or_options_or_idempotency__, IMPORT_reboot_aio_call.Options)
                __options__ = __request_or_options_or_idempotency__
                assert __options_or_idempotency__ is None or isinstance(__options_or_idempotency__, IMPORT_reboot_aio_idempotency.Idempotency)
                __idempotency__ = __options_or_idempotency__

                __request__ = OpsTicketCreateRequestFromInputFields(
                    property_id=property_id,
                    source=source,
                    raw_input=raw_input,
                    transcript_native=transcript_native,
                    transcript_en=transcript_en,
                    detected_language=detected_language,
                    sender_user_id=sender_user_id,
                    created_at=created_at,
                )
        elif isinstance(__state_id_or_request_or_options__, OpsTicket.CreateRequest):
            __request__ = __state_id_or_request_or_options__
            assert __request_or_options_or_idempotency__ is None or isinstance(__request_or_options_or_idempotency__, IMPORT_reboot_aio_call.Options)
            __options__ = __request_or_options_or_idempotency__
            assert __options_or_idempotency__ is None or isinstance(__options_or_idempotency__, IMPORT_reboot_aio_idempotency.Idempotency)
            __idempotency__ = __options_or_idempotency__
        else:
            assert (
                __state_id_or_request_or_options__ is None
                or isinstance(__state_id_or_request_or_options__, IMPORT_reboot_aio_call.Options)
            ), "Invalid argument type. Did you pass:\n" \
            "  - A 'state_id' that is not of type 'reboot.aio.types.StateId'?\n" \
            "  - 'options' that are not of type 'reboot.aio.call.Options'?"
            __options__ = __state_id_or_request_or_options__
            assert __request_or_options_or_idempotency__ is None or isinstance(__request_or_options_or_idempotency__, IMPORT_reboot_aio_idempotency.Idempotency)
            __idempotency__ = __request_or_options_or_idempotency__

            __request__ = OpsTicketCreateRequestFromInputFields(
                property_id=property_id,
                source=source,
                raw_input=raw_input,
                transcript_native=transcript_native,
                transcript_en=transcript_en,
                detected_language=detected_language,
                sender_user_id=sender_user_id,
                created_at=created_at,
            )

        if __idempotency__ is None:
            __args__: tuple[IMPORT_typing.Any, ...] = (
                __context__,
            )
            if __state_id__ is not None:
                # Don't include `state_id` if it is `None`, so that
                # we won't break the positional argument deduction.
                __args__ += (__state_id__,)
            __args__ += (
                __request__,
                __options__,
            )
            if isinstance(__context__, IMPORT_reboot_aio_contexts.WorkflowContext):
                return await (
                    __cls__.always() if __context__.within_until()
                    else (
                        __cls__.per_iteration() if __context__.within_loop()
                        else __cls__.per_workflow()
                    )
                ).Create(
                    *__args__
                )
            elif isinstance(__context__, IMPORT_reboot_aio_external.InitializeContext):
                return await __cls__.idempotently().Create(
                    *__args__
                )

        __metadata__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.GrpcMetadata] = None
        __bearer_token__: IMPORT_typing.Optional[str] = None

        if __options__ is not None:
            IMPORT_reboot_aio_types.assert_type(__options__, [IMPORT_reboot_aio_call.Options])
            if __options__.metadata is not None:
                __metadata__ = __options__.metadata
            if __options__.bearer_token is not None:
                __bearer_token__ = __options__.bearer_token

        if __state_id__ is None:
            if __idempotency__ is None:
                __state_id__ = str(IMPORT_uuid.uuid4())
            else:
                __state_id__ = __context__.generate_idempotent_state_id(
                    state_type_name=__cls__.__state_type_name__,
                    service_name=IMPORT_reboot_aio_types.ServiceName('loopos.v1.OpsTicketMethods'),
                    method='Create',
                    idempotency=__idempotency__,
                )

        __reference__ = OpsTicket.ref(
            __state_id__, bearer_token=__bearer_token__
        )
        __stub__ = __reference__._writer(__context__)
        return (
            __reference__,
            OpsTicketCreateResponseFromProto(
                await __stub__.Create(
                    __request__,
                    idempotency=__idempotency__,
                    metadata=__metadata__,
                    bearer_token=__bearer_token__,
                )
            ),
        )

    # Keep the original functions on the client, so old code will
    # continue to work, but use the new 'snake_case' method in
    # the new code.
    create = Create

    @IMPORT_typing.overload
    @classmethod
    def idempotently(cls, alias: IMPORT_typing.Optional[str] = None, *, how: IMPORT_reboot_aio_idempotency.How = IMPORT_reboot_aio_idempotency.PER_WORKFLOW) -> OpsTicket._ConstructIdempotently:
        ...

    @IMPORT_typing.overload
    @classmethod
    def idempotently(cls, *, key: IMPORT_uuid.UUID, how: IMPORT_reboot_aio_idempotency.How = IMPORT_reboot_aio_idempotency.PER_WORKFLOW) -> OpsTicket._ConstructIdempotently:
        ...

    @classmethod
    def idempotently(
        cls,
        alias: IMPORT_typing.Optional[str] = None,
        *,
        key: IMPORT_typing.Optional[IMPORT_uuid.UUID] = None,
        how: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.How] = None,
    ) -> OpsTicket._ConstructIdempotently:
        return OpsTicket._ConstructIdempotently(
            _idempotency=IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=alias,
                key=key,
                how=how,
            ),
        )

    @classmethod
    def per_workflow(
        cls,
        alias: IMPORT_typing.Optional[str] = None,
    ):
        return cls.idempotently(alias)

    @classmethod
    def per_iteration(
        cls,
        alias: IMPORT_typing.Optional[str] = None,
    ):
        return cls.idempotently(
            alias,
            how=IMPORT_reboot_aio_idempotency.PER_ITERATION,
        )

    @classmethod
    def always(cls):
        return cls.idempotently(
            key=IMPORT_reboot_aio_idempotency.make_expiring_idempotency_key(),
            how=IMPORT_reboot_aio_idempotency.ALWAYS,
        )

    @IMPORT_dataclasses.dataclass(frozen=True)
    class _ConstructIdempotently:

        _idempotency: IMPORT_reboot_aio_idempotency.Idempotency

        @IMPORT_typing.overload
        async def Create(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __state_id_or_request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId | OpsTicket.CreateRequest] = None,
            __request_or_options__: IMPORT_typing.Optional[OpsTicket.CreateRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
        ) -> tuple[OpsTicket.WeakReference, None]:
            ...

        @IMPORT_typing.overload
        async def Create(
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __state_id_or_request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId | IMPORT_reboot_aio_call.Options] = None,
            __request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            property_id: str | Unset = UNSET,
            source: str | Unset = UNSET,
            raw_input: str | Unset = UNSET,
            transcript_native: str | Unset = UNSET,
            transcript_en: str | Unset = UNSET,
            detected_language: str | Unset = UNSET,
            sender_user_id: str | Unset = UNSET,
            created_at: str | Unset = UNSET,
        ) -> tuple[OpsTicket.WeakReference, None]:
            ...

        async def Create( # type: ignore[misc]
            __this__,
            __context__: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
            __state_id_or_request_or_options__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId | OpsTicket.CreateRequest | IMPORT_reboot_aio_call.Options] = None,
            __request_or_options__: IMPORT_typing.Optional[OpsTicket.CreateRequest | IMPORT_reboot_aio_call.Options] = None,
            __options__: IMPORT_typing.Optional[IMPORT_reboot_aio_call.Options] = None,
            *,
            property_id: str | Unset = UNSET,
            source: str | Unset = UNSET,
            raw_input: str | Unset = UNSET,
            transcript_native: str | Unset = UNSET,
            transcript_en: str | Unset = UNSET,
            detected_language: str | Unset = UNSET,
            sender_user_id: str | Unset = UNSET,
            created_at: str | Unset = UNSET,
        ) -> tuple[OpsTicket.WeakReference, None]:
            # UX improvement: check that neither positional argument was accidentally
            # given a gRPC request type.
            IMPORT_reboot_aio_types.assert_not_request_type(__context__, request_type=OpsTicket.CreateRequest)
            IMPORT_reboot_aio_types.assert_not_request_type(__options__, request_type=OpsTicket.CreateRequest)

            __state_id__: IMPORT_typing.Optional[IMPORT_reboot_aio_types.StateId] = None
            __request__: IMPORT_typing.Optional[OpsTicket.CreateRequest] = None

            if isinstance(__state_id_or_request_or_options__, IMPORT_reboot_aio_types.StateId):
                __state_id__ = __state_id_or_request_or_options__
                if isinstance(__request_or_options__, OpsTicket.CreateRequest):
                    __request__ = __request_or_options__
                    if __options__ is not None and not isinstance(__options__, IMPORT_reboot_aio_call.Options):
                        raise TypeError(f"Expecting fourth positional argument to be of type 'reboot.aio.call.Options', got '{type(__request_or_options__).__name__}'")

                    assert property_id is UNSET
                    assert source is UNSET
                    assert raw_input is UNSET
                    assert transcript_native is UNSET
                    assert transcript_en is UNSET
                    assert detected_language is UNSET
                    assert sender_user_id is UNSET
                    assert created_at is UNSET
                else:
                    if __request_or_options__ is not None and not isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options):
                        raise TypeError(f"Expecting third positional argument to be of type 'reboot.aio.call.Options', got '{type(__request_or_options__).__name__}'")

                    __options__ = __request_or_options__

                    __request__ = OpsTicketCreateRequestFromInputFields(
                        property_id=property_id,
                        source=source,
                        raw_input=raw_input,
                        transcript_native=transcript_native,
                        transcript_en=transcript_en,
                        detected_language=detected_language,
                        sender_user_id=sender_user_id,
                        created_at=created_at,
                    )
            elif isinstance(__state_id_or_request_or_options__, OpsTicket.CreateRequest):
                __request__ = __state_id_or_request_or_options__
                if __request_or_options__ is not None and not isinstance(__request_or_options__, IMPORT_reboot_aio_call.Options):
                    raise TypeError(f"Expecting third positional argument to be of type 'reboot.aio.call.Options', got '{type(__request_or_options__).__name__}'")

                __options__ = __request_or_options__
            else:
                if __state_id_or_request_or_options__ is not None and not isinstance(__state_id_or_request_or_options__, IMPORT_reboot_aio_call.Options):
                    raise TypeError(f"Expecting second positional argument to either be of type 'str' or 'reboot.aio.call.Options', got '{type(__state_id_or_request_or_options__).__name__}'")

                __options__ = __state_id_or_request_or_options__

                __request__ = OpsTicketCreateRequestFromInputFields(
                    property_id=property_id,
                    source=source,
                    raw_input=raw_input,
                    transcript_native=transcript_native,
                    transcript_en=transcript_en,
                    detected_language=detected_language,
                    sender_user_id=sender_user_id,
                    created_at=created_at,
                )
            __args__: tuple[IMPORT_typing.Any, ...]

            if __state_id__ is not None:
                __args__ = (
                    __context__,
                    __state_id__,
                    __request__,
                    __options__,
                    __this__._idempotency,
                )
            else:
                __args__ = (
                    __context__,
                    __request__,
                    __options__,
                    __this__._idempotency,
                )

            return await OpsTicket.Create(
                *__args__,
            )

        # Keep the original functions on the client, so old code will
        # continue to work, but use the new 'snake_case' method in
        # the new code.
        create = Create



############################ Reference Node adapters ############################
# Used by Node.js WeakReference implementations to access Python code and
# vice-versa. Relevant to clients.

class UserWeakReferenceNodeAdaptor(User.WeakReference[User.WeakReference._Schedule]):

    async def _call(  # type: ignore[override]
        self,
        *,
        callable: IMPORT_typing.Callable[[IMPORT_google_protobuf_message.Message], IMPORT_typing.Awaitable],
        aborted_type: type[IMPORT_reboot.aio.aborted.Aborted],
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
    ) -> str:
        request = request_type()

        try:
            IMPORT_google_protobuf_json_format.Parse(json_request, request)
            response = await callable(request)
        except IMPORT_google_protobuf_json_format.ParseError as parse_error:
            aborted_error = IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"{parse_error}; "
                       "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
                )

            return IMPORT_json.dumps(
                {
                    'status': IMPORT_google_protobuf_json_format.MessageToDict(
                        aborted_error.to_status()
                    )
                }
            )
        except BaseException as exception:
            if isinstance(exception, aborted_type):
                return IMPORT_json.dumps(
                    {
                        'status': IMPORT_google_protobuf_json_format.MessageToDict(
                            exception.to_status()
                        )
                    }
                )
            raise
        else:
            return IMPORT_json.dumps(
                {
                    'response': IMPORT_google_protobuf_json_format.MessageToDict(
                        response
                    )
                }
            )

    async def _schedule(  # type: ignore[override]
        self,
        *,
        method: str,
        context: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        schedule: IMPORT_reboot_time_DateTimeWithTimeZone,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency],
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
    ) -> str:
        request = request_type()

        IMPORT_google_protobuf_json_format.Parse(json_request, request)

        if isinstance(context, IMPORT_reboot_aio_contexts.WriterContext):
            task = await getattr(
                UserServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ),
                method,
            )(request, schedule=schedule)

            return IMPORT_json.dumps(
                {
                    'taskId': IMPORT_google_protobuf_json_format.MessageToDict(
                        task.task_id
                    )
                }
            )

        # Add scheduling information to the metadata.
        metadata: IMPORT_reboot_aio_types.GrpcMetadata = (
            (IMPORT_reboot_aio_headers.TASK_SCHEDULE, schedule.isoformat()),
        )

        task_id = await getattr(super()._tasks(context), method)(
            request,
            idempotency=idempotency,
            metadata=metadata,
        )

        return IMPORT_json.dumps(
            {
                'taskId': IMPORT_google_protobuf_json_format.MessageToDict(task_id)
            }
        )

    async def _reader(  # type: ignore[override]
        self,
        method: str,
        context: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None

        if 'idempotency' in options:
            idempotency = IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=options['idempotency'].get('alias'),
                key=options['idempotency'].get('key'),
                how=(
                    IMPORT_reboot_aio_idempotency.ALWAYS
                    if options['idempotency'].get('always', False)
                    else IMPORT_reboot_aio_idempotency.PER_ITERATION
                    if options['idempotency'].get('perIteration')
                    else None
                ),
            )

        method_handle = IMPORT_functools.partial(
            getattr(super()._reader(context), method),
            bearer_token=options.get("bearerToken"),
            idempotency=idempotency,
        )
        return await self._call(
            callable=method_handle,
            aborted_type=getattr(
                User, method + 'Aborted'
            ),
            request_type=request_type,
            json_request=json_request,
        )

    async def _writer(  # type: ignore[override]
        self,
        method: str,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None

        if 'idempotency' in options:
            idempotency = IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=options['idempotency'].get('alias'),
                key=options['idempotency'].get('key'),
                how=(
                    IMPORT_reboot_aio_idempotency.ALWAYS
                    if options['idempotency'].get('always', False)
                    else IMPORT_reboot_aio_idempotency.PER_ITERATION
                    if options['idempotency'].get('perIteration')
                    else None
                ),
            )

        if 'schedule' in options:
            when = IMPORT_google_protobuf_timestamp_pb2.Timestamp()
            when.FromJsonString(options['schedule']['when'])
            return await self._schedule(
                method=method,
                context=context,
                schedule=IMPORT_reboot_time_DateTimeWithTimeZone.from_protobuf_timestamp(when),
                idempotency=idempotency,
                request_type=request_type,
                json_request=json_request,
            )

        method_handle = IMPORT_functools.partial(
            getattr(super()._writer(context), method),
            idempotency=idempotency,
            bearer_token=options.get("bearerToken"),
        )
        return await self._call(
            callable=method_handle,
            aborted_type=getattr(
                User, method + 'Aborted'
            ),
            request_type=request_type,
            json_request=json_request,
        )

    async def _transaction(  # type: ignore[override]
        self,
        method: str,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None

        if 'idempotency' in options:
            idempotency = IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=options['idempotency'].get('alias'),
                key=options['idempotency'].get('key'),
                how=(
                    IMPORT_reboot_aio_idempotency.ALWAYS
                    if options['idempotency'].get('always', False)
                    else IMPORT_reboot_aio_idempotency.PER_ITERATION
                    if options['idempotency'].get('perIteration')
                    else None
                ),
            )

        if 'schedule' in options:
            when = IMPORT_google_protobuf_timestamp_pb2.Timestamp()
            when.FromJsonString(options['schedule']['when'])
            return await self._schedule(
                method=method,
                context=context,
                schedule=IMPORT_reboot_time_DateTimeWithTimeZone.from_protobuf_timestamp(when),
                idempotency=idempotency,
                request_type=request_type,
                json_request=json_request,
            )

        method_handle = IMPORT_functools.partial(
            getattr(super()._workflow(context), method),
            idempotency=idempotency,
            bearer_token=options.get("bearerToken"),
        )
        return await self._call(
            callable=method_handle,
            aborted_type=getattr(
                User, method + 'Aborted'
            ),
            request_type=request_type,
            json_request=json_request,
        )

    async def _workflow(  # type: ignore[override]
        self,
        method: str,
        context: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None

        if 'idempotency' in options:
            idempotency = IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=options['idempotency'].get('alias'),
                key=options['idempotency'].get('key'),
                how=(
                    IMPORT_reboot_aio_idempotency.ALWAYS
                    if options['idempotency'].get('always', False)
                    else IMPORT_reboot_aio_idempotency.PER_ITERATION
                    if options['idempotency'].get('perIteration')
                    else None
                ),
            )

        assert 'schedule' in options

        when = IMPORT_google_protobuf_timestamp_pb2.Timestamp()
        when.FromJsonString(options['schedule']['when'])

        return await self._schedule(
            method=method,
            context=context,
            schedule=IMPORT_reboot_time_DateTimeWithTimeZone.from_protobuf_timestamp(when),
            idempotency=idempotency,
            request_type=request_type,
            json_request=json_request,
        )

class OpsTicketWeakReferenceNodeAdaptor(OpsTicket.WeakReference[OpsTicket.WeakReference._Schedule]):

    async def _call(  # type: ignore[override]
        self,
        *,
        callable: IMPORT_typing.Callable[[IMPORT_google_protobuf_message.Message], IMPORT_typing.Awaitable],
        aborted_type: type[IMPORT_reboot.aio.aborted.Aborted],
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
    ) -> str:
        request = request_type()

        try:
            IMPORT_google_protobuf_json_format.Parse(json_request, request)
            response = await callable(request)
        except IMPORT_google_protobuf_json_format.ParseError as parse_error:
            aborted_error = IMPORT_reboot.aio.aborted.SystemAborted(
                IMPORT_rbt_v1alpha1.errors_pb2.Unknown(),
                message=f"{parse_error}; "
                       "This is usually caused by a deeply nested protobuf message, which is not supported by protobuf.\n"
                        "See the limits here: https://protobuf.dev/programming-guides/proto-limits/"
                )

            return IMPORT_json.dumps(
                {
                    'status': IMPORT_google_protobuf_json_format.MessageToDict(
                        aborted_error.to_status()
                    )
                }
            )
        except BaseException as exception:
            if isinstance(exception, aborted_type):
                return IMPORT_json.dumps(
                    {
                        'status': IMPORT_google_protobuf_json_format.MessageToDict(
                            exception.to_status()
                        )
                    }
                )
            raise
        else:
            return IMPORT_json.dumps(
                {
                    'response': IMPORT_google_protobuf_json_format.MessageToDict(
                        response
                    )
                }
            )

    async def _schedule(  # type: ignore[override]
        self,
        *,
        method: str,
        context: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        schedule: IMPORT_reboot_time_DateTimeWithTimeZone,
        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency],
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
    ) -> str:
        request = request_type()

        IMPORT_google_protobuf_json_format.Parse(json_request, request)

        if isinstance(context, IMPORT_reboot_aio_contexts.WriterContext):
            task = await getattr(
                OpsTicketServicerTasks(
                    context=context,
                    state_ref=context._state_ref,
                ),
                method,
            )(request, schedule=schedule)

            return IMPORT_json.dumps(
                {
                    'taskId': IMPORT_google_protobuf_json_format.MessageToDict(
                        task.task_id
                    )
                }
            )

        # Add scheduling information to the metadata.
        metadata: IMPORT_reboot_aio_types.GrpcMetadata = (
            (IMPORT_reboot_aio_headers.TASK_SCHEDULE, schedule.isoformat()),
        )

        task_id = await getattr(super()._tasks(context), method)(
            request,
            idempotency=idempotency,
            metadata=metadata,
        )

        return IMPORT_json.dumps(
            {
                'taskId': IMPORT_google_protobuf_json_format.MessageToDict(task_id)
            }
        )

    async def _reader(  # type: ignore[override]
        self,
        method: str,
        context: IMPORT_reboot_aio_contexts.ReaderContext | IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None

        if 'idempotency' in options:
            idempotency = IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=options['idempotency'].get('alias'),
                key=options['idempotency'].get('key'),
                how=(
                    IMPORT_reboot_aio_idempotency.ALWAYS
                    if options['idempotency'].get('always', False)
                    else IMPORT_reboot_aio_idempotency.PER_ITERATION
                    if options['idempotency'].get('perIteration')
                    else None
                ),
            )

        method_handle = IMPORT_functools.partial(
            getattr(super()._reader(context), method),
            bearer_token=options.get("bearerToken"),
            idempotency=idempotency,
        )
        return await self._call(
            callable=method_handle,
            aborted_type=getattr(
                OpsTicket, method + 'Aborted'
            ),
            request_type=request_type,
            json_request=json_request,
        )

    async def _writer(  # type: ignore[override]
        self,
        method: str,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None

        if 'idempotency' in options:
            idempotency = IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=options['idempotency'].get('alias'),
                key=options['idempotency'].get('key'),
                how=(
                    IMPORT_reboot_aio_idempotency.ALWAYS
                    if options['idempotency'].get('always', False)
                    else IMPORT_reboot_aio_idempotency.PER_ITERATION
                    if options['idempotency'].get('perIteration')
                    else None
                ),
            )

        if 'schedule' in options:
            when = IMPORT_google_protobuf_timestamp_pb2.Timestamp()
            when.FromJsonString(options['schedule']['when'])
            return await self._schedule(
                method=method,
                context=context,
                schedule=IMPORT_reboot_time_DateTimeWithTimeZone.from_protobuf_timestamp(when),
                idempotency=idempotency,
                request_type=request_type,
                json_request=json_request,
            )

        method_handle = IMPORT_functools.partial(
            getattr(super()._writer(context), method),
            idempotency=idempotency,
            bearer_token=options.get("bearerToken"),
        )
        return await self._call(
            callable=method_handle,
            aborted_type=getattr(
                OpsTicket, method + 'Aborted'
            ),
            request_type=request_type,
            json_request=json_request,
        )

    async def _transaction(  # type: ignore[override]
        self,
        method: str,
        context: IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None

        if 'idempotency' in options:
            idempotency = IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=options['idempotency'].get('alias'),
                key=options['idempotency'].get('key'),
                how=(
                    IMPORT_reboot_aio_idempotency.ALWAYS
                    if options['idempotency'].get('always', False)
                    else IMPORT_reboot_aio_idempotency.PER_ITERATION
                    if options['idempotency'].get('perIteration')
                    else None
                ),
            )

        if 'schedule' in options:
            when = IMPORT_google_protobuf_timestamp_pb2.Timestamp()
            when.FromJsonString(options['schedule']['when'])
            return await self._schedule(
                method=method,
                context=context,
                schedule=IMPORT_reboot_time_DateTimeWithTimeZone.from_protobuf_timestamp(when),
                idempotency=idempotency,
                request_type=request_type,
                json_request=json_request,
            )

        method_handle = IMPORT_functools.partial(
            getattr(super()._workflow(context), method),
            idempotency=idempotency,
            bearer_token=options.get("bearerToken"),
        )
        return await self._call(
            callable=method_handle,
            aborted_type=getattr(
                OpsTicket, method + 'Aborted'
            ),
            request_type=request_type,
            json_request=json_request,
        )

    async def _workflow(  # type: ignore[override]
        self,
        method: str,
        context: IMPORT_reboot_aio_contexts.WriterContext | IMPORT_reboot_aio_contexts.TransactionContext | IMPORT_reboot_aio_contexts.WorkflowContext | IMPORT_reboot_aio_external.ExternalContext,
        request_type: type[IMPORT_google_protobuf_message.Message],
        json_request: str,
        json_options: str,
    ) -> str:
        options = IMPORT_json.loads(json_options)

        idempotency: IMPORT_typing.Optional[IMPORT_reboot_aio_idempotency.Idempotency] = None

        if 'idempotency' in options:
            idempotency = IMPORT_reboot_aio_contexts.Context.idempotency(
                alias=options['idempotency'].get('alias'),
                key=options['idempotency'].get('key'),
                how=(
                    IMPORT_reboot_aio_idempotency.ALWAYS
                    if options['idempotency'].get('always', False)
                    else IMPORT_reboot_aio_idempotency.PER_ITERATION
                    if options['idempotency'].get('perIteration')
                    else None
                ),
            )

        assert 'schedule' in options

        when = IMPORT_google_protobuf_timestamp_pb2.Timestamp()
        when.FromJsonString(options['schedule']['when'])

        return await self._schedule(
            method=method,
            context=context,
            schedule=IMPORT_reboot_time_DateTimeWithTimeZone.from_protobuf_timestamp(when),
            idempotency=idempotency,
            request_type=request_type,
            json_request=json_request,
        )


# yapf: enable
